---
epic: 5
date: 2026-09-24
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Retrospectiva da Épica 5 — Checkout e pagamento assíncrono

## Resumo da Épica

**Épica 5**, onze estórias, todas `done` no `sprint-status.yaml`. `detect-epic --epic 5` devolveu `story_count: 11` e `pending_stories: []` — nenhuma estória ficou para trás, e o veredito de máquina não é forçado a `rejected`.

**Faixa de diff:** `a454444^..4cbea8b` — a mesma que o passeio da `bmad-checkpoint-preview` leu (`HANDOFF.md:96`). 26 commits, nenhum merge (`merge_count: 0`, `merges_measured: 0`), 73 arquivos de código e artefato, **+10.139 / −861** nos caminhos `internal/ api/ web/ db/ cmd/`. O commit `64e6119` (retro da Épica 3) cai dentro da faixa sem pertencer a ela.

| Estória | Commit de implementação |
|---|---|
| 5.1 | `a454444` (+ `876ff56`, aprovação humana) |
| 5.2 | `400861b` |
| 5.3 | `dbcb2b8` |
| 5.4 | `6e5984f` |
| 5.5 | `f3cc0ff` |
| 5.6 | `9174f88` |
| 5.7 | `7cd26ac` (só teste) |
| 5.8 | `e421de8` |
| 5.9 | `577a6aa` |
| 5.10 | `22500bf` |
| 5.11 | `8a3dfd0` |

**Depois da faixa**, e fora dela, os defeitos do passeio foram corrigidos em `main` em 2026-09-24: `9240730` (D3, D5 — `spec-d3-d5-preco-por-item-sob-a-trava.md`), `ab4558c` (D2, D6, D7 — `spec-dialog-e-foco-d2-d6-d7-e2-e3.md`), `33ca441` (D1, D4 — `spec-d1-d4-e1-e4-e5.md`). As três specs estão `status: done`.

**Inventário de evidências — o que havia:**

- Spec da épica com Condições de Aceite declaradas por estória: `_bmad-output/planning-artifacts/epics.md:1270–1566`, mais os requisitos transversais em `:370–390`. Veredito `declared`.
- Contexto compilado: `epic-5-context.md`. Onze specs de estória, uma por estória, em `implementation-artifacts/`.
- `deferred-work.md`: **56 entradas** com origem na Épica 5, de todas as onze specs (a 5.6 sozinha deixou 14).
- `HANDOFF.md`: o registro do passeio humano da Épica 5 (`:96–133`), com D1–D7, O1, O2 e a nota sobre o AD-4.
- Retrospectiva anterior: `epic-3-retro-2026-09-19.md`. Não há retro da Épica 4 (`epic-4-retrospective: optional`).
- Suíte executada nesta retro, no `HEAD` `33ca441`: `go test ./...` verde (todos os pacotes `ok`), `npm test` no `web/` com 136 de 136.

**O que faltou:**

- **Sem registros de sessão.** As lições de processo abaixo saem de commits, specs, `deferred-work.md` e `HANDOFF.md`, nunca de sessão — escopo estreitado.
- **O `bmad-review` não foi invocado sobre a faixa.** Esta retro rodou como subagente headless, e a faixa exata já tinha passado pelo passeio humano com quatro subagentes aprofundando (`HANDOFF.md:104–106`) e por uma revisão por estória. As lentes adversária, de borda e de lacuna de verificação foram aplicadas **inline, estreitadas às fronteiras entre estórias** (5.5↔5.6, 5.6↔5.7, 5.8↔5.11, 5.10↔5.11). Registro do estreitamento.
- **O passeio só pelo teclado e com leitor de tela nunca foi feito** (`HANDOFF.md:131`, `:75`). É lacuna de verificação conhecida — não há resultado a relatar.
- **Visões agregadas derivadas inline**, por `git diff`, `wc` e `grep`; nenhuma ferramenta de grafo de dependência além do próprio `internal/fronteira_test.go`.

## Achados

### Delta de arquitetura

**A1 — A Épica 5 acrescentou três arestas ao grafo real, e nenhuma ao grafo da espinha · CONFIRMADO · agrava item aberto**

`git diff a454444^..4cbea8b -- internal/fronteira_test.go` mostra uma aresta nova na tabela: `plataforma → carrinho` (`internal/fronteira_test.go:27`), para traduzir os sentinelas do `carrinho`. Além dela, duas setas já existentes passaram a carregar portas novas: `pedido → carrinho` ganhou `ConfirmarPrecoVisto` (5.5) e `pedido → pagamento` ganhou `TemConfirmacaoPendente` (5.11, `HANDOFF.md:3`). O grafo do AD-1 em `ARCHITECTURE-SPINE.md:51–68` rotula `pedido → carrinho` só com `Esvaziar` e `pedido → pagamento` só com `IniciarTentativa · ConfirmacoesNaoAplicadas`, e não tem `plataforma`. A Estória 7.2 desenha o diagrama a partir dele. **Não gera item novo:** é o `epic-3-retro-item-18` (e o `epic-1-retro-item-12`), ainda abertos, com três divergências a mais.

### Crescimento de arquivo

**A2 — `internal/pedido/pedido.go` dobrou na épica · CONFIRMADO · aceitar, observar**

424 linhas em `a454444^`, 863 em `4cbea8b` (+439 líquidas, a maior churn de produção da faixa), e 1.196 no `HEAD`. A máquina em si foi para `maquina.go` (331) e o Frete para `frete.go` — a divisão por arquivo aconteceu. O que resta em `pedido.go` é o caso de uso de cada estória (`Criar`, `NovaTentativa`, `Expirar`, `Cancelar`…), e `SimularEntrega` e `Expirar` já dividem a casca `avancarPeloTempo` (`HANDOFF.md:3`). **Aceitar:** tamanho de módulo de domínio central, não god-class; registrado para que a Épica 7 (7.3, addendum contra o código) não o descubra como novidade.

### Reconciliação spec ↔ implementação

**A3 — A CA 🔒 da 5.6 ("recalcula sob o mesmo bloqueio… `TOTAL_DIVERGENTE` se o total mudou") foi embarcada só pelo total · CONFIRMADO · já corrigido**

`epics.md:1428–1431` pede a reconferência sob a trava; `pedido.go:282` (na faixa) conferia subtotal e Frete, não preço por Item, e dois preços que se compensam passavam — reproduzido na tela como o Pedido AZ-2026-000035 (`HANDOFF.md:111`, D3). O único teste de `TOTAL_DIVERGENTE` recusava no passo 2, então a checagem sob a trava podia ser apagada com a suíte verde (D5, `HANDOFF.md:112`). Corrigido em `9240730`: `internal/pedido/pedido.go:274` agora recusa todo Item com `produto.PrecoCentavos != item.PrecoVistoCentavos`, e três subtestes em `api/pedido_test.go` a prendem, inclusive no lugar (passo 5).

**A4 — A prova dessa CA 🔒 foi adiada duas vezes antes de o humano achar o defeito · CONFIRMADO · lição de processo**

A lacuna estava registrada: `deferred-work.md` tem, da 5.6, "O `TOTAL_DIVERGENTE` do passo 5 … não tem teste determinístico", e, da 5.7, "A bancada da 5.7 não prende o `TOTAL_DIVERGENTE` do passo 5". A estória 🔒 fechou com a prova da sua guarda no ledger, e o defeito que essa prova teria pego (A3) atravessou a épica. O contraste está na mesma épica: a 5.7 escreveu `travaDeReservaSerializa` (`api/concorrencia_test.go:189`), que **morre quando o `FOR UPDATE OF p` some** (`HANDOFF.md:3`). **Lição:** guarda de CA 🔒 não fecha sem o teste que morre quando ela sai; adiar essa prova é adiar a CA.

**A5 — O passo 5 do Roteiro B não era demonstrável sem reconfigurar · CONFIRMADO · já corrigido**

A CA da 5.11 (`epics.md:1561–1564`) pressupõe um Pedido de total `,95`–`,99`; o Catálogo Semeado não tinha nenhum Produto que fechasse assim (D1, `HANDOFF.md:110`). Corrigido em `33ca441` (`db/semente/001_catalogo_semeado.sql:42`, `media/gerar.go:86`, 3995 centavos). Ressalva registrada na spec: a `VersaoSemente` não mudou, então banco já semeado só vê o preço novo com `docker compose down -v` (`HANDOFF.md:67`) — a 7.4 ensaia de clone limpo, então não afeta a apresentação.

**A6 — O1: "Confirmar o novo preço" é trava só de tela · CONFIRMADO · decisão humana**

A entrada no checkout grava a ciência do preço ao reportar (AD-17, CA da 5.5 em `epics.md:1398–1403`); recarregar ou digitar `/checkout/revisao` dispensa o clique (`HANDOFF.md:119–121`). O D3 não fecha isso: ele compara com `PrecoVistoCentavos`, que a entrada já atualizou. A FR-19 pede "exibido e sinalizado", e ele é exibido. Hoje a garantia do servidor é: nenhum Item nasce com preço diferente do que a entrada registrou. **Decisão de Sung** — aceitar como leitura da FR-19 ou exigir ciência explícita no servidor.

**A7 — A ordem global de travas do AD-4 não cita o contador do ano · CONFIRMADO · reconciliar a espinha**

`ARCHITECTURE-SPINE.md:117` diz "pedido → produtos"; `Criar` pega `ProximoNumeroDoAno` antes das duas (`deferred-work.md`, entradas da 5.6 e da 5.7, "PARCIAL"), e o comentário de `db/migracoes/20260921120000_pedido_criacao.sql:15` diz que o gêmeo espera no índice, quando espera no contador (`HANDOFF.md:124–126`). Sem ciclo de travas (confirmado pelo passeio), mas a espinha é o que a 7.3 lê. **Emendar o AD-4 no lugar, com memlog.**

**A8 — `correlacao` em `pedido.transicao_status` segue ausente · CONFIRMADO · aceito, registrado**

NFR-9/AD-15 pedem correlação em toda transição; o adiamento está no addendum §10 e na resolução do `epic-1-retro-item-8`, e a primeira entrada da 5.1 no `deferred-work.md` o repete. Desvio aceito e rastreado — registrado aqui para não voltar como novidade.

### Lentes de código nas fronteiras entre estórias (inline, estreitado)

**A9 — Nenhum teste mata a remoção do `ORDER BY id` na trava da Reserva · CONFIRMADO · corrigir**

`travaDeReservaSerializa` usa um Produto só (`api/concorrencia_test.go:197`, `:212`), e o passeio confirmou que tirar o `ORDER BY id` não é pego (`HANDOFF.md:128–129`). É exatamente a armadilha que o `AGENTS.md` nomeia: dois Pedidos com os mesmos dois Produtos em ordem inversa travando um no outro (AD-4, AD-5). A 5.7 prova a soma sob a trava, não a ordem.

**A10 — `Corrente` é lido fora da trava do Pedido (fronteira 5.10↔5.11) · CONFIRMADO · adiar**

Uma nova Tentativa que comite entre a leitura da inbox e o `TravarPedido` deixaria uma recusa atrasada da Tentativa 1 liberar a Reserva da 2 (`deferred-work.md`, entrada da 5.10; a da 5.11 diz "ATUALIZADO, e não fechado", e que a 5.11 estreita a janela). Com o Provedor Simulado, a recusa da Tentativa 1 é aplicada antes de a 2 poder começar — o caminho não parece alcançável hoje, e o ledger já carrega o contexto. **Adiar**, sem item.

**A11 — Fronteira 5.8↔5.11: o instante da expiração e o relógio da tela saem da mesma fonte · CONFIRMADO · limpo**

Os dois saem de `max(ocorrido_em)` do histórico (`HANDOFF.md:3`), e o passeio viu `,95` expirar em 60 s exatos com a nova Tentativa sobrevivendo ao recarregamento (`HANDOFF.md:100–101`). Checado e limpo.

### Divergência de padrão

**A12 — Cinco commits `docs` da faixa em português · CONFIRMADO · aceitar**

`c0c0928`, `9486000`, `c707ff6`, `47073e6`, `9de32e5` ("HANDOFF aponta a …") contra a convenção do `AGENTS.md` (commit em inglês). A partir de `7b0f84c` a convenção voltou e ficou. Autocorrigido na própria épica; registrado para não reaparecer.

### O que a épica fez bem (com fonte)

- **O ledger voltou a ser alimentado**: 56 entradas, das onze specs, com fechamento explícito (`RESOLVIDO`, `RESOLVIDO EM PARTE`, `ATUALIZADO`) de entradas de estórias anteriores — o item 23 da retro da Épica 3 pegou.
- **Os três 🔒 existem e têm prova de pé**: FR-24 (criação atômica, 5.6), NFR-7 (`api/concorrencia_test.go`, oito paralelos na última unidade), FR-34 (`api/expiracao_test.go`, 414 linhas).
- **`EM_SEPARACAO` → `SEPARANDO`** foi decidido pelo glossário e reescrito por migração (`db/schema_test.go:258`).

## Verificação de comportamento

Não exercitada por esta retro no navegador. O que existe, com fonte:

- **Passeio humano de 2026-09-24** sobre a faixa exata (`HANDOFF.md:96–106`): Carrinho → Endereço → Revisão → Pedido nos três desfechos (`,00` aprovado em ~6 s virando Detalhe sem recarregar; `,90` recusado com motivo, Estoque de volta e "Tentar pagar de novo" chegando a `PAGO`; `,95` expirando em 60 s), Frete por região, `Idempotency-Key` estável, `TOTAL_DIVERGENTE`, a última unidade levada por outro, e nada rolando a 360 px (por `iframe`).
- **Suíte nesta retro**, no `HEAD` `33ca441`: `go test ./...` verde; `npm test` 136/136.
- **Não feito:** o passeio só pelo teclado a 1440 px e com leitor de tela (`HANDOFF.md:131`). Os D2/D6/D7 de foco corrigidos em `ab4558c` foram vistos no navegador só no passo Endereço e no `Esc` do Carrinho (`HANDOFF.md:66`). O `NFR-11 · UX-DR14` ("percorrer a UJ-1 inteira sem tocar no mouse", `epics.md:385`) **nunca foi checado** para o checkout — não é "checado e limpo".

## Acompanhamento das retros anteriores

Fonte: `action_items` em `sprint-status.yaml`. Há itens abertos das Épicas 1 e 3; a Épica 4 não teve retro. Headless: **nenhum status foi escrito**; as transições abaixo são só propostas.

| Selector (`id`) | Chegou? | Evidência | Status que ela sustenta |
|---|---|---|---|
| `epic-1-retro-item-3-fechar-o-furo-do-ad-1-acrescentar-media` | Não | `internal/fronteira_test.go:35` segue `"media": {}`; `:105` sem `-tags` | — |
| `epic-1-retro-item-5-por-um-portao-automatizado-sobre-go-test` | Não | sem `.github/workflows`; só o `prebuild` do `web/` (`web/package.json:7`) | — |
| `epic-1-retro-item-7-limitar-o-dominio-de-azamon_provedor_-_a` | Não | `internal/plataforma/config.go:222` só exige positivo; não há teto 99 | — |
| `epic-1-retro-item-9-marcar-resolvido-na-primeira-entrada-de` | Não | `deferred-work.md:6–7` sem marca | — |
| `epic-1-retro-item-10-levar-ao-deferred-work-md-os-achados-ain` | Não | `grep` por `VersaoSemente`, `AD-19` e o `CHECK` da soma no ledger: zero | — |
| `epic-1-retro-item-12-reconciliar-o-grafo-do-ad-1-na-espinha-c` | Não | `ARCHITECTURE-SPINE.md:51–68`; agravado pelo A1 | — |
| `epic-3-retro-item-13-apontar-a-sonda-do-nfr-4-db-medicao_nfr4` | Não | `db/medicao_nfr4_test.go:31` ainda "a forma que a Épica 3 vai implementar" | — |
| `epic-3-retro-item-14-por-o-portao-automatizado-sobre-go-test` | Não | idem item 5 | — |
| `epic-3-retro-item-15-acrescentar-uma-verificacao-de-largura-d` | Nenhuma evidência no laço do dev | só passeios humanos a 360 px por `iframe` | — |
| `epic-3-retro-item-16-reconciliar-epics-md-1041-o-verde-availa` | Não | a CA mudou de linha (`epics.md:1020`) e continua "**só** para Estoque disponível" | — |
| `epic-3-retro-item-17-fechar-o-furo-do-ad-1-media-catalogo-na` | Não | idem item 3 | — |
| `epic-3-retro-item-18-reconciliar-o-grafo-do-ad-1-em-architect` | Não | idem item 12 | — |
| `epic-3-retro-item-19-reconciliar-em_separacao-architecture-sp` | **Sim** | `ARCHITECTURE-SPINE.md:94–97` e `addendum.md:281` dizem `SEPARANDO`; o código foi ao glossário na 5.1 e a migração reescreve dados antigos (`db/schema_test.go:258`) | **`done`** |
| `epic-3-retro-item-20-reescrever-o-item-9-da-retro-da-epica-1` | Nenhuma evidência | — | — |
| `epic-3-retro-item-21-decidir-sobre-a-triplicacao-dos-cruds-ad` | Nenhuma evidência de decisão | — | — |
| `epic-3-retro-item-22-remover-a-linha-redundante-da-tabela-de` | Não | `ARCHITECTURE-SPINE.md:232–233` seguem as duas linhas | — |
| `epic-3-retro-item-23-voltar-a-alimentar-o-deferred-work-md-e` | **Sim, na Épica 5** | onze specs, uma por estória; 56 entradas no ledger, de todas as onze | **`done`** |
| `epic-3-retro-item-24-rodar-a-retrospectiva-da-epica-2-epic-2` | Não | `epic-2-retrospective: optional`, sem documento | — |

## Itens de ação

Todos propostos, nenhum aplicado. Dono: Sung.

1. **Fazer o passeio só pelo teclado, com leitor de tela, da UJ-1 no checkout** — Carrinho → Endereço (com o `Dialog` de novo Endereço) → Revisão → Pedido nos três desfechos, a 1440 px. Fonte: `HANDOFF.md:131`, `epics.md:385` (NFR-11 · UX-DR14). *Verificação.*
2. **Acrescentar o teste que morre sem o `ORDER BY id`** — dois Pedidos com os mesmos dois Produtos em ordem inversa, na bancada da 5.7. Fonte: A9, `HANDOFF.md:128–129`, `api/concorrencia_test.go:189`. *Remediação.*
3. **Emendar o AD-4 em `ARCHITECTURE-SPINE.md:117`** para nomear o contador do ano na ordem global de travas, com `memlog.py append`, e corrigir o comentário de `db/migracoes/20260921120000_pedido_criacao.sql:15`. Fonte: A7. *Reconciliação de spec, aguarda aplicação humana.*
4. **Decidir O1** — a ciência do preço visto como trava só de tela: aceitar como leitura da FR-19 ou exigir no servidor. Fonte: A6, `HANDOFF.md:119–121`. *Decisão.*
5. **Regra de processo: CA 🔒 não fecha com a prova da sua guarda no `deferred-work.md`** — a estória entrega o teste que morre quando a guarda sai, no molde de `travaDeReservaSerializa`. Fonte: A4. *Lição de processo.*

## Veredito de aceite

**`accepted-with-open-items`** · critérios **declarados** (`epics.md:1270–1566` e transversais `:370–390`) · veredito **de máquina**, sem decisão humana.

- Todas as onze estórias `done`; `pending_stories` vazia.
- Os três 🔒 (FR-24, FR-34, NFR-7) existem, com teste, e a suíte está verde no `HEAD`.
- Os dois desvios de CA que o passeio achou — a reconferência por Item da 5.6 (A3) e o passo 5 do Roteiro B da 5.11 (A5) — estão corrigidos em `main` (`9240730`, `33ca441`), com prova.
- **Ficam abertos e rastreados:** o passeio só pelo teclado (NFR-11, nunca checado), a prova da ordem da trava (A9), a emenda do AD-4 (A7), a decisão O1 (A6), e a `correlacao` (A8, desvio aceito).

## Perguntas em aberto

- **O passeio só pelo teclado pode mudar o veredito.** O NFR-11 é CA transversal declarada e nunca foi exercido no checkout; se ele achar um bloqueio (foco preso, passo inalcançável), o veredito passa a `rejected` até a correção.
- **O1:** a FR-19 aceita a ciência gravada ao reportar, ou pede o clique registrado no servidor?
- **O2** (`HANDOFF.md:122–123`): a seta no grupo de Endereços move o foco sem marcar. Defeito real ou artefato da injeção de tecla? Só o teclado de verdade decide — e cai no mesmo passeio do item 1. Relacionado: a entrada da 5.2 no `deferred-work.md` diz que o `RadioGroupItem` estiliza por `data-checked` e o Radix marca por `data-state` (`web/components/ui/radio-group.tsx:28`); não verificado na tela por esta retro.
- **A10** fica adiado assumindo que a recusa atrasada da Tentativa 1 não é alcançável com o Provedor Simulado. Se o time discordar, vira estória.

## Suposições (headless)

- Épica **5** tomada da invocação (`-H 5`), não detectada.
- `detect-epic --epic 5`: `story_count: 11`, `pending_stories: []`, `retro_status: optional`.
- Faixa `a454444^..4cbea8b` escolhida pelo primeiro commit `feat(5.1)` e pelo último `docs(5.11)`, e confere com a faixa do passeio humano (`HANDOFF.md:96`). As correções `9240730`, `ab4558c` e `33ca441` foram lidas como pós-épica.
- Veredito `accepted-with-open-items` rendido pela máquina, sem decisão humana.
- O passeio só pelo teclado foi tratado como lacuna conhecida, sem resultado inventado.
- O `bmad-review` não foi invocado; as lentes foram aplicadas inline sobre as fronteiras entre estórias.
- Cada um dos cinco itens de ação é proposta desta retro, não compromisso do time.
- As transições `epic-3-retro-item-19 → done` e `epic-3-retro-item-23 → done` foram só propostas; `--set-action-status` não foi passado.
