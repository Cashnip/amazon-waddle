---
epic: 6
date: 2026-09-24
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Retrospectiva da Épica 6 — Pedidos: acompanhamento, cancelamento e operação

## Resumo da Épica

**Épica 6**, sete estórias, todas `done` no `sprint-status.yaml`. `detect-epic --epic 6` devolveu `story_count: 7` e `pending_stories: []` — o veredito de máquina não é forçado a `rejected`.

**Faixa de diff:** `48f6723^..84a94f8` — do `feat(6.1)` até o `docs(6.4)` que registra a correção do painel. 16 commits, nenhum merge (`merge_count: 0`, `merges_measured: 0`), 48 arquivos, **+7.218 / −352**. A faixa inclui `7014591` (correção dos dois defeitos do painel da 6.4, spec `spec-6-4-confirmacao-e-anuncio-do-avanco-de-status.md`, `done`). O passeio humano leu `4cbea8b..84a94f8` sem a 6.4, que teve passeio próprio (`HANDOFF.md:135–150`).

| Estória | Commit de implementação |
|---|---|
| 6.1 | `48f6723` |
| 6.2 | `5f618db` |
| 6.3 | `b6093e9` |
| 6.4 | `081a60e`, correção `7014591` |
| 6.5 | `5b01aec` |
| 6.6 | `cd27088` (só teste) |
| 6.7 | `aadb350` |

**Depois da faixa**, os defeitos E1–E5 do passeio foram corrigidos em `main` em 2026-09-24: `ab4558c` (E2, E3 — `spec-dialog-e-foco-d2-d6-d7-e2-e3.md`) e `33ca441` (E1, E4, E5 — `spec-d1-d4-e1-e4-e5.md`), as duas `status: done`.

**Inventário de evidências — o que havia:**

- Spec da épica com Condições de Aceite declaradas: `_bmad-output/planning-artifacts/epics.md:1568–1747` e os transversais em `:370–390`. Veredito `declared`.
- `epic-6-context.md`; sete specs de estória e a spec de correção da 6.4.
- `deferred-work.md`: **15 entradas** com origem na Épica 6; a 6.2 não deixou nenhuma (ela não mudou Go — `HANDOFF.md:3`).
- `HANDOFF.md`: o passeio da Épica 6 (`:150–181`), o da correção da 6.4 (`:135–148`), e o que cada estória deixou para a seguinte (`:183–231`).
- Retrospectiva anterior: `epic-5-retro-2026-09-24.md`, escrita nesta mesma sessão, logo antes desta.
- Suíte executada nesta sessão no `HEAD` `33ca441`: `go test ./...` verde, `npm test` 136/136.

**O que faltou:**

- **Sem registros de sessão** — escopo de lição de processo estreitado a commits, specs, ledger e `HANDOFF.md`.
- **O `bmad-review` não foi invocado.** Subagente headless; a faixa já passou pelo passeio humano e por revisão por estória. Lentes aplicadas inline, estreitadas às fronteiras 6.3↔6.4↔6.5↔6.6 (as que dividem `api/sessao_test.go` e a tela do Pedido).
- **O passeio só pelo teclado e com leitor de tela nunca foi feito** (`HANDOFF.md:75`, `:180`). Lacuna conhecida, sem resultado a relatar.

## Achados

### Delta de arquitetura

**B1 — Nenhuma aresta nova no AD-1 · CONFIRMADO · limpo**

`git diff 48f6723^..84a94f8 -- internal/fronteira_test.go` é vazio. O Comprador do Detalhe administrativo vem por `identidade.BuscarComprador`, pela porta, sem JOIN entre schemas; o sinal da 6.5 vem da porta nova `pagamento.PedidosComAprovacaoSinalizada` e é juntado em Go (`HANDOFF.md:3`). As portas novas continuam fora dos rótulos do grafo da espinha — o mesmo item aberto da Épica 3 (`epic-3-retro-item-18`).

### Crescimento de arquivo

**B2 — `internal/pedido/pedido.go` chegou a 1.196 linhas, com três blocos que não se tocam · CONFIRMADO · aceitar, decisão de Sung**

863 linhas em `48f6723^`, 1.186 em `84a94f8`, 1.196 no `HEAD` — era 424 antes da Épica 5. Pelos símbolos: criação e ações do Comprador (`Criar`, `NovaTentativa`, `Cancelar`, até `:470`), leituras e painel (`Detalhar`, `Listar`, `ListarParaAdministrador`, `DetalharParaAdministrador`, `TransicionarPeloAdministrador`, `:475–910`), e relógio (`Varrer`, `SimularEntrega`, `Expirar`, `avancarPeloTempo`, `:915–1196`). Não é god-class — é um módulo com uma porta —, mas os três blocos já têm fronteira natural de arquivo, como `maquina.go` e `frete.go` tiveram. Registrado para a 7.3.

### Reconciliação spec ↔ implementação

**B3 — O avanço de Status do Administrador nasceu sem o `Dialog` que a EXPERIENCE exige · CONFIRMADO · já corrigido**

`081a60e` aplicava a transição no clique; `EXPERIENCE.md:131`, `:193`, `:272` pedem `Dialog` (spec da correção, seção Intent). E, com a Tabela filtrada, o sucesso não era anunciado. Corrigido na própria faixa por `7014591`. A regra estava na espinha de UX e não na CA da 6.4 (`epics.md:1650–1681`), e a revisão da estória não a pegou.

**B4 — O `Dialog` aberto por estado, sem devolver o foco, foi replicado em oito lugares · CONFIRMADO · já corrigido na raiz**

E2 (`HANDOFF.md:167`) é "o D6 de novo": o cancelamento do Comprador (6.3) e o avanço do Administrador (6.4) repetiram o padrão do novo Endereço da 5.2. `ab4558c` corrigiu no `DialogContent` compartilhado (`web/components/ui/dialog.tsx:79–85`), e não por tela — a spec diz que isso corrige os oito `Dialog`s de uma vez. Duplicação entre estórias que nenhuma sessão via, fechada no ponto único. A armadilha que ficou está registrada: **não pôr `autoFocus` dentro de `Dialog`** (`HANDOFF.md:66`), e o foco ainda cai no `body` quando o botão que abriu some ao confirmar (`deferred-work.md`, entrada da spec de `Dialog`).

**B5 — A decisão dos 14px do selo vive fora da espinha de UX · CONFIRMADO · reconciliar a espinha**

A CA da 6.7 foi atendida com o selo em 14px por decisão humana de 2026-09-24 (`HANDOFF.md:3`), mas o `DESIGN.md` ainda diz ao mesmo tempo "`Badge` sem alteração" (`DESIGN.md:170`) e "no mínimo 14px" (`DESIGN.md:141`), e o marcador "Pagamento aprovado" da 6.5 segue em 12px (`deferred-work.md`, entrada da 6.7; `HANDOFF.md:194–196`). A política do `AGENTS.md` manda registrar no `.memlog.md` da UX.

**B6 — "O Administrador não cancela Pedido" é tabela, não `if` · CONFIRMADO · limpo**

A tabela do AD-3 não tem linha de `AtorAdministrador` para `CANCELADO`, e não existe rota de cancelamento administrativo (`HANDOFF.md:3`; UX-DR11 em `epics.md:348` e `:1666`). Checado e limpo.

### Lentes de código nas fronteiras entre estórias (inline, estreitado)

**B7 — Três estórias declararam "por ÚLTIMO" o seu subteste no mesmo `TestSessaoEProduto` · CONFIRMADO · corrigir**

`api/sessao_test.go:566–569` diz que o painel da 6.4 "fica por ÚLTIMO de propósito", porque a Tabela sem filtro é global; mas a 6.5 foi registrada depois dele (`:573–580`) e a 6.6 depois das duas, também "por ÚLTIMO" (`:581–588`), deixando para trás um Pedido que faz **toda** chamada seguinte a `SimularEntrega` falhar (`HANDOFF.md:202–204`). O comentário da 6.4 já é falso, e a 6.5 lê a Tabela com `por_pagina=100` para não ser empurrada para fora (`HANDOFF.md:226–228`). São três estórias, cada uma certa sozinha, construindo uma cadeia de dependência de ordem sobre estado global que a próxima estória que tocar Pedido vai quebrar sem saber.

**B8 — O `de` enviado pela tela não tem teste que morra quando muda · CONFIRMADO · corrigir**

É a base inteira da distinção entre `ESTADO_JA_AVANCADO` e `TRANSICAO_INVALIDA` na 6.4 (`deferred-work.md`, entrada da 6.4; `HANDOFF.md:139–140`: "o `de` enviado é capturado no clique, não no confirmar"). A correção `7014591` mudou justamente quando o `de` é capturado, e a corrida só foi exercida pelo teste de `desfechoDaTransicao`, não pela tela (`HANDOFF.md:145–146`).

**B9 — O sinal da 6.5 é `false` fixo no 200 da transição administrativa · CONFIRMADO · aceito, registrado**

Correto hoje porque o destino do Administrador nunca é `CANCELADO`; passa a mentir se o AD-3 um dia der essa linha a ele. O comentário em `api/pedido_admin.go` diz onde a leitura entra (`HANDOFF.md:216–218`). Desvio consciente, sem item.

**B10 — O3: o `Dialog` do Comprador não pausa a consulta de 10 s · CONFIRMADO · aceito por decisão humana**

`HANDOFF.md:173–175` e `:66`: aceito sem mudança; a corrida termina com a frase certa. Registrado para não voltar.

### Processo

**B11 — O `HANDOFF.md` contradiz a si mesmo sobre o estado das Épicas 5 e 6 · CONFIRMADO · corrigir**

`HANDOFF.md:64–67` dá D1–D7 e E1–E5 como corrigidos; `:71–75`, no mesmo bloco "Próximo passo", ainda diz "D1–D7 abertos para o `bmad-build`", "E1–E5 abertos" e "`epic-6-retrospective` continua `optional`". A linha 3 é um parágrafo único que acumula o relato de 42 estórias. O `HANDOFF.md` teve +282 líquidas na Épica 5 (`git_evidence`) e está com 743 linhas. É a porta de entrada declarada no `AGENTS.md`, e a Épica 7 é quem a lê primeiro.

**B12 — A colheita do ledger caiu de 56 entradas na Épica 5 para 15 na 6 · CONFIRMADO · observar**

Por estória: 5,1 na Épica 5 contra 2,1 na 6, e a 6.2 com zero. Diferente da queda da Épica 3, há causa legível: a 6.2, a 6.6 e a 6.7 não mudaram Go (`HANDOFF.md:3`), e boa parte das entradas da 6 fecha entradas da 5 (`RESOLVIDO`, `DECIDIDO`). Sem item; registrado para a próxima retro comparar.

### O que a épica fez bem (com fonte)

- **Prova onde havia só afirmação:** a 6.6 prendeu as três condições da FR-33 sem mudar produção (`api/simulacao_test.go`), e um terceiro arranque do binário retoma Pedidos deixados pelo desligado (`cmd/azamon/main_test.go`).
- **Um ponto só para o Status na tela:** `SeloDoStatus` + `aparenciaDoSelo` em `web/lib/pedido.ts`, com duas guardas de fonte em `web/scripts/pedido.test.mjs` que derrubam o `prebuild` se alguém pintar de verde fora de `ENTREGUE` e Estoque (`HANDOFF.md:187–191`) — a UX-DR7 virou mecanismo.
- **A trava antes do CAS no cancelamento** acertou o duplo clique e a corrida com a aprovação, e fechou a entrada da 5.1 no ledger (`HANDOFF.md:3`; `deferred-work.md`, "RESOLVIDO" da 6.3).

## Verificação de comportamento

Não exercitada por esta retro no navegador. O que existe, com fonte:

- **Passeio humano de 2026-09-24** (`HANDOFF.md:150–162`), a 1440 px: marcador "Pagamento aprovado" só no Pedido certo e `Alert` persistente (6.5); Meus pedidos com paginação que sobrevive ao recarregamento e `?pagina=9` (6.1); um `,00` indo a "Entregue" sem recarregar com a linha do tempo completa (6.2); cancelar em `SEPARANDO` com o Estoque de volta, a corrida perdida com a frase certa, e cancelar em `PAGAMENTO_RECUSADO` sem rollback (6.3); os sete tons nas quatro superfícies e 360 px por `iframe` (6.7).
- **Painel da 6.4** aberto a 1440 px com mouse depois de `7014591` (`HANDOFF.md:145–146`).
- **Suíte** no `HEAD`: `go test ./...` verde; `npm test` 136/136.
- **Nunca checado:** o passeio só pelo teclado e com leitor de tela (NFR-11 · UX-DR14, `epics.md:385`); o cancelamento pela tela em `AGUARDANDO_PAGAMENTO` (sem Produto de `,95` na hora; possível desde `33ca441`); o estado vazio de Meus pedidos; `AZAMON_ENTREGA_SIMULACAO_ATIVA=false` (6.6) — todos em `HANDOFF.md:177–180`. Depois das correções: os `Dialog`s de cancelamento e de avanço de Status não foram reabertos na tela (`HANDOFF.md:66`), e E1, E4, E5 não foram abertos no navegador (`HANDOFF.md:67`).

## Acompanhamento das retros anteriores

Fonte: `action_items` em `sprint-status.yaml`. Headless: **nenhum status escrito**. Os itens das Épicas 1 e 3 foram conferidos um a um em `epic-5-retro-2026-09-24.md` (seção de mesmo nome), e a Épica 6 não mudou nenhum deles — `internal/fronteira_test.go` não mudou na faixa, não há `.github/workflows`, e o `ARCHITECTURE-SPINE.md` não foi tocado pela Épica 6. As duas transições que aquela retro propôs continuam propostas:

| Selector (`id`) | Chegou? | Evidência | Status que ela sustenta |
|---|---|---|---|
| `epic-3-retro-item-19-reconciliar-em_separacao-architecture-sp` | Sim | `ARCHITECTURE-SPINE.md:94–97`, `addendum.md:281` | `done` |
| `epic-3-retro-item-23-voltar-a-alimentar-o-deferred-work-md-e` | Sim na Épica 5; na 6, uma spec por estória e 15 entradas, 6.2 sem nenhuma (B12) | `deferred-work.md` | `done` |
| `epic-3-retro-item-16-reconciliar-epics-md-1041-o-verde-availa` | Não — e agora é contradição viva: a 6.7 embarcou o verde em `ENTREGUE` (`web/lib/pedido.ts`), e `epics.md:1020` ainda diz "**só** para Estoque disponível" | — | — |
| demais itens abertos das Épicas 1 e 3 | Não, sem mudança desde a retro da Épica 5 | ver `epic-5-retro-2026-09-24.md` | — |
| `epic-5-retro-item-25-fazer-o-passeio-so-pelo-teclado-com-leit` | Não | criado nesta sessão; nenhum commit depois | — |
| `epic-5-retro-item-26-acrescentar-a-bancada-da-5-7-o-teste-que` | Não | idem | — |
| `epic-5-retro-item-27-emendar-o-ad-4-em-architecture-spine-md` | Não | idem | — |
| `epic-5-retro-item-28-decidir-o1-a-ciencia-do-preco-visto-e-tr` | Não | idem | — |
| `epic-5-retro-item-29-regra-de-processo-ca-com-cadeado-nao-fec` | Não | idem | — |

## Itens de ação

Todos propostos, nenhum aplicado. Dono: Sung.

1. **Fazer o passeio só pelo teclado, com leitor de tela, das superfícies da Épica 6**, e reabrir na tela o que as correções deixaram sem ver: UJ-3 (cancelar, inclusive em `AGUARDANDO_PAGAMENTO` com o Produto de `,95`), o `Dialog` de avanço de Status do painel, Meus pedidos (E1 e o estado vazio), a frase da corrida até `ENTREGUE` (E4), e `AZAMON_ENTREGA_SIMULACAO_ATIVA=false`. Fonte: `HANDOFF.md:66–67`, `:177–180`; `epics.md:385`. *Verificação.*
2. **Registrar a decisão dos 14px do selo no `DESIGN.md`**, com `memlog.py append` no diretório da UX, e decidir o marcador "Pagamento aprovado" em 12px. Fonte: B5, `deferred-work.md` (6.7). *Reconciliação de spec, aguarda o dono da UX.*
3. **Desfazer a cadeia de ordem em `TestSessaoEProduto`**: fazer os subtestes da 6.4, 6.5 e 6.6 conferirem só os próprios Pedidos em vez de depender da posição, ou, no mínimo, corrigir o comentário "por ÚLTIMO" de `api/sessao_test.go:567`. Fonte: B7. *Remediação.*
4. **Acrescentar o teste que morre quando o `de` da transição administrativa deixa de ser o Status visto no clique.** Fonte: B8, `deferred-work.md` (6.4). *Remediação.*
5. **Compactar o `HANDOFF.md` antes da Épica 7**: tirar a contradição de `:71–75` contra `:64–67` e trocar o parágrafo único da linha 3 por estado atual. Fonte: B11. *Remediação de documento.*

## Veredito de aceite

**`accepted-with-open-items`** · critérios **declarados** (`epics.md:1568–1747` e transversais) · veredito **de máquina**, sem decisão humana.

- Todas as sete estórias `done`; `pending_stories` vazia.
- FR-29 a FR-33 com prova de servidor e passeio humano de cada superfície; a FR-33 ganhou a prova das condições que o caminho feliz não alcança.
- Os dois desvios do painel da 6.4 (B3) foram corrigidos dentro da faixa, e E1–E5 em `main` depois dela.
- **Ficam abertos e rastreados:** o passeio só pelo teclado (NFR-11, nunca checado), a cadeia de ordem dos testes (B7), o `de` sem prova (B8), a reconciliação dos 14px (B5), e o `HANDOFF.md` contraditório (B11).

## Perguntas em aberto

- **O passeio só pelo teclado pode mudar o veredito**, como na Épica 5: se ele achar bloqueio na UJ-3 ou no painel, o veredito passa a `rejected` até a correção.
- **Dividir `pedido.go` pelos três blocos (B2)** — agora, antes de a 7.3 percorrer o addendum contra o código, ou aceitar como está?
- **O `epic-3-retro-item-16`** virou contradição entre a CA da 3.5 e a da 6.7. Reconciliar `epics.md` resolve; alguém precisa decidir se `epics.md` ainda é documento vivo depois da construção.
- **O E5 foi tratado como defeito** (`33ca441` tirou o `role="alert"`), embora o `HANDOFF.md:170` o deixasse "para decidir". Não há registro de decisão humana; esta retro não o reabre.

## Suposições (headless)

- Épica **6** tomada da invocação (`-H 6`), não detectada.
- `detect-epic --epic 6`: `story_count: 7`, `pending_stories: []`, `retro_status: optional`.
- Faixa `48f6723^..84a94f8`, do primeiro `feat(6.1)` ao `docs(6.4)` que fecha a correção do painel; `ab4558c` e `33ca441` lidos como pós-épica.
- A retro da Épica 5, escrita nesta mesma sessão, foi tratada como a retrospectiva anterior; o acompanhamento dos itens das Épicas 1 e 3 foi herdado dela e reconferido só no que a Épica 6 poderia ter mudado.
- Veredito `accepted-with-open-items` rendido pela máquina, sem decisão humana.
- O passeio só pelo teclado foi tratado como lacuna conhecida, sem resultado inventado.
- O `bmad-review` não foi invocado; lentes aplicadas inline sobre as fronteiras entre estórias.
- Cada um dos cinco itens de ação é proposta desta retro, não compromisso do time.
- Nenhuma transição de item anterior foi escrita; `--set-action-status` não foi passado.
