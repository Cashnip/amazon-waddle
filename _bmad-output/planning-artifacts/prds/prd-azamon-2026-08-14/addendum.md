---
title: Azamon — Addendum Técnico do PRD
status: final
created: 2026-08-14
updated: 2026-09-25
---

# Addendum — Azamon

Este documento guarda o que o `prd.md` deliberadamente não carrega: escolhas de tecnologia, desenho de mecanismo, alternativas descartadas e o caminho de migração. É insumo direto para `bmad-architecture` — o PRD diz *o que o sistema faz*, este addendum diz *o que já foi decidido sobre como*.

**A arquitetura já rodou** (2026-09-06). A espinha está em `_bmad-output/planning-artifacts/architecture/architecture-azamon-2026-09-05/ARCHITECTURE-SPINE.md`, com 20 invariantes de ID estável (`AD-1`..`AD-20`). Onde este addendum e a espinha falarem do mesmo assunto, **a espinha decide o mecanismo e este documento guarda o porquê**. O §9 abaixo registra onde cada delegação foi parar.

## 1. Stack imposta

| Camada | Escolha | Origem |
|---|---|---|
| Back-end | **Go** | Imposta pelo usuário |
| Front-end | **Next.js 16** sobre **React 19** | Decidido em `bmad-architecture` (2026-09-06); fecha a Questão em Aberto 1 do PRD |
| Banco de dados | **PostgreSQL** | Imposta |
| Cache / apoio | **Redis** | Imposta |
| Execução | **Docker obrigatório** para subir os serviços | Imposta |

**Observação sobre o Redis, levantada na discussão:** ele precisa de um uso nomeado, senão é dependência decorativa. O Carrinho **não** é um bom candidato — a decisão de carrinho só para autenticado (FR-16) e persistente entre Sessões (§4.4 do PRD) o coloca naturalmente no PostgreSQL, onde ele sobrevive a reinício e não expira sozinho. Três usos foram propostos aqui e levados à arquitetura. **Um sobreviveu, e é dito em voz alta que os outros dois não** — que era exatamente o combinado:

| Uso proposto | Veredito da arquitetura |
|---|---|
| Sessão, token de redefinição de senha (FR-3) e contador de bloqueio de login (FR-2) | **Sobreviveu, e é o emprego único do Redis.** Os três são dado com prazo de validade, que é o que o `TTL` nativo faz melhor que o PostgreSQL. Custo aceito e declarado: reiniciar o Redis encerra todas as Sessões (`AD-20`) |
| Cache de listagens da vitrine e de busca (NFR-4) | **Recusado.** Com 5.000 Produtos a folga vem do tamanho da tabela, e cache sem medição é uma segunda cópia da verdade para invalidar. Se o NFR-4 falhar em medição, o caminho é índice antes de cache |
| Fila leve para a Tentativa de Pagamento (FR-25) e a simulação de entrega (FR-33) | **Recusado.** A invariante 6 abaixo já obriga uma varredura do histórico de transições no PostgreSQL — que sobrevive a reinício, o que a fila não faz. Existindo a varredura obrigatória, a fila vira um segundo caminho para o mesmo efeito, com um teste a mais e uma corrida a mais (`AD-6`) |

O Redis fica no `docker-compose` com um emprego nomeado, não como enfeite.

**Aderência da stack ao destino declarado:** Go com monólito modular é particularmente favorável ao plano de microsserviços do §11 do PRD — pacote por domínio, interface pública explícita por módulo, e a extração vira mover pacote e substituir chamada de função por chamada de rede. A escolha de stack, feita antes deste PRD, já barateia a fase 2 técnica.

## 2. Máquina de estados do Pedido — desenho

A FR-28 do PRD define as transições. Aqui está o que a arquitetura precisa preservar:

**Estados e quem os provoca**

| Transição | Provocada por | Efeito colateral obrigatório |
|---|---|---|
| (criação) → `AGUARDANDO_PAGAMENTO` | Checkout do Comprador (FR-23) | Cria Reserva de Estoque, tudo ou nada (FR-24) |
| `AGUARDANDO_PAGAMENTO` → `PAGO` | Confirmação assíncrona do Provedor de Pagamento (FR-26) | Mantém a Reserva de Estoque |
| `AGUARDANDO_PAGAMENTO` → `PAGAMENTO_RECUSADO` | Confirmação assíncrona do Provedor de Pagamento (FR-27) | Libera a Reserva de Estoque |
| `PAGAMENTO_RECUSADO` → `AGUARDANDO_PAGAMENTO` | Nova Tentativa de Pagamento pelo Comprador (FR-27) | Cria nova Reserva de Estoque; falha se indisponível |
| `PAGO` → `SEPARANDO` | Administrador (FR-32) **ou** simulação de entrega (FR-33) | Nenhum sobre Estoque |
| `SEPARANDO` → `ENVIADO` | Administrador (FR-32) **ou** simulação de entrega (FR-33) | **Consolida a Reserva de Estoque:** o Estoque total baixa e a Reserva encerra (FR-28) |
| `ENVIADO` → `ENTREGUE` | Administrador (FR-32) **ou** simulação de entrega (FR-33) | Nenhum sobre Estoque |
| `AGUARDANDO_PAGAMENTO` → `PAGAMENTO_RECUSADO` | Expiração da Tentativa de Pagamento (FR-34) | Libera a Reserva de Estoque |
| Qualquer de {`AGUARDANDO_PAGAMENTO`, `PAGAMENTO_RECUSADO`, `PAGO`, `SEPARANDO`} → `CANCELADO` | Comprador (FR-31) | Libera a Reserva de Estoque, se ativa |

**Invariantes que a implementação precisa garantir**

1. **A transição é o único ponto de mutação do Pedido.** Conteúdo do Pedido (Itens de Pedido, preços praticados, Endereço, Frete) é imutável após a criação. Isso é o que faz a FR-30 (preço praticado no detalhe) funcionar sem esforço.
2. **Transição inválida falha alto.** Não há caminho silencioso: origem inválida devolve erro nomeado, seja o chamador o Administrador, o Comprador ou o processo de simulação.
3. **Toda transição escreve no histórico** (estado anterior, novo, quem, quando) — é o que sustenta NFR-9 e a linha do tempo da FR-30.
4. **Efeito sobre Estoque acontece na mesma transação da transição.** Cancelar e devolver ao Estoque não podem divergir; um sem o outro é o bug mais caro deste sistema.
5. **Concorrência entre Administrador e simulação de entrega** (nota do PM em §4.6 do PRD) resolve-se por transição condicionada ao estado esperado — quem chega segundo perde e recebe a recusa da invariante 2.
6. **Nenhum estado é alcançado por tempo em memória.** Tanto a expiração da Tentativa de Pagamento (FR-34) quanto o avanço da simulação de entrega (FR-33) derivam do histórico de transições — "está neste estado desde quando" — e não de temporizador de processo. Reiniciar o sistema não pode congelar Pedido nenhum. Este é o requisito que mais provavelmente seria violado por uma implementação ingênua, e o que mais provavelmente quebraria a apresentação.
7. **Confirmação tardia não ressuscita Pedido.** Uma aprovação que chega depois do cancelamento é registrada na Tentativa de Pagamento e sinalizada ao Administrador, mas não altera o Status do Pedido (FR-26). A transição inválida da invariante 2 não pode virar perda silenciosa da informação de que o pagamento foi aprovado.

## 3. Reserva de Estoque e concorrência

NFR-7 exige que duas criações simultâneas de Pedido para a última unidade produzam exatamente um Pedido. O desenho a validar na arquitetura:

- Estoque disponível é derivado (`total − reservas ativas`), não um contador mutável espalhado. A Reserva tem vida curta e limitada: nasce com o Pedido e termina no envio (consolidando, com baixa do total), no cancelamento, na recusa ou na expiração. Nunca fica ativa indefinidamente — se ficasse, o disponível cairia para sempre e a guarda da FR-11 travaria o Administrador.
- A criação do Pedido decide sobre o Estoque **dentro de uma transação**, com bloqueio de linha por Produto ou verificação condicional que falhe sob conflito.
- A criação é atômica sobre todos os itens: se qualquer um faltar, nada é criado (FR-24).
- O teste de concorrência do NFR-7 é o critério de aceite — dispara N requisições paralelas e verifica `pedidos_criados == estoque_inicial`.

`ponytail:` bloqueio pessimista por linha de Produto é o suficiente para a escala deste projeto; se o volume da demonstração revelar contenção, o caminho é reserva otimista com repetição, não fila de pedidos.

## 4. Fronteira do Provedor de Pagamento

O que o NFR-3 e a SM-5 cobram na prática:

- Uma interface com as operações mínimas: **iniciar Tentativa de Pagamento** e **receber confirmação**.
  - *(7.3: substituída pela 1.7 — a interface é contrato, e não `interface` Go: a assinatura de `pagamento.IniciarTentativa` mais a rota `POST /api/v1/webhooks/pagamento`, com o adapter em `pagamento.Simulado` e `EmitirConfirmacoesDevidas`. A emenda do AD-8 e o porquê estão na entrada da 7.3, no §10)*
- O Provedor Simulado implementa a interface e decide o resultado **pelos centavos do total do Pedido**, com as faixas declaradas no §7.1 do PRD — determinístico e acionável na demonstração sem reconfigurar nada e sem superfície de operador (FR-25), não aleatório. A faixa vale para a primeira Tentativa de Pagamento; da segunda em diante o resultado é `APROVADO`, senão a nova tentativa da FR-27 não existiria.
- A confirmação chega **fora da requisição do checkout**, mesmo no simulado. Esse é o ponto que a discussão marcou como inegociável: o gateway real confirma por fora, então o MVP também confirma por fora, ou a virada vira reescrita.
- A confirmação é idempotente por chave (NFR-12) — a mesma confirmação recebida duas vezes avança o Pedido uma vez só. É o comportamento exato que um webhook real exige.
- A tela do Pedido reflete o novo Status do Pedido sem recarregamento manual (FR-26). O mecanismo — polling curto, SSE ou WebSocket — é decisão de arquitetura; polling é o suficiente e o mais barato.

**Os dois casos que o mock precisa resolver porque o gateway real vai exigir**

- **Confirmação que nunca chega** (FR-34). Um pagamento assíncrono sem prazo de expiração deixa o Pedido preso e o Estoque comprometido indefinidamente. Com o Provedor Simulado, o sintoma é um Pedido travado; com o Stripe, é um Pedido travado *e* uma cobrança em estado indefinido. Resolver agora custa um prazo e uma varredura periódica; resolver depois custa descobrir isso em produção.
- **Confirmação que chega depois do cancelamento** (FR-26). É a corrida clássica de checkout. No mock, registrar e sinalizar basta. Na fase 2, este é exatamente o ponto onde entra o estorno automático — e o registro na Tentativa de Pagamento é o que torna esse estorno possível sem arqueologia de log.

**Na virada para o Stripe (fase 2):** troca-se a implementação e a configuração. O checkout, o Pedido, a Reserva de Estoque e a máquina de estados não são tocados. Se isso não for verdade quando chegar a hora, o NFR-3 foi violado em algum ponto do caminho.

## 5. Regra de Frete

Proposta a confirmar (Suposição 6 do PRD): tabela de faixas de CEP → região → valor fixo, com isenção acima de um limiar de subtotal. Vive como dado configurável, não como `if` espalhado no checkout. CEP fora de faixa cai na região padrão (FR-21).

## 6. Blindagens baratas contra suposições frágeis

A auditoria de suposições (§16 do PRD) encontrou duas de alto impacto cuja reversão fica barata **se** o modelo de dados nascer preparado. É o mesmo movimento do Vendedor sem portal: a estrutura comporta, a interface não expõe.

- **Pedido não dividido por Vendedor** (Suposição 2). O Item de Pedido já registra o Vendedor (FR-23). Se o enunciado do trabalho exigir separação por Vendedor, o caminho é agrupar na consulta e na exibição — nenhuma migração, nenhum remodelamento do Pedido. **Custo hoje: zero, já está no requisito.**
- **Categoria plana** (Suposição 4). A Categoria nasce com referência opcional a uma categoria-pai, não preenchida no MVP e não exposta na interface. Hierarquia depois é preencher o campo e ajustar a navegação. **Custo hoje: uma coluna nula.** Sem ela, é migração de dados num sistema que já tem Produtos e Pedidos.

Registrado aqui, e não no PRD, porque são decisões de modelo — o PRD mantém a capacidade como plana e a interface como plana.

## 7. Alternativas consideradas e descartadas

*Registrado porque a defesa do trabalho vale mais com o que foi rejeitado do que com o que foi escolhido.*

| Alternativa | Por que foi descartada | O que se perdeu |
|---|---|---|
| **Marketplace completo** (auto-cadastro, moderação de anúncio, repasse financeiro, painel de vendas) | Dobra o escopo — é o negócio inteiro, não uma funcionalidade. Repasse sem dinheiro real é ficção, e moderação sem volume é tela vazia | Fidelidade à Amazon na parte que mais a define. Mitigado pelo meio-termo: o Vendedor existe no modelo de dados e aparece na página de Produto |
| **Portal do Vendedor mínimo** (Vendedor entra e gerencia apenas os Produtos dele) | **Reexaminado — a rejeição original estava mal fundamentada.** Não é metade do projeto: as telas são as da FR-8 e FR-9, que já existem, e a diferença é um predicado de autorização que a FR-4 já obriga a testar. Fica fora do MVP por sequenciamento, não por custo | Nada estrutural. É o **candidato número um a ser puxado para dentro** se o cronograma segurar — e o único item de fase 2 que muda a natureza do sistema, não só a vitrine |
| **Loja única sem Vendedor** | O Catálogo viraria um CRUD e o sistema deixaria de ter qualquer traço de marketplace | Nada de esforço; muito de identidade do produto |
| **Gateway real em sandbox** (Stripe test mode) desde o MVP | **Conflita com o NFR-15:** a demonstração precisa rodar sem internet, e integração real exige rede. *(A justificativa antiga — "túnel público é risco de véspera" — não se sustenta mais: o Stripe CLI encaminha webhook para localhost sem ngrok. Substituída pela razão estrutural.)* | A demonstração de integração real. Compensado pelo NFR-3 e pela SM-5, que provam que a porta está aberta — e pelo fato de que FR-34, FR-26 e NFR-12 já implementam a parte difícil da integração |
| **Pagamento síncrono simulado** (`if` retornando aprovado na própria requisição) | Seria menos código hoje e reescrita do checkout amanhã | Nada que valha o preço |
| **Carrinho anônimo com fusão no login** | O custo não é a fusão — a FR-19 já revalida preço e disponibilidade, então fundir é somar quantidades respeitando o Estoque. O custo é o **estado anônimo com ciclo de vida próprio**: sessão de visitante, expiração, limpeza de carrinho órfão, tudo num sistema sem visitantes reais | Aderência ao comportamento canônico de comércio eletrônico. É a rejeição mais visível para quem abrir o sistema sem conta |
| **Elasticsearch no MVP** | Um serviço a mais no `docker-compose`, indexação a manter sincronizada, e o ganho é invisível com 200 Produtos | Relevância e tolerância a erro de digitação. Recuperável na fase 2 atrás da interface de busca |
| **Microsserviços desde o início** | Consome o semestre em orquestração e entrega serviços que não conversam | Demonstração de sistema distribuído — **e essa perda é real**, porque em trabalho de faculdade a fase 2 costuma nunca acontecer. Resposta: o marco opcional de extração de um serviço, no §11 do PRD |
| **Divisão do Pedido por Vendedor** | Só faz sentido com repasse financeiro, que não existe aqui | Fidelidade ao comportamento real de marketplace |

## 8. Ordem de construção sugerida

**Passo 0 — esqueleto vertical, antes de tudo o que vem abaixo.** A ordem a seguir é correta por dependência e perigosa por risco: seguida ao pé da letra, ela produz um sistema que só funciona no fim do semestre, e o fim do semestre é onde o tempo acaba. Antes de alargar qualquer camada, atravessar o sistema inteiro uma vez: **um** Produto semeado, **um** Comprador, **um** Pedido que nasce, é pago pelo Provedor Simulado e chega a `ENTREGUE` — com telas feias, sem busca, sem filtro, sem painel. Isso força a integração entre front e back na semana 2 ou 3, quando ainda há tempo de descobrir que o decimal virou ponto flutuante e que o cookie de sessão não atravessa a porta.

Fechado o esqueleto, a ordem abaixo governa o alargamento:

1. **Identidade e papéis** (FR-1 a FR-5) — tudo depende de saber quem está falando.
2. **Catálogo e Estoque** (FR-6 a FR-11) — sem Produto não há nada para buscar nem comprar.
3. **Máquina de estados do Pedido** (FR-28) — com testes, **antes** das telas que a consomem. É o pré-requisito de quatro funcionalidades e de dois roteiros de demonstração.
4. **Carrinho** (FR-16 a FR-19).
5. **Checkout e pagamento assíncrono** (FR-20 a FR-27) — junta Estoque, Pedido e Provedor de Pagamento.
6. **Busca e filtros** (FR-12 a FR-15) — paralelizável a partir do passo 2, é o corte mais independente do sistema.
7. **Pós-venda e operação** (FR-29 a FR-33) — colhe o que o passo 3 plantou.

Os passos 6 e 1–2 são as fronteiras mais limpas para dividir o time em paralelo.

## 9. O que este addendum delegou à arquitetura

Os seis itens abaixo foram deixados em aberto de propósito e resolvidos por `bmad-architecture` em 2026-09-06. O mecanismo mora na espinha; a linha aqui existe para que ninguém procure no lugar errado.

| Delegação | Onde foi parar |
|---|---|
| Escolha entre Next.js e React | **Next.js 16 sobre React 19**, como casca de apresentação sem regra de negócio, reescrevendo `/api/*` para o Go — uma origem só, sem CORS (`AD-10`) |
| Desenho de esquema de banco | **Um schema do PostgreSQL por módulo, chave estrangeira cruzando schema proibida** (`AD-2`). Entidades e travessias na Semente Estrutural da espinha; colunas vivem nas migrações |
| Estrutura de pastas do repositório | Árvore na Semente Estrutural: `internal/<módulo>/` com um arquivo público por módulo, `api/` só de tradução, `internal/plataforma/` para o transversal |
| Mecanismo de atualização da tela do Pedido | **Consulta em intervalo**, em três superfícies e mais nenhuma, com todo instante absoluto e vindo do servidor (`AD-18`) — como a `EXPERIENCE.md` já pedia. Sem WebSocket |
| Biblioteca de testes | Híbrido: máquina de estados em memória; `testcontainers-go` com PostgreSQL real onde a transação **é** o objeto do teste (NFR-7, NFR-12, FR-24) |
| Estratégia de migração de banco | **goose** com versionamento por *timestamp*, arquivos SQL embutidos por `embed.FS` e aplicados no arranque do binário, antes da semente. Nunca auto-migração |

*(7.3: duas linhas da tabela acima não valem mais ao pé da letra. "Um arquivo público por módulo": a porta é o pacote, e outro módulo chama símbolos de arquivos irmãos, como `identidade.BuscarEndereco` em `endereco.go` (2.5). "Três superfícies e mais nenhuma": o Detalhe do Pedido do Administrador é a quarta (6.4). O porquê está na entrada da 7.3, no §10.)*

**O que a arquitetura decidiu e este addendum não previa** — registrado aqui porque a SM-7 exige que este documento continue vivo:

- **O Catálogo é dono do Estoque, incluindo a Reserva de Estoque** (`AD-5`). A alternativa, o Pedido guardar a Reserva, cria ciclo: calcular o disponível exigiria o Catálogo perguntar ao Pedido, que já pergunta o preço ao Catálogo.
- **A ordem é metade da corretude do §3 acima.** Bloquear a linha de Produto com `ORDER BY id FOR UPDATE` **antes** de somar as reservas ativas, em dois comandos. Ler a soma antes de segurar a trava vende a mesma última unidade duas vezes, e o `ORDER BY` fecha o impasse entre dois Pedidos com os mesmos dois Produtos em ordem inversa.
- **A confirmação do pagamento entra por webhook e é aplicada por *inbox*** (`AD-7`): `pagamento` grava com restrição única e **não conhece `pedido`**; a varredura de `pedido` lê e aplica. Sem ciclo, com idempotência estrutural em vez de código defensivo, e sobrevivendo a reinício.
- **Emitir a confirmação simulada mora em `pagamento`, não em `pedido`** (`AD-6`). É comportamento do Provedor: dentro do Pedido, poria conhecimento de gateway no domínio e quebraria o NFR-3 na primeira troca.
- **A FR-3 não tem canal de entrega.** Não existe serviço de e-mail (§5 do PRD, e o NFR-15 proíbe rede), então o token vai para o log estruturado (`AD-20`). A FR-3 é a primeira da ordem de corte do §6.3; se sobreviver, o caminho é um contêiner Mailpit, que roda offline.

## 10. O que a construção decidiu

A SM-7 exige que este documento continue vivo: decisão de arquitetura tomada durante a construção volta para cá, na mesma estória que a tomou.

**Estória 1.1 — andaime do serviço** (2026-09-08):

- **`internal/plataforma` é aresta permitida para todos.** A tabela do `AD-1` não a lista porque ela não é um dos seis módulos do NFR-2: é a camada de config, log, correlação, transação e tradução de erro que a própria espinha define. O teste de fronteira (`internal/fronteira_test.go`) trata `plataforma` como universalmente importável e mantém todo o resto exaustivo — importar `internal/pedido/db/gerado` de fora de `pedido`, por exemplo, continua sendo falha de teste.
- **Convenção de nome das variáveis `AZAMON_*`.** Nenhuma fonte nomeava uma sequer, e a partir daqui outras estórias dependem delas: `AZAMON_<ÁREA>_<PARÂMETRO>`, duração no formato do Go (`15m`, `60s`, `168h`), valor monetário com sufixo `_CENTAVOS`, booleano `true`/`false`. As 23 estão no `.env` versionado. A regra do Provedor Simulado (`§7.1`) virou duas variáveis, `AZAMON_PROVEDOR_APROVADO_ATE_CENTAVOS` e `AZAMON_PROVEDOR_RECUSADO_ATE_CENTAVOS`, porque são duas faixas e a terceira é o complemento.
- **DSN e credenciais ficam no `.env` versionado.** Não existe produção (só demonstração e teste), então as credenciais de demonstração não são segredo e versioná-las é o que faz o `docker compose up` de clone limpo funcionar (NFR-1). Segredo real, se um dia existir, não entra no repositório — por isso `AZAMON_POSTGRES_DSN` e `AZAMON_REDIS_URL` são as duas únicas variáveis **sem padrão no código**: faltando, o processo não sobe.
- **`default_transaction_isolation` não é declarado no `docker-compose.yml`.** O padrão do PostgreSQL já é `READ COMMITTED`, que é o que o `AD-4` exige; declará-lo no compose criaria uma segunda fonte de verdade que nenhum teste vigia. A corretude do `AD-5` depende de bloqueio explícito, não do nível de isolamento.
- **A mensagem do envelope de erro é a do sentinela, nunca a do erro embrulhado.** Descoberto ao escrever o tradutor do `AD-14`: `err.Error()` de um `fmt.Errorf("reservar: %w", …)` levaria o contexto de desenvolvedor para dentro de um texto que a *Voice and Tone* governa. O tradutor usa o `Error()` do sentinela que casou, e o específico ("restam 2 unidades") viaja em `dados`.
- **O `//go:embed` das migrações mora em `db/embutido.go`, não em `internal/plataforma/migracao.go`.** A diretiva não atravessa `..` — só enxerga a árvore do próprio pacote. `plataforma.Migrar` recebe o `fs.FS` como parâmetro, o que de quebra torna o caso "migração quebrada derruba o arranque" testável sem banco.
- **O healthcheck do serviço `azamon` é o próprio binário, por `/azamon -saude`.** A imagem é `scratch` — sem shell e sem `curl` —, e a alternativa seria engordar a imagem só para o compose conseguir perguntar se o serviço está de pé. O subcomando faz um GET em `127.0.0.1:<porta>/api/v1/saude` e sai com 0 ou 1, o que dá ao `web` um `depends_on: service_healthy` de verdade e faz o endpoint de saúde ser usado por alguém, em vez de só existir.

**Estória 1.2 — casca do navegador** (2026-09-09):

- **Verificação 1 fechada, e o resultado é o bom: o `Set-Cookie` do Go atravessa o `rewrites()` do Next íntegro.** Provado por sonda temporária — `GET /api/v1/sonda-cookie` no `api/`, um cookie descartável que existe só para o `curl` ter o que atravessar. Pela porta 3000 o cabeçalho volta completo, `HttpOnly` e `SameSite=Lax` inclusive, e o `X-Correlation-Id` do Go vem junto. A saída declarada — *proxy* explícito no lugar do *rewrite* — **não precisa ser acionada**, e o cookie de Sessão da 1.5 pode contar com o caminho. A estória 1.5 remove `api/sonda_cookie.go` e o registro dele em `api/rotas.go` ao entregar a Sessão de verdade.
- **Verificação 3 fechada: `npx shadcn init` roda limpo em Next 16.3.4 + React 19.2.8.** A ressalva M1 de `reviews/review-versoes.md` — "com npm a resolução de peer deps pede flag" — **não se materializou**: o npm 11 que vem no Node 24.20.0 instalou os 15 componentes sem `--legacy-peer-deps` e sem `--force`. Um desvio real apareceu no lugar: o CLI 4.21 **não instala mais o Tailwind**, ele exige o Tailwind 4 já presente e aborta em "No Tailwind CSS configuration found". A ordem correta é `npm i tailwindcss @tailwindcss/postcss`, `postcss.config.mjs`, `@import "tailwindcss"` e só então `shadcn init`.
- **`Toast` não existe mais no registro do shadcn; o componente é `sonner`.** O CLI recusa `add toast` com "only available for Base UI projects". A lista de quinze da UX-DR1 fica intacta em número e em papel — `components/ui/sonner.tsx` é o Toast do Azamon —, e `button` entrou como décimo sexto arquivo porque `pagination` depende dele. O `sonner.tsx` gerado importa `next-themes`; sem `ThemeProvider` o `theme` cai em `"system"`, e aí **quem resolve o `prefers-color-scheme` é o próprio Sonner, por dentro** — fora da variante `dark` do Tailwind, que o `globals.css` já neutralizou. Ou seja: num sistema operacional em tema escuro, o Toast sairia escuro num aplicativo que declara não ter modo escuro. O arquivo fica **sem edição**, como a UX-DR1 manda; a correção é no ponto de montagem, e a primeira estória que montar o Toaster escreve `<Toaster theme="light" />`.
- **O `next.config.ts` precisa entrar no estágio de execução da imagem `web`.** O `rewrites()` é resolvido pelo `next start` em tempo de execução, não assado no `.next` da construção. O `Dockerfile` da 1.1 copiava só `package.json`, `node_modules`, `.next` e `public` — com isso a imagem subia, servia `/` e devolvia 404 do Next em `/api`, que é a falha mais cara de diagnosticar da estória inteira.
- **A armadilha do `middleware.ts` foi tratada por omissão.** Nada em 1.2 precisa de middleware, então o arquivo simplesmente não existe — e um arquivo que não existe não pode ser ignorado em silêncio. Quando alguma estória precisar, o nome é `proxy.ts`.
- **O verificador do AD-12 é `prebuild` no `package.json`, não um passo de CI.** Não existe CI no repositório e o rodapé "Adiado" da espinha não pede um. `web/scripts/verificar-offline.mjs` é Node puro, faz `grep` pelos quatro padrões em todo o `web/` fora de `node_modules`, `.next` e do próprio verificador, e sai diferente de zero nomeando arquivo, linha e padrão. Como `prebuild`, derruba junto `npm run build` e a construção da imagem `web` — que é exatamente o portão que o AD-12 pede, sem infraestrutura nova, e roda igual no Windows e no Alpine.
- **`Azamon Sans` é o Inter variável, subconjunto latino, renomeado no repositório.** Um WOFF2 de 48 KB em `web/public/fonts/`, licença OFL ao lado, faixa de peso 100–900 num arquivo só — o `[ASSUMPTION]` do `DESIGN.md` sobre o arquivo concreto está resolvido. A pilha de sistema segue declarada como alternativa no mesmo `--font-sans`.
- **Os três papéis tipográficos viraram `@utility` do Tailwind 4, não tokens `--text-*`.** `wordmark`, `preco` e `preco-centavos` precisam carregar peso, tracking e `font-variant-numeric` juntos, e a chave `--text-*` não comporta o último. Como `@utility`, os dois papéis monetários já nascem com `tabular-nums` embutido, o que tira do autor de cada tela a chance de esquecer o NFR-13.
- **Uma variável nova: `AZAMON_API_URL`, padrão `http://azamon:8080`.** Não está no `.env` porque o padrão já é o nome de serviço do compose; existe só para quem roda `next dev` fora do compose apontar o rewrite para `localhost`.

**Estória 1.3 — primeira migração e o Catálogo Semeado** (2026-09-11):

- **Verificação 2 fechada, e o resultado é o bom: o sqlc v1.31.1 analisa `DEFAULT uuidv7()` sem erro.** `sqlc generate` roda limpo sobre as migrações goose e escreve `internal/identidade/db/gerado/` e `internal/catalogo/db/gerado/`; a saída declarada — gerar o `uuid` no lado Go — **não precisa ser acionada**, e a chave primária continua `uuid DEFAULT uuidv7()` no banco. Com isso as cinco verificações de risco da épica ficam com quatro fechadas; sobra só o p95 ≤ 500 ms com 5.000 Produtos, que é a 1.4.
- **O recorte de schema por módulo no sqlc é um glob sobre o nome do arquivo de migração** (`db/migracoes/*_catalogo_*.sql`). É isto que torna o prefixo de módulo no nome da migração um mecanismo, e não decoração: um módulo nunca enxerga o schema do outro, que é a fronteira do `AD-2` um nível acima. Migração nova de `catalogo` entra no recorte sozinha, sem editar o `sqlc.yaml`.
  - *(7.3: substituída pela 3.5 — o bloco de `busca` no `sqlc.yaml` lê as migrações de `catalogo`, para tipar a VIEW `produto_visivel`. O porquê está na entrada da 7.3)*
- **`emit_exact_table_names: true` nos dois módulos.** A inflexão do sqlc trata os nomes como inglês e devolve `CatalogoCategorium` para `catalogo.categoria`. Como a convenção do `AD-2` já é `snake_case` singular, a inflexão só tem o que estragar. O `rename:` do sqlc não resolve — ele age sobre coluna, não sobre o nome do struct da tabela.
- **O marcador da semente é a decisão, não uma consulta prévia.** `INSERT INTO public.semente (versao) … ON CONFLICT DO NOTHING` roda dentro da mesma transação que aplica o SQL: zero linhas afetadas significa que outro binário já semeou, e a transação fecha sem escrever. Ler a tabela e depois decidir seria a corrida clássica, com dois binários lendo vazio no mesmo arranque. Resolve "aplicar uma vez" e "dois binários ao mesmo tempo" com um comando, sem trava separada — e o teste sobre `testcontainers` dispara os dois de fato, em paralelo.
- **`Semear` sem nenhum `.sql` é erro; `Migrar` sem nenhuma migração é caso normal.** A assimetria é deliberada: um embed quebrado que passasse por "catálogo vazio" gravaria o marcador e o arranque seguinte nem tentaria de novo. A migração vazia não tem esse efeito colateral.
- **Os `uuid` da semente são derivados por SHA-1 do nome (o formato do v5), não sorteados.** `DEFAULT uuidv7()` é o certo para dado nascido em execução e o errado para a semente: cada `docker compose down -v` daria identificador novo, e a condição de aceite pede exatamente o contrário. Derivar do nome dá determinismo sem que ninguém precise digitar 62 literais à mão, e o SQL versionado continua sendo só literais.
- **O Catálogo Semeado é gerado, e o gerador é o original.** `media/gerar.go` (com `//go:build ignore`, rodado por `go run media/gerar.go`) carrega a lista escrita à mão — 50 Produtos, 5 Categorias, 5 Vendedores, nomes, descrições e preços em português plausível — e escreve os dois artefatos determinísticos: um SVG por Produto em `media/` e `db/semente/001_catalogo_semeado.sql`. Gerador e saída ficam os dois versionados: a construção é offline e ninguém precisa do Go instalado para subir o projeto. Editar o SQL à mão é perder a edição no próximo `go run`.
- **As imagens dos Produtos são SVG placeholder desenhados no próprio repositório** — cor da Categoria como fundo, nome do Produto no meio, fonte da pilha do sistema. Nenhum byte vem da rede (`AD-12`), nenhum arquivo externo precisa chegar, e trocar placeholder por foto de verdade depois é substituir o arquivo em `media/` sem tocar no banco, porque a URL gravada no Produto não muda. As cores escolhidas evitam de propósito o verde e o laranja do `DESIGN.md`, que carregam significado.
- **A rota da imagem é `GET /api/v1/media/{arquivo}`, servida do `embed.FS`.** Fica sob `/api/v1` porque é o único prefixo que o `rewrites()` do Next atravessa, e é o que permite que a URL gravada no Produto seja **relativa**. Quem barra travessia de diretório é o `fs.ValidPath` de dentro do `ReadFile` do embed, que recusa qualquer caminho com `..` ou com `/` à frente — **não** o `{arquivo}` do `ServeMux`, que casa o caminho escapado e entrega o segmento já decodificado (`%2F` chega ao handler como `/`). Arquivo inexistente sai no envelope do `AD-14`, como qualquer rota. O pacote `media` entrou na tabela de arestas do `AD-1` como destino permitido de `api` e origem sem destino nenhum.
- **As cinco tabelas nascem sem coluna de data.** Nenhuma estória desta épica lê `criado_em`, e a semente determinística não ganha nada com um `now()` dentro. A convenção "toda data é `timestamptz`" continua valendo — só não tem, ainda, a quem se aplicar dentro de `identidade` e `catalogo`. Quem precisar de auditoria de linha acrescenta a coluna na estória que precisar dela.
- **E-mail normalizado é restrição de banco, não disciplina de chamador:** `email text NOT NULL UNIQUE CHECK (email = lower(email))`. Com o `CHECK`, o `UNIQUE` passa a valer sobre a forma normalizada de verdade, e nenhum caminho de escrita futuro consegue furar a regra esquecendo um `lower()`.
- **As credenciais de demonstração estão no `README.md`:** `comprador@azamon.test` / `azamon-comprador` e `admin@azamon.test` / `azamon-admin`. Os hashes são Argon2id no formato PHC com os parâmetros do `AD-9` (`m=19456`, `t=2`, `p=1`, sal de 16 bytes), literais no gerador porque a semente é determinística — o sal fixo é consequência do determinismo, não descuido. Não existe produção, e estas contas não são segredo.

**Estória 1.4 — a medição do NFR-4 com 5.000 Produtos** (2026-09-11):

- **Verificação 4 fechada, e o resultado é o bom — com uma surpresa útil.** Com 5.050 Produtos no banco (os 50 do Catálogo Semeado mais os 5.000 do conjunto de medição), a consulta com termo, Categoria e faixa de preço mede **p95 = 0,34 ms**, mediana 0,24 ms e máximo 0,56 ms em 100 execuções sob `EXPLAIN (ANALYZE, FORMAT JSON)`. O teto do `NFR-4` é 500 ms: a folga é de três ordens de grandeza. A decisão **"índice antes de serviço" está confirmada** — nem Elasticsearch, nem cache Redis para a busca, e o assunto não precisa ser reaberto nesta escala. As cinco verificações de risco da épica ficam todas fechadas.
  - *(7.3: o 0,34 ms é da sonda de `db/medicao_nfr4_test.go`, que lê `catalogo.produto` direto. A consulta que a busca da Épica 3 embarcou — a VIEW `produto_visivel`, `ORDER BY CASE` e a contagem `ContarVisiveis` — foi medida na retrospectiva da Épica 3: pior p95 de 8,47 ms por requisição, contra o teto de 500 ms. Ver a entrada da 7.3)*
- **A surpresa: nesta escala o planejador não usa o índice GIN `pg_trgm`.** Com o filtro de Categoria presente, o plano é `Bitmap Index Scan em produto_categoria_id_idx → Bitmap Heap Scan`, e o `LIKE` vira filtro sobre as ~1.010 linhas que voltam. Sem Categoria escolhida — a Vitrine com só o termo digitado —, o plano é `Seq Scan`, e ainda assim p95 = 1,33 ms. O GIN não é o que faz o número ficar verde; 5.000 linhas cabem em memória e varrer é barato. Ele fica como **seguro de crescimento**: o dia em que o Catálogo tiver volume para o planejador preferi-lo, o índice já está lá e a consulta não muda. Registrar isto importa porque o contrário — "o p95 passou porque o GIN está lá" — é a conclusão errada mais fácil de tirar da mesma medição.
- **Os dois índices de chave estrangeira adiados na 1.3 entraram nesta migração**, `produto_vendedor_id_idx` e `produto_categoria_id_idx` — e o segundo é justamente o que o planejador escolheu. O trabalho adiado não era higiene: era o índice que paga.
- **A normalização é na escrita, feita pelo Go** (`catalogo.Normalizar`, minúscula e sem diacrítico), e `media/gerar.go` passou a usar a mesma função em vez da tabela de acentos que mantinha por conta própria. O preenchimento das linhas que já existiam, dentro da migração, usa `translate()` com o mesmo mapa: `unaccent()` não é `IMMUTABLE`, depende do dicionário carregado e a extensão nem está instalada, o que é o motivo de o `AD-16` mandar normalizar fora do banco.
- **O conjunto de 5.000 é semente separada, com marcador próprio**, em `db/semente-grande/` e ligada só por `AZAMON_SEMENTE_GRANDE=true`. O catálogo da demonstração continua com exatamente 50 Produtos (`SM-C3`), e `public.semente` guarda as duas versões de forma independente — `plataforma.Semear` já recebia o `fs.FS` e o marcador como parâmetro, então o mecanismo não mudou.
- **Os 5.000 saem de 50 × 10 × 10, dentro do próprio SQL.** Cada Produto semeado ganha uma linha (`Prata`, `Bronze`, …) e uma série (`2016`…`2025`), por `CROSS JOIN` sobre `catalogo.produto` — o `SELECT` enxerga o instantâneo anterior ao `INSERT`, então só os 50 entram na multiplicação. A alternativa, gerar 5.000 `INSERT` literais pelo `media/gerar.go`, custaria ~1,6 MB de SQL versionado para o mesmo efeito. As palavras de linha e série são ASCII de propósito: `lower()` dá o mesmo resultado que `catalogo.Normalizar` daria, e `lower()` é `IMMUTABLE`.
- **A sonda de medição mora no teste, não no `sqlc`.** `db/medicao_nfr4_test.go` é quem sobe o Postgres, semeia os dois conjuntos, roda `ANALYZE` e mede. A busca de verdade, com paginação e rota, é da Épica 3 — gerar agora uma consulta `sqlc` que ninguém chama seria código morto nascido com a estória.

**Estória 1.5 — um Comprador entra e vê um Produto** (2026-09-11):

- **Verificação 1 agora está fechada pela Sessão de verdade, e a sonda descartável da 1.2 saiu.** `api/sonda_cookie.go` e o teste dele foram apagados junto com o registro da rota; quem exercita o mesmo caminho a partir daqui é `POST /api/v1/sessoes`, cujo `Set-Cookie` carrega os mesmos atributos que a sonda provou atravessarem o `rewrites()` — `HttpOnly`, `SameSite=Lax`, `Path=/`. O que a sonda mediu com um cookie vazio, a estória mede com o cookie que a demonstração usa.
- **O cliente Redis é `github.com/redis/go-redis/v9`**, a única dependência nova da estória. A tabela Stack fixa a versão do servidor (Redis 8.10.1) e não nomeava cliente; `go-redis` é o cliente de fato do ecossistema, traz `ParseURL` (que consome a `AZAMON_REDIS_URL` como está, sem montar opções à mão) e `SetEx`/`TTL` diretos. `golang.org/x/crypto`, que já estava no `go.sum` como indireta, virou direta por causa do `argon2`.
- **A conexão com o Redis é preguiçosa, e isso é decisão e não descuido.** `plataforma.AbrirRedis` só faz `redis.ParseURL` + `redis.NewClient`, sem `Ping`. Exigir Redis acessível no arranque derrubaria `/api/v1/saude` e os dois testes de `cmd/azamon/main_test.go`, que apontam `AZAMON_REDIS_URL` para uma porta morta de propósito — e custaria um contêiner a mais no teste para o mesmo efeito. Quem depende do Redis é a Sessão, e é ela quem falha se ele não estiver lá.
- **Conta inexistente e senha errada saem idênticas, byte a byte, e no mesmo tempo.** Duas mensagens distintas viram enumeração de contas; dois tempos distintos também. O caminho sem linha encontrada roda `argon2.IDKey` sobre um hash de descarte com os parâmetros do `AD-9` antes de devolver o sentinela — sem isso, o "não existe" volta em microssegundos e o "senha errada" em dezenas de milissegundos.
- **Um sentinela de 400 nasceu no próprio `plataforma/erro`: `ErrEntradaInvalida`.** A matriz da estória pede 400 com o envelope padrão para corpo malformado ou acima do limite, e isso é falha de tradução, não de domínio — mesmo lugar e mesmo motivo do `ErrNaoEncontrado` que já morava lá. Os sentinelas de módulo da estória (`identidade.ErrCredencialInvalida` e `identidade.ErrSessaoInvalida`, os dois 401) entraram no mesmo registro, na fatia ordenada do `AD-14`.
- **`internal/plataforma/erro` passou a importar `internal/identidade`, e a tabela do `AD-1` ganhou a aresta.** É consequência direta do `AD-14`: traduzir todo sentinela num arquivo só obriga esse arquivo a alcançar a porta pública de cada módulo cujo sentinela ele traduz. A aresta é `plataforma → identidade` e só pela porta; a seta contrária continua proibida — módulo de domínio não conhece `plataforma/erro`.
- **`Autenticar` e `BuscarProduto` recebem a `DBTX` do sqlc, não `*gerado.Queries`.** Com o parâmetro tipado na interface, `api/` passa o `*pgxpool.Pool` sem importar o pacote gerado de dentro do módulo — que é exatamente o que o teste de fronteira proíbe (a única porta de `identidade` é `internal/identidade`). Os testes de `api/` aplicam migração e semente por `os.DirFS("..")` pelo mesmo motivo: importar o pacote `db` daria a `api` uma aresta que a tabela não tem.
- **A Sessão guarda `{id, nome}` em JSON no Redis, sob `sessao:<token>`.** O cookie continua opaco e sem conteúdo — 256 bits de `crypto/rand` em hexadecimal —, e o valor guardado é o mínimo que fecha a estória (o nome, para a saudação) mais o identificador que a 1.6 vai precisar para dizer de quem é o Pedido. Ler a Sessão não custa consulta ao Postgres.
  - *(7.3: substituída pela 2.4 e pela 2.6 — a Sessão guarda `{id, nome, email, papel}`. O porquê está na entrada da 7.3)*
- **O roteiro chega à Página de Produto por dois links temporários na página de conferência da 1.2**, para `/entrar` e para o Produto semeado, com o `uuid` v5 escrito literalmente no front — ele é derivado do nome em `media/gerar.go` e por isso estável a cada `down -v`. Mesmo padrão descartável da sonda de cookie: a Épica 3 entrega a busca e remove os dois. O login **não** redireciona para Produto nenhum.
- **A Página de Produto é Server Component com um filho `"use client"`.** O detalhe do Produto é buscado no servidor do Next por `AZAMON_API_URL` (caminho relativo não existe lá), e a saudação do Comprador é buscada no navegador por `/api/v1/sessao` com caminho relativo — é essa ida e volta que faz o cookie atravessar o `rewrites()` de verdade. O `R$` é escrito só aqui, pelos papéis `preco` e `preco-centavos`; o que atravessa a API é `preco_centavos` inteiro.

**Estória 1.6 — um Pedido nasce em `AGUARDANDO_PAGAMENTO`** (2026-09-12):

- **O Status é `text` com `CHECK`, e não enum do Postgres.** Não existe `CREATE TYPE` no repositório, e `ALTER TYPE … ADD VALUE` não roda dentro de transação — o que faria de cada estória que acrescenta estado uma migração de forma diferente de todas as outras. Os sete Status já são conhecidos, então o `CHECK` nasce completo na primeira migração e a Épica 5 só passa a usá-los, sem migração nova. O mesmo vale para o estado da Reserva de Estoque (`ATIVA | LIBERADA | CONSOLIDADA`).
- **O número legível vem de um contador por ano, não de uma `SEQUENCE`.** Uma sequência por ano exigiria `CREATE SEQUENCE` dinâmico dentro do caso de uso. `pedido.contador_numero(ano, ultimo)` com `INSERT … ON CONFLICT DO UPDATE … RETURNING` resolve na mesma transação: sem DDL em tempo de execução, e **sem buraco na numeração quando a transação é revertida** — uma compra recusada por Estoque não queima um número. O custo é serializar a criação de Pedidos do mesmo ano numa linha, o que na escala da demonstração não se mede. É a única tabela do projeto cuja chave primária não é `uuid DEFAULT uuidv7()`: a chave é o próprio ano, e `db/schema_test.go` registra a exceção explicitamente.
- **A transação é aberta e fechada no handler**, `api/pedido.go` — o primeiro `pgx.Tx` do repositório. `api/` é quem já segura o pool, e o `AD-4` pede uma transação por caso de uso com `tx` explícito; um ajudante em `plataforma` seria uma camada a mais para embrulhar `Begin`, `Rollback` e `Commit`. O `defer tx.Rollback` usa `context.WithoutCancel`: o cliente pode desistir no meio, e desfazer precisa de um contexto vivo.
  - *(7.3: substituída pela 5.6 — a transação é aberta por `emTransacao`, em `api/transacao.go`, que repete uma vez em `40P01`/`40001`; ver o item "A repetição única" da 5.6)*
- **`pedido.Criar` orquestra o caso de uso inteiro, e `api/` só traduz.** O handler resolve o Comprador, abre a transação e mapeia erro em status; quem congela o Item, numera, grava o histórico e chama `catalogo.Reservar` é o módulo. É a aresta `pedido → catalogo` que a tabela do `AD-1` já previa, usada pela primeira vez — pôr essa sequência no handler seria regra de domínio em `api/`.
- **A ordem do `AD-5` virou duas consultas nomeadas separadas**, `TravarProdutosParaReserva` e `SomarReservasAtivas`. Uma consulta só não teria como errar a ordem, e é justamente a ordem que protege o Estoque: com a soma antes da trava, dois Pedidos simultâneos leem o mesmo "sobra uma". O `ORDER BY id` fica no `SELECT … FOR UPDATE` desde já, embora a estória compre um Produto por Pedido, porque é ele que impede o impasse quando o Carrinho da Épica 4 trouxer vários.
- **`catalogo.produto` ganhou `estoque_total integer NOT NULL DEFAULT 10`.** Com padrão, para que nem a semente, nem `media/gerar.go`, nem `db/semente-grande/` precisassem mudar, e `VersaoSemente` ficasse onde está. Sem o teto, a soma das reservas ativas não teria com o que comparar e nenhuma compra seria jamais recusada — o `AD-5` existiria no papel e não no comportamento. A Épica 3 constrói o predicado de visibilidade do `AD-19` sobre a coluna já existente.
- **O disponível viaja em `dados`, não na mensagem.** `catalogo.ErrEstoqueInsuficiente` é o sentinela traduzido em 409, e `catalogo.EstoqueInsuficiente` é o erro tipado que carrega o número até `api/` por `errors.As`. A mensagem publicada continua sendo a do sentinela (`AD-14`); o dado específico é campo do envelope.
- **`internal/plataforma/erro` passou a importar também `internal/catalogo` e `internal/pedido`.** Mesma consequência do `AD-14` que a 1.5 registrou para `identidade`: o ponto único de tradução alcança a porta pública de todo módulo cujo sentinela ele traduz. A tabela do `AD-1` em `internal/fronteira_test.go` ganhou as duas arestas.
- **Não há middleware de sessão.** `api/sessao.go` extraiu `compradorDaRequisicao`, chamada pelos dois handlers autenticados que existem em todo o sistema — `GET /api/v1/sessao` e `POST /api/v1/pedidos`. Duas chamadas não pagam uma camada.
  - *(7.3: substituída pela 2.4 — o lado do Comprador continua sem middleware, mas o Administrador tem guarda por prefixo, `somenteAdministrador`. O porquê está na entrada da 7.3)*
- **`escreverJSON` passou a receber o status.** O `201` do Pedido obrigou: `WriteHeader` congela os cabeçalhos já postos, e escrever o status fora do serializador perderia o `Content-Type`. Continua sendo o único lugar que serializa resposta de sucesso.
- **O Comprador vê o Pedido nascer na própria Página de Produto**, pelo filho `"use client"` `comprar.tsx`, no molde da `saudacao.tsx`: `fetch` relativo com `credentials: "same-origin"`, nenhuma rota nova no Next e nenhuma rota de leitura no Go. A tela de acompanhamento é da 1.7. O botão usa `primary-strong` — o laranja que o `DESIGN.md` reserva ao passo irreversível, uma vez por fluxo.
  - *(7.3: substituída pela 5.6 — o botão do esqueleto saiu, e o único caminho até um Pedido é Carrinho → Endereço → Revisão; ver o item "O botão do esqueleto saiu" da 5.6)*

**Estória 1.7 — o Provedor Simulado confirma por webhook e o Pedido vai a `PAGO`** (2026-09-12):

- **A faixa do §7.1 é lida sobre `total_centavos % 100`, e a ambiguidade que a 1.1 registrou está resolvida.** `AZAMON_PROVEDOR_APROVADO_ATE_CENTAVOS=89` significa "aprova enquanto o resto dos centavos do total for ≤ 89", nunca "aprova até R$ 0,89" — é o que "pelos centavos do total" quer dizer no PRD, e é a única leitura que torna a faixa acionável por escolha de Produto, sem reconfigurar nada. O nome da variável fica como está; quem a lê agora é `pagamento.Simulado.Decidir`, provado nas fronteiras das três faixas.
- **A 1.7 decide as três faixas mas só emite a aprovada.** Um total com centavos em `,90`–`,94` fica em `AGUARDANDO_PAGAMENTO` — entre os 50 Produtos semeados, treze caem aí, o Fone de R$ 249,90 inclusive. Recusa, nova Tentativa e expiração são da Épica 5; emitir agora uma confirmação recusada exigiria a transição que ainda não existe.
  - *(7.3: substituída pela 5.10 — a recusa também é emitida; ver o primeiro item da 5.10)*
- **A emissão da confirmação é derivada, não marcada.** A varredura procura Tentativa cujo atraso venceu e que **não tem linha na inbox** — não existe coluna "já emiti". Marcar exigiria escrever depois do POST, e a queda entre o commit e a escrita deixaria a Tentativa sem confirmação para sempre. Derivando, reemitir é o comportamento normal, e a restrição única sobre a chave de idempotência transforma o reenvio em no-op. É também o que faz o contêiner reiniciado retomar sozinho.
- **A função de envio entra como parâmetro; `pagamento` não importa `net/http`.** O `AD-6` põe a emissão em `pagamento`, e `TestDominioNaoConheceHTTP` proíbe HTTP em módulo de domínio. As duas regras convivem com o transporte injetado: `EmitirConfirmacoesDevidas` recebe `Enviar`, e quem a monta com `http.NewRequestWithContext` é `cmd/azamon`, que já é quem fala HTTP. O tipo `Confirmacao` — o corpo que atravessa a rede — mora em `pagamento` e é reusado pelo handler do webhook: duas definições seriam duas verdades que divergem na primeira mudança.
- **`pagamento` não conhece `pedido`, e a aresta que existe é a contrária.** `pedido.Varrer` lê a inbox por `pagamento.ConfirmacoesNaoAplicadas`, aplica sob transação própria e marca a linha terminal por `pagamento.Marcar`. A Tentativa nasce dentro da transação de `pedido.Criar`, estreando a aresta `pedido → pagamento` que a tabela do `AD-1` já previa. O teste de fronteira prova as duas coisas sem mudança nenhuma na tabela.
- **Nenhum sentinela novo, e por isso `plataforma/erro` não mudou.** As quatro linhas de erro da matriz da estória são sentinelas que já existiam: `ErrEntradaInvalida` (400), `ErrNaoEncontrado` (404) e `ErrSessaoInvalida` (401). Chave de idempotência sem Tentativa sai de `pagamento` como `pgx.ErrNoRows`, no mesmo padrão que `pedido.Criar` já usava para Produto inexistente — o módulo não inventa vocabulário para dizer "não achei". Consequência: a aresta `plataforma → pagamento` **não** existe, e o `AD-1` fica como estava.
- **Toda confirmação chega a um estado terminal, e "não aplicável" é desfecho, não erro.** `PENDENTE` vira `APLICADA` quando a confirmação é da Tentativa corrente, é aprovada e o Pedido ainda aguarda pagamento; em todo o resto vira `NAO_APLICAVEL_SINALIZADA`. Sem o segundo estado, a confirmação que chega tarde — Pedido já `PAGO`, ou Tentativa superada — ficaria `PENDENTE` para sempre e a varredura a releria a cada segundo.
- **Uma transação por Pedido na varredura, com `FOR UPDATE SKIP LOCKED`.** O tique que encontra o Pedido já travado não espera: a confirmação continua `PENDENTE` e o tique seguinte a pega. Uma transação por tique faria um Pedido problemático desfazer o trabalho dos outros. `ErrEstadoJaAvancado` é registrado e seguido em frente, como o `AD-6` manda.
- **O único relógio é o `time.Ticker` de `cmd/azamon`, e ele não decide nada.** Chama, nesta ordem, `pedido.Varrer` (aplica) e `pagamento.EmitirConfirmacoesDevidas` (emite), e sai por `ctx.Done()` no mesmo padrão do goroutine de `Shutdown`. Os passos `expirar` e `simular` entram na mesma lista na 1.8 e na Épica 5. Módulo nenhum tem `time.Timer`, `time.After` ou `time.AfterFunc` — nem o Simulado.
- **A tela do Pedido em processamento não busca no servidor do Next.** Diferente da Página de Produto, a leitura do Pedido é autenticada, e o cookie de Sessão só existe no navegador: um Server Component teria de reencaminhar o cabeçalho `Cookie` à mão, que é regra no Node e o `AD-10` proíbe. A rota `/pedidos/[id]` é casca com cabeçalho, e todo o dado vem do filho `"use client"`, que consulta por caminho relativo a cada 3 s enquanto o Status for `AGUARDANDO_PAGAMENTO` e para assim que ele avança. O instante exibido é o `atualizado_em` do servidor, RFC 3339 — nada é contado no navegador.
- **`comprar.tsx` deixou de mostrar a confirmação e passou a navegar.** Depois do 201 o Comprador vai para `/pedidos/<id>`, com o `id` que o corpo da criação traz desde a 1.6. O botão fica desabilitado durante a navegação: reabilitá-lo abriria a janela para uma segunda compra enquanto a tela ainda é a do Produto.
  - *(7.3: `comprar.tsx` saiu na 5.6; ver o item "O botão do esqueleto saiu" da 5.6)*
- **O webhook é autenticado por segredo compartilhado, e a chave de idempotência não autentica ninguém.** A chave é derivada do identificador do Pedido, que volta no 201 da compra e vai na URL de `/pedidos/<id>` — ou seja, está ao alcance do próprio Comprador, que forjaria a confirmação e levaria o Pedido a `PAGO` sem pagar, inclusive um da faixa que o Simulado nunca aprova. `AZAMON_WEBHOOK_SEGREDO` nasce **obrigatória e sem padrão**, no molde da DSN e da URL do Redis (credencial não mora no código, `AD-13`), viaja no cabeçalho `X-Azamon-Segredo` e é comparada com `subtle.ConstantTimeCompare` antes de o corpo ser lido. Cabeçalho ausente e cabeçalho errado saem no mesmo 401, sem dizer qual dos dois foi. O sentinela `erro.ErrNaoAutorizado` nasceu no próprio `plataforma/erro`, pelo mesmo motivo de `ErrEntradaInvalida`: é falha de tradução, não de domínio — e por isso continua sem aresta nova no `AD-1`.
- **O cliente do webhook tem prazo; o `http.DefaultClient` não tem.** A emissão é serial dentro do único goroutine da varredura, então um POST pendurado congelaria aplicar e emitir para o sistema inteiro, sem erro registrado. `tempoDoWebhook` é constante de pacote em `cmd/azamon`, no precedente declarado de `tempoDeCabecalho`: higiene de cliente, como aquele é de servidor, e por isso não vira variável `AZAMON_`.
- **`atualizado_em` sai em `RFC3339Nano`.** Duas transições do mesmo Pedido cabem no mesmo segundo, e com resolução de segundo a tela — e o teste — não teriam como distinguir o instante do nascimento do instante do avanço. Continua sendo RFC 3339, absoluto e do servidor; quem formata em pt-BR é o navegador, sem contar duração.
- **`PAGO` mantém a Reserva de Estoque `ATIVA`.** Consolidar é da 1.8, e o teste confere a Reserva depois do avanço justamente porque "pagou, então consolida" é a suposição fácil de fazer sem querer.

**Estória 1.8 — a varredura leva o Pedido até `ENTREGUE`** (2026-09-12):

- **`expirar` não é desta estória: pertence à 5.11.** As condições de aceite da épica listam o tique como `aplicar → expirar → simular → emitir`, mas a expiração da Tentativa é inteira da Épica 5 — ela transita o Pedido para `PAGAMENTO_RECUSADO` com `TEMPO_ESGOTADO` e libera a Reserva, e nenhuma das três coisas existe aqui. Antecipar o passo seria construir metade de uma estória de outra épica. A ordem do tique passou a ser `aplicar → simular → emitir`, com o lugar de `expirar` reservado em comentário entre os dois primeiros.
  - *(7.3: substituída pela 5.11 — o tique é `aplicar → expirar → simular → emitir`; ver o item "`expirar` não tem interruptor" da 5.11. A tabela do AD-6 foi emendada na 7.3)*
- **A consolidação chegou antes da Épica 3, e só ela.** O `AD-19`, a liberação da Reserva e o predicado de visibilidade continuam lá; o que desceu para a Épica 1 é `catalogo.Consolidar`, porque a transição `SEPARANDO → ENVIADO` é o ciclo fechado que a épica existe para provar e não há como fechá-lo sem baixar o Estoque. É a **única** passagem do sistema em que `catalogo.produto.estoque_total` muda — nenhuma outra transição toca em quantidade.
  - *(7.3: substituída pela 3.4 — ela criou a outra passagem em que o Estoque total muda, o ajuste do Administrador, fora do Pedido; entre as transições do Pedido, `Consolidar` continua a única)*
- **Consolidar Pedido sem Reserva `ATIVA` é no-op que devolve `nil`.** O `WHERE estado = 'ATIVA'` do `UPDATE … RETURNING` é compare-and-swap como o do Status: zero linhas devolvidas significa "já consolidada", e tratá-lo como erro faria um tique repetido baixar o Estoque duas vezes ou derrubar a transação inteira. É a mesma armadilha que o `AGENTS.md` já registra para `Liberar`.
- **A baixa percorre os Produtos em ordem de identificador, e quem ordena é o Go.** `UPDATE … RETURNING` não aceita `ORDER BY`, então a ordenação é sobre as linhas devolvidas, por `bytes.Compare` dos 16 bytes do `uuid`. É a mesma disciplina do `ORDER BY id FOR UPDATE` do `Reservar` (`AD-5`): na Épica 1 o Pedido tem um Produto só e a ordem não muda nada, e ela existe para que o Carrinho da Épica 4 não descubra o impasse com tudo já pronto.
- **`TravarPedido` passa a devolver o Status e o instante da última transição juntos**, pelo `max(t.ocorrido_em)` que o `BuscarPedidoDoComprador` já usava. A decisão de avançar precisa dos dois travados um com o outro; uma segunda consulta travada quase idêntica seriam duas oportunidades de divergirem. `aplicarConfirmacao` acompanhou a troca em uma linha.
- **Todo avanço deriva do histórico, nunca de estado em memória.** `PedidosParaAvancar` seleciona por "está neste Status desde antes do corte", e o corte é `agora - AZAMON_ENTREGA_INTERVALO`. Consequência direta: o contêiner derrubado entre `PAGO` e `ENVIADO` retoma cada Pedido de onde parou, sem intervenção e sem coluna de controle.
- **A lista de avanços é uma só, e dela saem as duas coisas.** `simulacao` é um `map` com as três passagens (`PAGO → SEPARANDO → ENVIADO → ENTREGUE`); `proximoDaSimulacao` a consulta, e os candidatos da consulta são as suas próprias chaves. Duas listas — uma no SQL, outra no Go — divergiriam na primeira estória que acrescentasse um estado.
- **Candidatos lidos fora da transação, decisão reconferida dentro dela**, no molde do `Varrer` da 1.7. Uma transação por Pedido com `FOR UPDATE SKIP LOCKED`: selecionar já travando faria uma transação por tique, e um Pedido que falhasse desfaria o avanço de todos os outros. A releitura travada é o que torna inofensiva a corrida entre dois tiques sobrepostos — o segundo ou não trava a linha, ou reencontra um Status que já não é candidato, ou recebe `ErrEstadoJaAvancado`, que é registrado em nível informativo e seguido em frente.
- **`pedido.EstadoTerminal` é a única declaração de "acabou" (`AD-18`)**, e terminal é `ENTREGUE` ou `CANCELADO` e mais nada. A resposta de `GET /api/v1/pedidos/{id}` carrega `terminal` ao lado de `status`, e é por ele que a tela para de consultar: a regra não é redeclarada em JavaScript, onde divergiria no dia em que a Épica 6 mexer nos estados finais.
- **A tela ganhou dois ritmos: 3 s enquanto `AGUARDANDO_PAGAMENTO`, 10 s depois.** A primeira janela é curta porque a confirmação chega em segundos; as etapas da entrega são longas, e manter 3 s seria consulta por consulta sem nada mudar. `setInterval` não troca de período depois de armado, então mudar de ritmo é recriá-lo — com uma guarda para não reprogramar a cada consulta, o que adiaria a próxima para sempre. Os rótulos passaram a cobrir os sete Status do `CHECK`, e não só os alcançáveis hoje.
- **O autor das transições da simulação é `simulacao-entrega`, e o `autorDaVarredura` da 1.7 virou `autorDoProvedor`.** O nome antigo dizia por qual mecanismo a linha entrou; o novo diz quem decidiu — que é o que a coluna `autor` do `NFR-9` significa, e o que separa as três linhas da entrega da linha do pagamento no histórico do mesmo Pedido.
  - *(7.3: substituída pela 5.1 — `autor` virou `ator`, e o valor da simulação é `SIMULACAO`; ver o item "`autor` virou `ator`" da 5.1)*
- **Nenhuma migração, nenhuma variável nova.** Os sete Status estão no `CHECK` desde a 1.6 e a 1.7 fechou o último schema da épica. `AZAMON_ENTREGA_INTERVALO` e `AZAMON_ENTREGA_SIMULACAO_ATIVA` já existiam no `.env` e no `config.go` desde a 1.1 sem uso em produção — esta é a estória que passou a lê-las, o interruptor guardando a chamada em `cmd/azamon` e o intervalo entrando como parâmetro em `SimularEntrega`.

**Estória 1.9 — clone limpo, rede desconectada, README de 15 minutos** (2026-09-13):

- **O NFR-1 está medido, e o número é 3 min 26 s.** Medição **a frio**, como a estória decidiu: `docker builder prune -af` (14,1 GB de cache apagados) e remoção das imagens base e das duas do projeto antes de o cronômetro começar — um número tirado de cache quente seria propaganda, não medição. Do `git clone` ao primeiro Produto renderizado na porta 3000: **206 s**, sendo 3 s de clone (o mesmo tempo pelo GitHub e por cópia local, conferido nos dois) e 203 s de `docker compose up` baixando as quatro imagens base e construindo as duas do projeto. A saúde do Go respondeu em 203 s, a casca em 206 s. Teto do NFR-1: 900 s. Da segunda vez em diante, com as imagens já construídas, a subida inteira leva **16 s**. Docker 29.4.3 em Windows 11; além de Git e Docker, nenhum outro pré-requisito foi necessário — Go, Node, PostgreSQL e Redis ficaram nos contêineres, como o README agora promete na primeira linha. **Metade da SM-3 continua sem prova:** a condição de aceite fala de "alguém que não escreveu o código", e quem cronometrou foi o autor, na máquina do autor — a medição a frio foi a substituição decidida em 2026-09-13 (apagar o cache aproxima o relógio do de quem chega do zero), e ela não substitui o seguidor independente. Quem fecha essa metade é a regra de ensaio do §12 do PRD: a verificação é **semanal e por pessoas diferentes**, porque verificada uma vez em setembro ela não prova nada em dezembro.
- **As cinco verificações do passo 0 fecham aqui, com desfecho nomeado cada uma.** As quatro do time deram positivo e cada uma tem a entrada que a fechou: **1** (o `Set-Cookie` do Go atravessa o `rewrites()`) e **3** (`npx shadcn init` roda limpo em Next 16 + React 19) na 1.2, a 1 reconfirmada pela Sessão de verdade na 1.5; **2** (o sqlc v1.31.1 analisa `DEFAULT uuidv7()`) na 1.3; **4** (p95 de 0,34 ms contra um teto de 500 ms com 5.050 Produtos) na 1.4. Nenhuma saída declarada precisou ser acionada. A **5** — enunciado ou rubrica do professor — é **externa ao time**: é o bloqueio 2 do `HANDOFF.md`, dono Sung, e continua aberta por não depender de ninguém daqui. Ela não toca o esqueleto vertical; o que ela move são as quatro suposições do §16 do PRD, e o caminho ao obtê-la já está escrito (`bmad-prd` em modo atualização, depois a espinha e a spec). Encerrar a épica com a 5 em aberto é a decisão, e não um esquecimento.
- **O `docker-compose.offline.yml` foi escrito, provado e descartado no mesmo dia.** Uma rede `internal: true` nega exatamente o que o NFR-15 nega, mas o Docker **descarta a publicação de portas em silêncio** quando o contêiner só está em rede interna: com o override no ar, `docker inspect` devolve `{"3000/tcp":[]}` e `{"8080/tcp":[]}`, `docker compose ps` mostra `3000/tcp` sem mapeamento, e `curl http://localhost:3000` não conecta. Sem aviso, sem erro no `up`. O arquivo não entrou no repositório, como a matriz da estória mandava; o ensaio de apresentação volta a ser desligar a rede da máquina à mão, e o README diz por quê para que ninguém reescreva o mesmo arquivo daqui a um mês.
- **O ensaio offline aconteceu por dentro da rede interna, e o esqueleto inteiro andou.** Com a pilha no ar sob o override e um contêiner de `curl` anexado à mesma rede: `https://example.com` e `https://fonts.googleapis.com` não resolvem o nome; entrar devolve o `Set-Cookie` de Sessão, que vale pelo caminho relativo (o `rewrites()` outra vez); a Página de Produto responde 200; o SVG do Produto (848 B) e o `azamon-sans.woff2` (48 KB) saem dos contêineres; e o Pedido percorre `AGUARDANDO_PAGAMENTO` → `PAGO` (19 s) → `SEPARANDO` (49 s) → `ENVIADO` (81 s) → `ENTREGUE` (111 s) sozinho. As quatro páginas servidas não têm **nenhuma** URL absoluta; nos ativos construídos as únicas que aparecem são o namespace `www.w3.org` de SVG e os links de documentação que o Next e o React embutem nas próprias mensagens de erro (`nextjs.org`, `react.dev`, `github.com`) — texto, não requisição, e nada foi buscado durante o passeio.
- **O passeio ligado na casca não chega a `PAGO`, e isso é da faixa do Provedor, não defeito.** O único link de Produto da página de conferência é o Fone de Ouvido Bluetooth Aurora, R$ 249,90 — centavos `,90`, que é a faixa que o Simulado **recusa** e que a 1.7 decidiu não emitir, porque recusar é da Épica 5. Confirmado na pilha em execução: 45 s depois da compra o Pedido segue em `AGUARDANDO_PAGAMENTO`, sem erro na tela. Quem segue o README sem saber disso conclui que o sistema travou. A correção é de documentação, não de código — nenhum comportamento novo entra nesta estória —, e o README passou a mandar trocar o identificador na URL por um Produto de centavos `,00` (Caixa de Som Portátil Maré, R$ 189,00), que percorre o ciclo inteiro em 1 min 41 s. A Épica 3 remove os dois links temporários ao entregar a busca, e é lá que o problema desaparece sozinho.
  - *(7.3: substituída pela 5.8 e pela 5.10 — `,90`–`,94` recusa na primeira Tentativa e a nova Tentativa aprova, e o README já diz isso)*
- **O portão do AD-12 foi exercitado, não inspecionado.** Uma linha `@import url(https://fonts.googleapis.com/...)` no topo de `web/app/globals.css` faz `npm run build` sair 1 antes de construir qualquer coisa, nomeando arquivo, linha e os **dois** padrões que a linha viola (`fonts.googleapis` e `@import url(http`). A linha foi removida em seguida e `git status` voltou limpo. `verificar-offline.mjs` e `internal/fronteira_test.go` não mudaram — a condição de aceite fecha rodando-os, e os dois estão verdes num clone limpo.
- **Nenhuma linha de código mudou nesta estória.** O diff é `README.md`, `HANDOFF.md`, este arquivo, o `.memlog.md` do PRD, o `sprint-status.yaml` e uma entrada em `deferred-work.md`. Sem rota, tabela, coluna, índice, migração ou variável `AZAMON_` nova — o que a épica precisava era de prova, não de comportamento.

**Estória 2.1 — cadastro de Comprador** (registrado na 7.3, 2026-09-25):

- **A duplicidade de e-mail é decidida pelo `UNIQUE` do banco, nunca por um `SELECT` antes do `INSERT`.** `identidade.Cadastrar` traduz o SQLSTATE `23505` em `ErrEmailJaCadastrado`, que sai como 409 `EMAIL_JA_CADASTRADO`. O motivo: dois cadastros simultâneos do mesmo e-mail passariam juntos por um teste feito antes da gravação. É o único ponto do sistema em que se confirma que uma conta existe, e isso é de propósito: sem isso o visitante não saberia por que o cadastro não vai. (`internal/identidade/identidade.go:124-137`; spec `spec-2-1-cadastro-de-comprador.md:24`)
- **O erro em linha tem um contrato só: `dados.campo`.** `erro.EscreverCampo` escreve 400 `CAMPO_INVALIDO` com o nome do campo em `dados`, e o 409 da duplicidade leva o mesmo `dados.campo`. A tela liga a mensagem ao campo de mesmo nome e põe o foco nele, numa leitura só. O `ENTRADA_INVALIDA` era genérico demais para o UX-DR16, que manda nomear o motivo. Os formulários usam `noValidate`, porque a validação nativa do navegador esconderia a mensagem do Go, que é a que segue a Voice and Tone (AD-10/AD-14). (`internal/plataforma/erro/erro.go:149-155`; `api/comprador.go:48-52`; `web/app/cadastrar/page.tsx:69,148`; spec `spec-2-1-cadastro-de-comprador.md:72,98-99,161`)
- **Quatro variáveis novas: `AZAMON_COMPRADOR_NOME_MAX` (120), `AZAMON_EMAIL_MAX` (254), `AZAMON_SENHA_MIN` (8) e `AZAMON_SENHA_MAX` (128).** `CarregarConfig` recusa no arranque `SENHA_MIN > SENHA_MAX`, porque um par invertido sobe e recusa toda senha. O NFR-16 exige limiar declarado, e o `env_test` confere o par `.env`/config. (`internal/plataforma/config.go:91-94,143-144`; `.env:13-16`; spec `spec-2-1-cadastro-de-comprador.md:68,126`)
- **Nome e senha contam runas; o e-mail conta octetos.** Um teto medido em bytes recusaria nomes acentuados antes da hora. O 254 do e-mail vem da RFC 5321, cuja unidade é o octeto: medido em runas, um endereço acentuado de 411 bytes passaria. A forma do e-mail é conferida por `net/mail`, e não por regex, porque toda regex curta para a RFC 5322 erra dos dois lados. A igualdade com o texto original recusa a forma com nome de exibição, e o ponto no domínio é exigido à mão para o validador dizer o mesmo que a mensagem dele. (`api/comprador.go:69-86,116-123`; spec `spec-2-1-cadastro-de-comprador.md:88-93,111-113`)
- **O cadastro já abre a Sessão, pelo mesmo `abrirSessao` do login.** `POST /api/v1/compradores` responde 201 com a mesma `saidaSessao`. Duplicar o `SetCookie` perderia um atributo num dos lados na primeira mudança. (`api/comprador.go:58-61`; `api/sessao.go:120-136`; `api/rotas.go:43`; spec `spec-2-1-cadastro-de-comprador.md:74-75`)

**Estória 2.2 — autenticação, encerramento de Sessão e bloqueio** (registrado na 7.3, 2026-09-25):

- **O contador de bloqueio é `falhas:<e-mail normalizado>|<origem>`, com `INCR` seguido de `EXPIRE NX`, em janela fixa.** O `NX`, e não um `EXPIRE` só na primeira falha, porque os dois comandos não são atômicos: um `EXPIRE` que falhasse deixaria o par bloqueado para sempre. O `NX` cura a chave sem prazo na falha seguinte e não mexe na que já tem prazo. O e-mail passa pela mesma `normalizarEmail` do `Autenticar`, senão cada variação de maiúsculas daria cinco tentativas de graça. (`internal/identidade/bloqueio.go:13-25,42-60`; `internal/identidade/identidade.go:168-174`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:25,91-96,105-108`)
- **O bloqueio é consultado antes do Argon2id e conta exista ou não a conta.** A resposta é 429 `MUITAS_TENTATIVAS`, igual nos dois casos. O bloqueio é também o limite de custo da rota. Se valesse só para conta existente, a própria resposta confirmaria o e-mail. O login válido apaga o contador do par. Uma falha ao gravar o contador não muda o 401, porque virá-lo em 500 diria ao atacante que a rota tropeçou. O e-mail acima de `EmailMax` é recusado com o 401 de sempre antes de virar chave de Redis, senão um chamador anônimo cunharia chaves de 4 KiB. (`api/sessao.go:65-112`; `internal/identidade/bloqueio.go:27-40`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:24,26-27,101-104,119-121`)
- **A origem do bloqueio é o host do `RemoteAddr`, sem a porta e sem `X-Forwarded-For`.** O cabeçalho chega do navegador, e quem quisesse escapar do bloqueio só teria de variá-lo. A porta efêmera faria cada conexão TCP virar chave nova. No compose a origem é constante, e o par vale na prática como só-e-mail. (`api/sessao.go:152-161`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:31,97-100`)
- **O TTL da Sessão desliza: toda leitura renova o prazo por `GetEx`.** É o que o §7.1 chama de expiração por inatividade. Um comando só, porque ler e depois expirar seriam duas idas ao Redis com uma janela entre elas. O domínio não conhece a `Config`, então quem passa o prazo é `api/`. Uma aba de acompanhamento aberta mantém a Sessão viva, e isso foi aceito. (`internal/identidade/sessao.go:68-76`; `api/sessao.go:186-188`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:30,109-111`)
- **`DELETE /api/v1/sessao` apaga a chave no Redis, é idempotente e responde 204 mesmo sem Sessão.** Expirar só o cookie não invalida nada, e um 401 diria que aquele cookie valia alguma coisa. A única exceção é o Redis recusar o apagamento: aí sai 500, porque prometer que a Sessão morreu sem ter apagado seria pior. `cookieSessao` é o único construtor do cookie, para emitir e para expirar, porque o navegador só apaga quando o Nome e o Path batem. (`api/sessao.go:138-150,211-229`; `internal/identidade/sessao.go:90-102`; `api/rotas.go:40`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:28,112-114,119-122`)
- **Erro cuja mensagem nomeia um limiar da `Config` fica fora do `registro`, numa função `erro.Escrever<X>` própria.** `EscreverBloqueio` segue o precedente do `EscreverCampo`, e a 2.5 repete com `EscreverLimiteDeEnderecos`. O motivo: um sentinela fixo teria de repetir o número. `EscreverBloqueio` recebe a duração e arredonda para cima, porque truncar faria um `=30s` mandar tentar "em 0 minutos". Todas passam por `envelopar`, o único ponto que serializa o envelope. (`internal/plataforma/erro/erro.go:233-262`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:75,115-118`)
- **O destino de retorno ao Login é um módulo só, `web/lib/destino.ts`, e é `.ts` puro para o `node --test` do `prebuild` o importar.** `destinoSeguro` aceita só caminho relativo. Recusa `//` e `/\`, caractere de controle (`/%09/mal` vira `//mal`) e o próprio `/entrar`, porque senão `/entrar` vira redirecionador aberto. `paraLogin` monta o link com o caminho e a busca. É a única porta de volta que todas as telas do 401 usam. O Node lê o tipo direto, mas não JSX, e o `web/` não monta componente em teste. (`web/lib/destino.ts:1-35`; `web/app/entrar/page.tsx:56`; `web/scripts/destino.test.mjs:1-3`; spec `spec-2-2-autenticacao-encerramento-de-sessao-e-bloqueio.md:29,82,126-134`)

**Estória 2.3 — recuperação de senha** (registrado na 7.3, 2026-09-25):

- **Rotas: `POST /api/v1/redefinicoes-de-senha` e `PUT /api/v1/redefinicoes-de-senha/{token}`.** O POST responde o mesmo 202 de corpo vazio em todos os caminhos: conta inexistente, e-mail acima do teto, corpo que nem é JSON, e também a falha de Redis, que vai só ao log. `SolicitarRedefinicao` só falha depois de achar a conta, então um 500 ali enumeraria contas. O PUT responde 204 ou 404 `TOKEN_INVALIDO`, um código só para token expirado, usado ou inexistente. O token vai no caminho porque é o próprio link que chega ao Comprador. (`api/rotas.go:44-48`; `api/redefinicao.go:20-63`; `internal/plataforma/erro/erro.go:56`; `internal/identidade/redefinicao.go:16-19`; spec `spec-2-3-recuperacao-de-senha.md:23,39-44`, triagem linha 131)
- **O token nasce com um ponteiro reverso: `redefinicao:<token>` → id e `redefinicao-de:<id>` → token, com o mesmo TTL (`AZAMON_SENHA_TOKEN_VALIDADE`).** A solicitação nova lê o ponteiro, apaga o token anterior e grava os dois de novo. É o que faz "nunca dois tokens válidos" ser verdade sem varrer nada: é uma chave por Comprador, e ela expira junto. A Sessão e o token saem do mesmo `novoToken()` de 256 bits, porque um token de redefinição mais fraco que o cookie seria a porta mais fraca da mesma conta. (`internal/identidade/redefinicao.go:21-71`; `internal/identidade/sessao.go:40-48`; spec `spec-2-3-recuperacao-de-senha.md:24,69,159-166`)
- **Encerrar todas as Sessões de um Comprador é `SCAN` sobre `sessao:*`, sem índice reverso.** Um índice custaria um comando a mais em toda requisição autenticada, para servir uma operação que acontece uma vez por senha esquecida. A varredura usa `Get` puro, não `GetEx`, para não dar sobrevida às Sessões dos outros. Não para na primeira falha: devolve o primeiro erro no fim. Só apaga Sessão com `Papel == PapelComprador` (2.4). (`internal/identidade/sessao.go:104-160`; spec `spec-2-3-recuperacao-de-senha.md:68,168`, triagem linha 132; `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:154-156`)
- **A ordem da redefinição é normativa.** Primeiro valida a senha, antes de resgatar o token: a recusa por forma não gasta o link nem um Argon2id. Depois grava a senha, e só então apaga o token, senão uma falha do Postgres deixaria o Comprador sem token e com a senha velha. Em seguida limpa o contador de bloqueio e, por último, varre as Sessões. Não abre Sessão: o Comprador volta pelo Login. A senha nova passa por `validarSenha`, a mesma função do cadastro, porque "as mesmas regras da 2.1" só é verdade se for a mesma função. (`api/redefinicao.go:65-91`; `api/comprador.go:88-104`; `internal/identidade/redefinicao.go:73-130`; spec `spec-2-3-recuperacao-de-senha.md:26,31,45,71-72,116-118`)
- **Redefinir a senha tira o e-mail do bloqueio em todas as origens, com o e-mail escapado no glob.** Decisão do humano, leitura (a) das três: o contador protegia uma senha que deixou de existir, e quem tem o token já podia trocar a senha. Vale para todas as origens porque o link pode ser aberto de outro aparelho. `*`, `?` e `[` são permitidos no e-mail pela RFC 5322, e sem escape o padrão varreria o contador dos vizinhos. `AtualizarSenhaDoComprador` virou `:one … RETURNING email`, o que dá o e-mail sem `SELECT` a mais e distingue o `UPDATE` que não achou linha. (`internal/identidade/bloqueio.go:71-123`; `internal/identidade/db/consultas.sql:36-48`; spec `spec-2-3-recuperacao-de-senha.md:104-121`, triagem linha 147)

**Estória 2.4 — autorização por dono e separação de papéis** (registrado na 7.3, 2026-09-25):

- **Cada papel tem a sua rota de login e a sua função de autenticação, e o papel é carimbado por qual função autenticou.** `POST /api/v1/sessoes` consulta só `comprador` (`Autenticar`), e `POST /api/v1/admin/sessoes` consulta só `administrador` (`AutenticarAdministrador`, com o mesmo `hashDeDescarte`). Não existe ordem de consulta, então o mesmo e-mail pode existir nas duas tabelas sem ambiguidade. Proibir isso exigiria uma restrição compartilhada e uma migração nova. O login administrativo fica no mux raiz, fora da própria guarda (o padrão mais específico ganha): sem isso ninguém entraria. (`internal/identidade/identidade.go:41-50,87-114`; `api/rotas.go:145-148`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:28-33,101-105,203-207`)
- **Os dois logins passam pelo mesmo `entrar`, com a autenticação injetada como fecho.** Duas cópias divergiriam no bloqueio por tentativas, e o Administrador ficaria com um Argon2id sem teto de custo: a conta mais valiosa atrás da porta mais fraca. Consequência aceita: o contador `falhas:` é compartilhado entre os dois papéis. É a direção conservadora, porque trocar de rota não dá orçamento novo a quem força o mesmo e-mail. (`api/sessao.go:46-56`; `api/admin.go:12-22`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:140-144`, triagem linha 183)
- **A guarda administrativa é um `ServeMux` próprio montado atrás de `somenteAdministrador`, e cobre também `/api/v1/admin` sem barra.** Rota nova entra no mux `admin` e herda a guarda sem ninguém lembrar dela. Sem o padrão exato, o `ServeMux` responderia `307` com corpo HTML antes da guarda, e isso confirmaria a subárvore ao anônimo e criaria um segundo contrato de erro (AD-14). O Redis fora do ar também sai 404, mas vai ao log, porque um 500 contaria que há rota atrás da guarda. `lerSessaoAdministrador` relê a Sessão, com `ponytail:` para levar a `Conta` no contexto quando compensar. (`api/rotas.go:102-144`; `api/admin.go:24-66`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:114-116,150-153`, triagem linhas 170-173)
- **Todo corpo JSON é lido por `decodificarCorpo`, que exige `Content-Type: application/json`, e isso fecha a fixação de Sessão por formulário cross-site.** Um `<form>` só emite `text/plain`, `x-www-form-urlencoded` ou `multipart/form-data`, e `application/json` exigiria `fetch`, que o preflight do CORS barra. O tipo é analisado com `mime.ParseMediaType`, para o `; charset=utf-8` passar. A recusa mora junto do `MaxBytesReader` e não num middleware, porque GET e DELETE não têm corpo. Handler novo com corpo que não passe por aqui reabre o furo. (`api/rotas.go:160-190`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:48-51,117-120,145-149,209-214`)
- **O papel é conferido de dentro da Sessão, e o papel errado ou vazio é `ErrSessaoInvalida`.** A Sessão de Administrador numa rota de Comprador sai 401, e a de Comprador sob `/api/v1/admin/` sai 404. Uma Sessão gravada antes do campo `papel` desserializa vazia e é recusada, forçando um login novo, porque ela não sabe dizer o que é. (`api/sessao.go:168-198`; `internal/identidade/sessao.go:64-66`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:216-219`, triagem linha 175)
- **`negacaoPorDono` é o teste de negação por dono, e recurso novo com dono entra nele em vez de ganhar prova própria.** Ele exige a igualdade byte a byte entre a resposta do recurso alheio e a do uuid inexistente, menos o `X-Correlation-Id`. A 2.5 o estendeu para Endereço e a Épica 6 para o cancelamento. `guardaDoPrefixoAdministrativo` prova que um handler novo sem checagem nenhuma já responde 404 ao Comprador. Sem ele, a posse era só precedente e não mecanismo. (`api/autorizacao_test.go:36-39,177-181`; spec `spec-2-4-autorizacao-por-dono-e-separacao-de-papeis.md:16-24,41-44,121-123`)

**Estória 2.5 — Endereços do Comprador** (registrado na 7.3, 2026-09-25):

- **`identidade.endereco` tem chave estrangeira para `identidade.comprador`, no mesmo schema, com `ON DELETE CASCADE`.** O AD-2 proíbe a travessia entre schemas, e não a integridade interna. Não há coluna de Endereço padrão: "escolher" (FR-5) é a listagem, e quem escolhe é o checkout. Remover é `DELETE` de verdade, porque o Pedido congela o Endereço na criação (AD-3), e desativar criaria uma lista a filtrar para sempre. Os campos são o mínimo de uma etiqueta de entrega, decidido pelo humano; `complemento` é o único opcional, e é `''` em vez de `NULL`. (`db/migracoes/20260915120000_identidade_endereco.sql:1-36`; spec `spec-2-5-enderecos-do-comprador.md:46-60,226-235`)
- **O CEP é gravado em 8 dígitos sem hífen, e a UF é validada contra as 27 siglas.** Uma forma só no banco deixa a Faixa de Frete (5.3) comparar por intervalo de texto sem normalizar a cada leitura. A máscara é da tela. A normalização mora em `identidade`, como `normalizarEmail`, e os `CHECK` a garantem no banco. A lista de siglas é regra de `api/`, porque `ZZ` passa por qualquer teto de dois caracteres. (`db/migracoes/20260915120000_identidade_endereco.sql:15-19,28-31`; `api/endereco.go:39-51,214-217`; `internal/identidade/endereco.go:217-227`; spec `spec-2-5-enderecos-do-comprador.md:50-55`)
- **O teto de Endereços por Comprador é conferido dentro do próprio `INSERT`, e alcançá-lo sai 409 `LIMITE_DE_ENDERECOS`.** É `INSERT … SELECT … WHERE (SELECT count(*) …) < @maximo`: uma ida ao banco só, e zero linhas significa teto alcançado, no mesmo jeito do compare-and-swap do Pedido. Em `READ COMMITTED`, dois cadastros simultâneos podem passar um Endereço do teto. Isso foi aceito porque o teto não é regra de dinheiro nem de Estoque; apertar custaria um `FOR UPDATE` por cadastro. O 409 vem de `erro.EscreverLimiteDeEnderecos`, fora do `registro`, porque o "Never" proibia sentinela nova e a mensagem nomeia o limiar. (`internal/identidade/db/consultas.sql:71-81`; `internal/identidade/endereco.go:90-106`; `api/endereco.go:84-89`; `internal/plataforma/erro/erro.go:157-167`; spec `spec-2-5-enderecos-do-comprador.md:71,162-173`)
- **Duas variáveis novas: `AZAMON_ENDERECO_TEXTO_MAX` (120, em runas) e `AZAMON_ENDERECO_POR_COMPRADOR_MAX` (20).** Todo limite de campo vem da `Config` (AD-13). (`internal/plataforma/config.go:101-102`; `.env:17-18`; spec `spec-2-5-enderecos-do-comprador.md:33-34,134-135`)
- **Rotas `GET|POST /api/v1/enderecos` e `PUT|DELETE /api/v1/enderecos/{id}`, no mux raiz, com a lista ordenada por `id`.** O `id` é uuidv7, então a ordem é a de criação sem coluna de instante. A ordem estável é contrato da escolha do checkout (5.2). Alheio, inexistente e malformado saem os três como `pgx.ErrNoRows` e o mesmo 404; o `RemoverEndereco` é `:execrows` para o zero não ser engolido. (`api/rotas.go:71-77`; `internal/identidade/db/consultas.sql:50-62,96-102`; `internal/identidade/endereco.go:206-215`; spec `spec-2-5-enderecos-do-comprador.md:46-49,139-140`, triagem linhas 192,195)

**Estória 2.6 — menu da conta e retorno ao ponto de partida** (registrado na 7.3, 2026-09-25):

- **A casca da loja é um componente só, `Casca`, com `MenuDaConta` dentro, e o `sair()` é um módulo só, `web/lib/sessao.ts`.** A extração fechou o cabeçalho duplicado em seis páginas. Tela pública ou do Comprador nova usa `<Casca>` e passa o próprio `<main>`, porque as larguras divergem. `sair()` saiu do componente de dropdown porque o Perfil também sai, e duas cópias do `DELETE` e do recarregamento divergiriam. O `MenuDaConta` só chama `paraLogin()` depois de montar, porque `window` não existe na renderização do servidor. (`web/components/casca.tsx:9-21`; `web/components/menu-da-conta.tsx:14-45`; `web/lib/sessao.ts:1-13`; spec `spec-2-6-menu-da-conta-e-o-retorno-ao-ponto-de-partida.md:21-25,84-99`, triagem linha 173)
- **`AutenticarAdministrador` não preenche `Conta.Email`, de propósito.** Nenhuma tela de Administrador o lê. `saidaSessao.Email` não tem `omitempty`, então preenchê-lo faria `POST /api/v1/admin/sessoes` devolver o e-mail enquanto `GET /api/v1/admin/sessao` devolve sem ele: duas rotas administrativas discordando do mesmo campo. O porquê está em comentário na função, para ninguém "corrigir" a assimetria. (`internal/identidade/identidade.go:107-113`; `api/admin.go:43`; spec `spec-2-6-menu-da-conta-e-o-retorno-ao-ponto-de-partida.md:171,230`)
- **Não existe rota de troca de senha com a senha atual: o "Trocar senha" do Perfil leva ao `/esqueci-a-senha` da FR-3.** Nenhuma FR pede a troca com senha atual. O Perfil lê o e-mail de `GET /api/v1/sessao`, sem rota própria. (`web/app/perfil/perfil.tsx:13,24,67`; spec `spec-2-6-menu-da-conta-e-o-retorno-ao-ponto-de-partida.md:37-39,191-195`)

**Estória 3.1 — gestão de Vendedores** (registrado na 7.3, 2026-09-25):

- **O predicado de visibilidade do AD-19 é a VIEW `catalogo.produto_visivel`, e o próprio `catalogo` lê por ela.** A regra ("Vendedor ativo", depois "e Produto ativo") existe num lugar só. A Página de Produto, `Reservar`, `Disponivel`, `Visiveis`, `Resumos` e a `busca` leem dela, e nenhum outro lugar escreve `WHERE ativo`. É o sentido contrário ao que a espinha descreve ("exposto por `catalogo.Visiveis`; a VIEW é construída sobre o mesmo predicado"): aqui a VIEW é a fonte e `Visiveis` a lê. As colunas são nomeadas e não `p.*`, porque o Postgres congela a lista no `CREATE`. (`db/migracoes/20260918120000_catalogo_produto_visivel.sql:1-16`; `internal/catalogo/db/consultas.sql:51-57`; `internal/catalogo/catalogo.go:118-131`; spec `spec-3-1-gestao-de-vendedores.md:18`, `:26`, `:65`)
- **A trava da Reserva é `FOR UPDATE OF p`, e a visibilidade entra por subconsulta à VIEW.** Um `FOR UPDATE` sobre a VIEW travaria também a linha do Vendedor, e Pedidos de Produtos diferentes do mesmo Vendedor passariam a esperar um pelo outro. A janela que sobra (Vendedor desativado no meio de uma Reserva) foi aceita. (`internal/catalogo/db/consultas.sql:20-29`; spec `spec-3-1-gestao-de-vendedores.md:66`, `:87`)
- **O banco decide o duplicado e a remoção recusada, sem `SELECT` antes.** `UNIQUE` (23505) vira `VENDEDOR_JA_CADASTRADO` e a FK `produto.vendedor_id` (23503) vira `VENDEDOR_COM_PRODUTOS`, os dois em 409. O duplicado leva `dados.campo = "nome"`. Id malformado é `pgx.ErrNoRows`, o mesmo 404 do inexistente. A spec não registra o porquê de não contar antes. (`internal/catalogo/vendedor.go:84-95`, `:101-109`; `internal/plataforma/erro/erro.go:84-85`; `api/vendedor.go:87-96`; spec `spec-3-1-gestao-de-vendedores.md:22`, `:35-38`)
- **Rotas `GET/POST /api/v1/admin/vendedores` e `PUT/DELETE /api/v1/admin/vendedores/{id}` no mux `admin`, sem checagem de papel no handler.** No `PUT`, `ativo` é obrigatório (400 `campo:ativo`), para que um campo esquecido não desative o Vendedor. (`api/rotas.go:108-112`; `api/vendedor.go:16-21`, `:115-117`; spec `spec-3-1-gestao-de-vendedores.md:70`, `:111`)
- **Variável nova `AZAMON_VENDEDOR_NOME_MAX`, padrão 120, contada em runas depois de aparar as bordas.** (`internal/plataforma/config.go:124`; `.env:34`; `api/vendedor.go:106-114`; spec `spec-3-1-gestao-de-vendedores.md:22`, `:68`)
- **`db/schema_test.go` conta só `BASE TABLE`.** Sem o filtro, a VIEW quebraria a contagem das tabelas do esqueleto. (`db/schema_test.go:51`; spec `spec-3-1-gestao-de-vendedores.md:111`)
- **A casca administrativa do `web/` não renderiza nada sem Sessão de Administrador.** `CascaAdmin` consulta `GET /api/v1/admin/sessao` e só renderiza depois do 200. Qualquer outra resposta faz `router.replace("/admin/entrar")`. A guarda de verdade continua sendo o 404 do prefixo no Go. O login `/admin/entrar` fica fora da `Casca` da loja, e `/admin` redireciona para `/admin/vendedores`. (`web/components/casca-admin.tsx:16-20`, `:36-51`; `web/app/admin/page.tsx:5-7`; spec `spec-3-1-gestao-de-vendedores.md:72-74`)

**Estórias 3.2 e 3.3 — Categorias e Produtos** (registrado na 7.3, 2026-09-25):

- **`catalogo.produto.ativo boolean NOT NULL DEFAULT true`, e desativar é um `UPDATE` dessa coluna. Produto não se remove.** A VIEW foi recriada por `CREATE OR REPLACE` com as mesmas colunas, que é o único caso que essa forma aceita. O "Produto ativo" veio da 3.4 para cá porque, sem ele, desativar (FR-9) não teria efeito. Não existe rota `DELETE` de Produto. (`db/migracoes/20260918130000_catalogo_produto_ativo.sql:1-14`; `api/rotas.go:118-123`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:39`, `:83`, `:112`)
- **O 23503 do Produto vira erro de campo pelo nome da constraint.** `produto_vendedor_id_fkey` vira `campo:vendedor_id` e `produto_categoria_id_fkey` vira `campo:categoria_id`, os dois em 400. Uuid malformado dá o mesmo erro de campo. Renomear a FK numa migração quebra o mapeamento em silêncio. (`internal/catalogo/produto.go:192-215`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:53`, `:85`)
- **Categoria com Produtos: a contagem só roda depois do 23503.** O caminho comum, remover uma Categoria vazia, continua sendo uma ida ao banco. A recusa é 409 `CATEGORIA_COM_PRODUTOS` com `dados.produtos = N`. `EscreverCategoriaComProdutos` fica fora do registro do AD-14 porque a mensagem nomeia um número que vem de fora do sentinela. `erro.Milhar` passou a ser o formato de todo número nomeado em mensagem de erro. (`internal/catalogo/categoria.go:69-93`; `internal/catalogo/db/consultas.sql:136-143`; `internal/plataforma/erro/erro.go:169-181`, `:219-231`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:51`, `:85-86`, `:114`)
- **Tetos novos na Config: `AZAMON_CATEGORIA_NOME_MAX` (120), `AZAMON_PRODUTO_PRECO_MAX_CENTAVOS` (100000000) e `AZAMON_PRODUTO_ESTOQUE_MAX` (100000).** O arranque recusa `ESTOQUE_MAX` acima de `math.MaxInt32`, porque a conversão para `integer` daria a volta e gravaria Estoque negativo. (`internal/plataforma/config.go:121-125`, `:158-159`; `.env:32-35`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:25-29`, `:129`)
- **O teto do corpo do Produto é por rota: `6 × (nome_max + descricao_max) + 4 KiB`, por `decodificarCorpoAte`.** Os 4 KiB padrão não comportam uma descrição de 4.000 caracteres, e 6 bytes por caractere é o pior caso do JSON (`\uXXXX`). (`api/produto_admin.go:166-170`; `api/rotas.go:177-190`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:137`)
- **O corpo do Produto: `estoque_total` só no `POST` e `ativo` obrigatório no `PUT`, os dois como ponteiro.** Assim a ausência não vira zero em silêncio. (`api/produto_admin.go:18-30`, `:188-193`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:53-54`, `:141`)
- **A imagem do Produto é `""` ou uma URL de `media.Arquivos`, e a lista sai de `GET /api/v1/admin/midias`.** A lista é `fs.Glob(media.Arquivos, "*.svg")`, a mesma fonte que serve os bytes, então só entra o que `GET /api/v1/media/{arquivo}` de fato responde. Upload é da fase 2. (`api/media.go:33-50`; `api/produto_admin.go:190-191`; `api/rotas.go:134-135`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:33`, `:57`, `:87`)
- **`ImagemDoProduto` é o componente único de imagem de Produto: `<img>` em 4/3, e não `next/image`.** Sem URL ou com falha (inclusive antes da hidratação, por `complete && naturalWidth === 0`), ele vira um bloco neutro com o nome, nunca o ícone de imagem quebrada. O AD-10 da espinha ainda fala em `next/image` com `unoptimized`, mas nenhuma tela usa `next/image`. O `images.unoptimized` do `next.config.ts` ficou sem uso. (`web/components/imagem-do-produto.tsx:6-9`, `:27-44`; `web/next.config.ts:19`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:93`)
- **Reais e centavos são convertidos por texto em `web/lib/preco.ts`, nunca por ponto flutuante.** "12,90" vira 1290 juntando dígitos, e quem decide se o valor é aceitável é o Go. `web/lib/pedir.ts` é o `fetch` comum das telas administrativas: mesma origem, JSON, e envelope lido sem estourar. (`web/lib/preco.ts:1-22`; `web/lib/pedir.ts:1-13`; spec `spec-3-2-3-3-gestao-de-categorias-e-produtos.md:44`, `:140`)

**Estória 3.4 — Estoque disponível e guarda de Reservas** (registrado na 7.3, 2026-09-25):

- **O ajuste do Estoque total pelo Administrador tem rota própria, `PUT /api/v1/admin/produtos/{id}/estoque`, e o `PUT` do Produto não aceita `estoque_total`.** Desativar e reativar reenviam a linha lida da lista. Se o total fosse junto, um Administrador com a tela aberta desfaria em silêncio a baixa de uma consolidação. Com a rota própria, toda escrita do total é intencional. A transação é aberta no handler com `Begin` (molde da 1.6), fora de `emTransacao`. (`api/rotas.go:124-126`; `api/produto_admin.go:100-142`; `internal/catalogo/db/consultas.sql:182-192`; spec `spec-3-4-estoque-disponivel-guarda-de-reservas-e-visibilidade.md:22`, `:42`, `:99`)
- **O ajuste trava a linha de `catalogo.produto` sem passar pela VIEW, e só depois soma as Reservas ativas.** Vale para Produto inativo, e travar pela VIEW transformaria o inativo em 404. Total abaixo do comprometido sai 409 `ESTOQUE_COMPROMETIDO` com `dados: {campo: "estoque_total", comprometidas: N}`, com singular e plural na mensagem. O teto é o mesmo da criação. (`internal/catalogo/db/consultas.sql:66-76`; `internal/catalogo/produto.go:157-190`; `internal/plataforma/erro/erro.go:183-194`; spec `spec-3-4-estoque-disponivel-guarda-de-reservas-e-visibilidade.md:34-35`, `:51`, `:101`)
- **A assinatura definitiva de `Reservar` é em lote: `Reservar(ctx, tx, pedidoID string, itens []ItemReserva)`.** O mesmo Produto repetido soma numa Reserva só, porque o índice único parcial recusaria o segundo `INSERT`. `Quantidade <= 0` é erro de quem chama, antes da trava, porque somada a outra linha abateria a Reserva. Id malformado vira `EstoqueInsuficiente`, que agora leva `ProdutoID` na grafia recebida. `Liberar(ctx, tx, pedidoID)` é por Pedido e libera todas as Reservas dele. (`internal/catalogo/catalogo.go:92-97`, `:225-306`, `:316-325`; spec `spec-3-4-estoque-disponivel-guarda-de-reservas-e-visibilidade.md:64`, `:116`, `:121`)
- **`Disponivel` e `Visiveis` recebem `DBTX` e devolvem `(map[string]…, error)`, indexados pela grafia que o chamador passou, sobre uma consulta só.** A espinha escreve `Disponivel(ctx, produtoIDs []uuid) map[uuid]int`, sem banco e sem erro. `Resumos` (4.3), no mesmo arquivo, indexa pelo id canônico: a divergência é deliberada e está documentada, e um uuid em maiúsculas acerta num e erra no outro. (`internal/catalogo/catalogo.go:99-131`, `:144-148`, `:176-206`; spec `spec-3-4-estoque-disponivel-guarda-de-reservas-e-visibilidade.md:54-55`, `:81`; retro `epic-3-retro-2026-09-19.md:116-118`)
- **A VIEW calcula `estoque_disponivel = greatest(total − Σ ATIVA, 0)` por subconsulta correlacionada, e mudar a lista de colunas da VIEW é `DROP` + `CREATE`.** `CREATE OR REPLACE VIEW` não tira coluna. A spec não registra o porquê do `greatest` nem da subconsulta. O comentário da migração diz que a subconsulta usa `reserva_estoque_produto_id_idx` linha a linha, sem somar as Reservas do Catálogo inteiro. (`db/migracoes/20260918140000_catalogo_estoque_disponivel.sql:1-23`; spec `spec-3-4-estoque-disponivel-guarda-de-reservas-e-visibilidade.md:67`, `:79`)

**Estória 3.5 — a Vitrine e o envelope de listagem** (registrado na 7.3, 2026-09-25):

- **Um só leitor de paginação e um só envelope para a loja e para o Administrador.** `listagem[T]` e `paginacaoDe(r, cfg)` moram em `api/rotas.go`. `por_pagina` acima de `AZAMON_PAGINA_TAMANHO_MAX` é rebaixado ao teto, não recusado. Valor não inteiro ou menor que 1 é 400 no campo. Com isso a listagem administrativa passou a aceitar `por_pagina` também. (`api/rotas.go:203-234`; `api/produto_admin.go:52-67`; spec `spec-3-5-a-vitrine-e-o-envelope-de-listagem.md:26`, `:57`, `:73`, `:115`)
- **A listagem conta primeiro, e uma página além do total devolve fatia vazia sem consultar a lista.** A comparação vem antes de multiplicar, para que uma `pagina` enorme não transborde. `busca.Listar` e `catalogo.ListarProdutosAdmin` seguem o mesmo molde. A divergência momentânea entre contagem e lista foi aceita. (`internal/busca/busca.go:76-87`; `internal/catalogo/produto.go:55-79`; spec `spec-3-5-a-vitrine-e-o-envelope-de-listagem.md:54`, `:112`)
- **A Vitrine (`/`) é Server Component que busca no Go por `AZAMON_API_URL` com `cache: "no-store"`, e o carregamento é o `loading.tsx`.** O `Skeleton` em grade sai do Suspense nativo do App Router, sem estado de carregamento à mão, e a URL é o estado. É o padrão da Página de Produto da 1.5 levado à Vitrine. O texto do AD-10 na espinha ainda proíbe "`fetch` do servidor Next para o Go dentro de RSC", e esse conflito não está registrado em lugar nenhum. (`web/app/page.tsx:16-17`, `:56-78`; spec `spec-3-5-a-vitrine-e-o-envelope-de-listagem.md:76-82`, `:94`)
- **Breakpoint arbitrário do Tailwind v4 se escreve em `rem`, não em `px`.** O Tailwind v4 ordena as variantes de largura por unidade, então `min-[1440px]` saía no CSS antes de `lg:` (64rem), perdia a cascata, e a Vitrine nunca chegou a quatro colunas. Corrigido em `min-[90rem]` (`48c8c27`). A varredura da retro não achou outra ocorrência. (`web/components/cartao-de-produto.tsx:8-12`; retro `epic-3-retro-2026-09-19.md:77-81`)

**Estória 3.6 — Página de Produto** (registrado na 7.3, 2026-09-25):

- **O detalhe ganha a Categoria por `JOIN` com `catalogo.categoria` dentro de `catalogo`, e a VIEW não muda.** Pôr o nome da Categoria na VIEW mudaria, por causa de uma tela só, o contrato que `busca` lê. `GET /api/v1/produtos/{id}` passa a devolver `categoria_id`, `categoria` e `estoque_disponivel`, nunca o total. (`internal/catalogo/db/consultas.sql:1-11`; `api/produto.go:17-28`; spec `spec-3-6-pagina-de-produto.md:23-24`, `:40`, `:76`)
- **Só o 404 da API vira a tela "Este Produto não está disponível.", por `notFound()` e `not-found.tsx` do segmento. Outro erro é lançado.** Aceito e registrado: com o `loading.tsx`, o streaming começa antes do `notFound()`, e a página sai com HTTP 200 (a API continua 404). Um 404 real exigiria tirar o `loading.tsx`. (`web/app/produtos/[id]/page.tsx:46-50`; `web/app/produtos/[id]/not-found.tsx:4-7`; spec `spec-3-6-pagina-de-produto.md:54`, `:78`, `:104`)
- **O teto de 10 unidades por Item de Carrinho entrou como constante do `web/` (`TETO_POR_ITEM`), com a quantidade limitada a `min(10, disponível)` para o seletor e para o `?quantidade` que volta do Login.** A spec só diz que "na 3.6 é uma constante do web/", sem o porquê. Ver "Não vale mais": a promessa de passar a vir do Go não se cumpriu. (`web/lib/quantidade.ts:1-17`; spec `spec-3-6-pagina-de-produto.md:26-27`, `:99`)

**Estórias 3.7 a 3.9 — busca, filtros e ordenação** (registrado na 7.3, 2026-09-25):

- **`catalogo.produto.criado_em timestamptz NOT NULL DEFAULT now()`, exposta pela VIEW, sustenta "Mais recentes".** Contraria a decisão da 1.3 de não ter coluna de data no `catalogo`, que já previa isso: quem precisar acrescenta. A semente insere numa transação só, então os Produtos semeados empatam no instante e o `id` desempata. (`db/migracoes/20260918150000_catalogo_produto_criado_em.sql:1-21`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:18`, `:54`)
- **Uma consulta sqlc só para termo, filtros e ordenação.** Cada filtro é opcional por `sqlc.narg(...) IS NULL OR`. A ordem é `ORDER BY CASE` com uma chave por ordenação (`recentes`, o padrão, `preco_asc`, `preco_desc`) e sempre termina em `id`. `ContarVisiveis` repete o mesmo `WHERE`. As três estórias mexem no mesmo `WHERE` e `ORDER BY`, e com `CASE` o plano não ordena por índice, custo aceito. (`internal/busca/db/consultas.sql:1-29`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:26`, `:55`, `:82`)
- **O termo passa por `catalogo.Normalizar` primeiro e só depois pelo escape de `\`, `%` e `_`, e casa por `LIKE` sobre `busca_normalizada`.** `tsvector` e `unaccent` ficam proibidos. Termo vazio ou só de espaços é "sem termo". (`internal/busca/busca.go:50-63`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:23`, `:32`)
- **Contrato de `GET /api/v1/produtos`: `termo`, `categoria`, `preco_min` e `preco_max` em centavos, e `ordenacao`, todos validados em `api/` com 400 no campo.** O termo é limitado por `AZAMON_BUSCA_TERMO_MAX` contado em runas, e NUL (`%00`) é recusado antes do banco, que o recusaria com 500. Faixa invertida é 400 `campo:preco_min`. Categoria bem formada e inexistente devolve 200 vazio. (`api/busca.go:53-105`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:24-26`, `:36-45`, `:99`)
- **O sentinela de `busca` (`ErrCategoriaMalformada`) é traduzido em `api/`, fora do registro de `plataforma/erro`.** A tabela do AD-1 não tem a aresta `plataforma → busca`. A spec não registra o porquê. (`internal/busca/busca.go:27-29`, `:64-68`; `api/busca.go:38-41`; `internal/fronteira_test.go:31`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:42`)
- **`GET /api/v1/categorias` é rota pública de `catalogo` no mux raiz, sem Sessão, com o mesmo handler da listagem administrativa.** A Categoria não tem nada que o visitante não possa ver. A conversão `saidaCategoria(c)` serve de guarda de compilação: acrescentar `CategoriaPaiID` a `catalogo.Categoria` quebra o build em vez de vazar o campo na rota pública. A tabela de posse de rotas do AD-16 só cobre Produto, e esta rota não está nela. (`api/rotas.go:52-55`; `api/categoria.go:16-38`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:28`, `:58`; retro `epic-3-retro-2026-09-19.md:133`)
- **A Vitrine higieniza a URL antes de chamar o Go.** Valor que o Go recusaria (termo acima do teto, preço que não é centavo, ordenação desconhecida, Categoria que não é uuid) é descartado em vez de virar página de erro. O uuid vai em minúsculas. A faixa invertida lista sem o filtro de preço, com a mensagem no painel. O teto 100 está escrito literalmente no `web/`, e isso foi aceito na revisão: diverge se `AZAMON_BUSCA_TERMO_MAX` mudar. (`web/app/page.tsx:26-42`, `:64-68`; `web/components/busca-global.tsx:18-19`; spec `spec-3-7-3-9-busca-filtros-e-ordenacao.md:100-101`, `:106`)

**Estória 3.10 — a barra da loja** (registrado na 7.3, 2026-09-25):

- **A `Casca` da loja virou `"use client"` e carrega `GET /api/v1/categorias` no navegador, a cada montagem.** Ela serve a telas cliente e servidor, e buscar no servidor exigiria passar as Categorias por props em treze chamadas. Se a carga falha, a busca fica só com "Todas" e a Faixa fica vazia, sem erro na tela. A Vitrine mantém a sua própria carga no servidor, para os filtros. (`web/components/casca.tsx:9-35`; spec `spec-3-10-chrome-da-loja.md:51`, `:75`, `:45`)
- **`useSearchParams` na busca global obriga um `<Suspense>` na `Casca`, senão o `next build` quebra nas páginas estáticas.** O fallback é o mesmo formulário vazio. Um `key` com a querystring recria os campos a cada navegação. (`web/components/casca.tsx:47-49`; `web/components/busca-global.tsx:14-22`; spec `spec-3-10-chrome-da-loja.md:26`, `:52`)
- **A busca global é uma consulta nova.** `destinoDaBusca`, pura, leva só termo e Categoria à URL e descarta faixa de preço, ordenação e página. Ela e `destinoDaCategoria` ficam em `web/lib/busca.ts`, testadas pelo `npm test`, que roda no `prebuild`. (`web/lib/busca.ts:1-14`; `web/package.json:6-7`; spec `spec-3-10-chrome-da-loja.md:24-25`, `:62`, `:77`)

**Estória 4.1 — Carrinho exclusivo de Comprador autenticado** (registrado na 7.3, 2026-09-25):

- **Um Carrinho por Comprador e um Item de Carrinho por Produto são restrições do banco, e o Carrinho nasce na primeira adição.** `carrinho.carrinho` tem `comprador_id uuid NOT NULL UNIQUE`, e `carrinho.item_carrinho` tem `UNIQUE (carrinho_id, produto_id)`, com `comprador_id` e `produto_id` como `uuid` sem `REFERENCES` (AD-2). `GarantirCarrinho` é um upsert por `comprador_id`. O `UNIQUE` garante a FR-16 sem depender da disciplina de quem chama, e a migração obrigava a decidir o que fazer com o Produto repetido: ele soma ao Item existente e nunca abre segunda linha. O Carrinho não expira. (`db/migracoes/20260918160000_carrinho_schema.sql:10-25`, `internal/carrinho/db/consultas.sql:3-7`; spec `spec-4-1-carrinho-exclusivo-de-comprador-autenticado.md:27,33,111`)
- **A soma e o teto por Item são conferidos numa ida só ao banco, e zero linhas devolvidas significa o teto.** `AdicionarItem` faz `ON CONFLICT … DO UPDATE SET quantidade = item.quantidade + EXCLUDED.quantidade … WHERE soma <= @teto`, e o `RETURNING` vazio vira `carrinho.ErrTetoPorItem`. O teto vem de `cfg.CarrinhoUnidadesMax` (`AZAMON_CARRINHO_UNIDADES_MAX`) e é conferido duas vezes: na entrada, em `api/`, e na soma, dentro da consulta. O teto é validação na fronteira de confiança (NFR-14), e na consulta ele continua atômico. (`internal/carrinho/db/consultas.sql:12-19`, `internal/carrinho/carrinho.go:161-170`, `api/carrinho.go:114-119`; spec `spec-4-1-…:31,84-86,111`)
- **O Carrinho não tem identificador na rota; o Item de Carrinho tem, e é nele que o NFR-6 é testado.** As rotas são `POST /api/v1/carrinho/itens` e `DELETE /api/v1/carrinho/itens/{id}`, e o Carrinho é resolvido pela Sessão. O único identificador que um Comprador consegue forjar é o do Item, e por isso é o Item que entra em `negacaoPorDono`. (`api/rotas.go:86-95`, `api/autorizacao_test.go:81-82`; spec `spec-4-1-…:109`)
- **A adição do visitante atravessa o Login pelo marcador `adicionar=1` no `destino`, e não por estado guardado.** A Caixa de compra manda ao Login com `?quantidade=N&adicionar=1`. Na volta com Sessão, ela cria o Item uma vez só (guarda por `useRef`, contra o efeito duplo do StrictMode) e tira o marcador com `router.replace`. Se o Produto está esgotado na volta, nada é criado. Os motivos: nem o Redis nem o navegador podem ter estado de Carrinho, recarregar a página não pode somar de novo, e criar o Item em silêncio com o Produto esgotado foi o achado corrigido na revisão. (`web/app/produtos/[id]/caixa-de-compra.tsx:57,89-99`, `web/app/produtos/[id]/page.tsx:94`; spec `spec-4-1-…:21,97-98,103,105,125`)

**Estórias 4.2 e 4.3 — adicionar, alterar e esvaziar o Carrinho** (registrado na 7.3, 2026-09-25):

- **O Carrinho recusa a soma resultante do Item acima do Estoque disponível, com o mesmo 409 do Pedido e os números em `dados`.** `carrinho.AcimaDoEstoque` embrulha `catalogo.ErrEstoqueInsuficiente`, e com isso `errors.Is` casa o mesmo código `ESTOQUE_INSUFICIENTE`. `erro.EscreverEstoqueInsuficiente` fica fora do registro de sentinelas porque a mensagem nomeia o Produto e o disponível, que o sentinela não carrega. Com zero disponível, a mensagem diz "{nome} está indisponível.". O teto é conferido antes do Estoque, e os dois antes de tocar no Carrinho. O critério é a soma porque um Item com 3 de um Produto com 4 disponíveis recusa +2: o Item passaria a pedir 5. A tela não reescreve a mensagem (AD-14). (`internal/carrinho/carrinho.go:32-42,148-156`, `internal/plataforma/erro/erro.go:196-217`; spec `spec-4-2-4-3-adicionar-alterar-e-esvaziar-o-carrinho.md:27,42-44,100`)
- **Existem dois esvaziares, com nomes, rotas e assinaturas distintos: `carrinho.Limpar` é o do Comprador e `carrinho.Esvaziar` é o da criação do Pedido.** O esvaziar da FR-18 é `DELETE /api/v1/carrinho/itens` → `Limpar(ctx, bd, compradorID)`: apaga todos os Itens do dono e mantém a linha do Carrinho. `Esvaziar(ctx, tx, compradorID, itemIDs)` fica reservado a `pedido.Criar`. A separação de nomes, rota e consulta impede que um seja usado no lugar do outro. A frase do AD-3 "não existe `DELETE /api/v1/carrinho`" vale só para o fim da criação; ela foi lida sem emenda, e a leitura está no `.memlog.md` da arquitetura, mas não no addendum. (`internal/carrinho/carrinho.go:262-268,373`, `internal/carrinho/db/consultas.sql:58-63`, `api/carrinho.go:201-217`, `api/rotas.go:94`; spec `spec-4-2-4-3-…:85,98`)
- **`PATCH /api/v1/carrinho/itens/{id}` recebe a quantidade absoluta, e zero remove o Item com 204.** A quantidade chega crua (`json.RawMessage`) para o 400 nomear o campo `quantidade`. O mínimo depende da rota (1 na adição, 0 na alteração), e o teto da Config é aplicado em `api/`. Por que zero remove, a spec não registra; a quantidade crua existe para o 400 nomear o campo. (`api/carrinho.go:16-27,63-73,128-151`, `api/rotas.go:93`; spec `spec-4-2-4-3-…:46-48,79`; `spec-4-1-…:155`)
- **Um Produto que saiu da visibilidade continua no `GET /api/v1/carrinho`, com `visivel: false`, fora do subtotal e dentro de `unidades`.** Preço, Estoque e visibilidade vêm em lote pela porta nova `catalogo.Resumos`, que lê a VIEW numa consulta só (sem N+1), e o Produto invisível é a chave ausente no mapa. A linha continua lá para o Comprador poder removê-la, sem esconder Item nem quebrar o subtotal. (`internal/carrinho/carrinho.go:242-255`, `internal/catalogo/catalogo.go:144-148`; spec `spec-4-2-4-3-…:50,76,102`)
- **A barra superior e as telas se falam pelo evento `azamon:carrinho` na janela, e não por consulta em intervalo; só a leitura mais recente escreve o contador.** Quem altera o Carrinho chama `avisarCarrinhoAlterado()`: a Caixa de compra, o Carrinho e, desde a 5.6, a Revisão. O `CarrinhoNaBarra` relê `GET /api/v1/carrinho` a cada aviso e descarta resposta fora de ordem. A spec proíbe consulta em intervalo, e duas leituras seguidas voltando fora de ordem deixavam o número velho ganhar (achado corrigido na revisão). (`web/lib/carrinho.ts:38-45`, `web/components/carrinho-na-barra.tsx:25-46`; spec `spec-4-2-4-3-…:36,81-82,117`)

**Estória 4.4 — revalidação do Carrinho na abertura** (registrado na 7.3, 2026-09-25):

- **A revalidação da FR-19 é decidida no Go e viaja por linha no `GET /api/v1/carrinho`.** Cada linha leva `preco_visto_centavos`, `estoque_disponivel`, `preco_mudou` e `bloqueio`, e este é um de três valores fechados: `""`, `indisponivel` (invisível, ou disponível 0) e `acima_do_estoque` (0 < disponível < quantidade). A regra é `bloqueioDe`, em `carrinho`, com as constantes exportadas. Produto desativado e Produto de Vendedor desativado saem iguais (FR-12). A tela só reparte as linhas (`revalidacaoDe`) e combina tudo em `podeAvancar`. A regra de preço e de Estoque não mora no Node (AD-10), e a 5.5 reaproveita a mesma regra na entrada do checkout. (`internal/carrinho/carrinho.go:58-68,94-102,249-252`, `api/carrinho.go:35-51`, `web/lib/carrinho.ts:108-114`; spec `spec-4-4-revalidacao-do-carrinho-na-abertura.md:18,24,78`)
- **A confirmação de preço no Carrinho guarda, por Item, o preço que estava na tela, e só no estado do componente.** `PrecosConfirmados` é um mapa de id para preço. Se o preço mudar de novo, o valor guardado deixa de ser o atual e o `Alert` volta. Se a confirmação fosse ao servidor, o `GET` ou uma rota nova gravaria `preco_visto_centavos`, e a mudança de preço sumiria antes de o checkout reportá-la (AD-17). (`web/lib/carrinho.ts:96,116-120`, `web/app/carrinho/meu-carrinho.tsx:66,265`; spec `spec-4-4-…:23,80`)

**Estória 4.5 — a tela do Carrinho não promete o que o sistema não faz** (registrado na 7.3, 2026-09-25):

- **O `GET /api/v1/carrinho` leva o limiar `frete_isencao_centavos` na raiz, e não a distância pronta; a distância é calculada em `web/lib/carrinho.ts` sobre o subtotal exibido.** O campo vem de `s.cfg.FreteIsencaoCentavos` (`AZAMON_FRETE_ISENCAO_CENTAVOS`) e sai também no envelope da entrada no checkout. `comQuantidade` precisa repassá-lo. Se a distância viesse pronta do servidor, ela ficaria velha durante a edição otimista, exatamente enquanto o subtotal ao lado muda. Vindo o limiar, a frase e o subtotal saem da mesma soma, e o NFR-13 continua valendo nesse intervalo. O limiar é dado de configuração e não regra, então o AD-10 fica intacto. (`api/carrinho.go:53-61,188`, `web/lib/carrinho.ts:68-71,76-79`, `web/app/carrinho/meu-carrinho.tsx:201`; spec `spec-4-5-a-tela-do-carrinho-nao-promete-o-que-o-sistema-nao-faz.md:19,25,102`)
- **O ambiente de teste usa um limiar diferente do padrão de produção.** `freteIsencaoDeTeste = 25000`, contra o padrão de 29900 do `config.go`. Com o valor igual ao padrão, o teste passaria mesmo com o número cravado no handler; diferente, ele prova que a leitura repassa a Config, que é a terceira condição de aceite. (`api/carrinho_test.go:20,195-196`, `internal/plataforma/config.go:109`; spec `spec-4-5-…:81`)

**Estória 5.1 — a máquina de estados do Pedido, completa e com testes** (2026-09-19):

- **O Status é `SEPARANDO`, e o código é que mudou.** O esqueleto da Épica 1 gravou `EM_SEPARACAO` no `CHECK`, no Go e em duas telas, contra o glossário do PRD, o `AD-3`, a `EXPERIENCE.md` e a `SPEC.md`. A retrospectiva da Épica 1 propunha o contrário — reconciliar os documentos ao código —, e a decisão de 2026-09-19 foi pelo glossário, que é termo do §3 e não se troca por sinônimo. A migração `20260919120000_pedido_maquina_de_estados` reescreve os dados antigos, então um volume de `docker compose` anterior sobe sem `down -v`. As três menções desta seção, escritas na Épica 1, foram corrigidas junto.
- **A tabela do `AD-3` é dado em `internal/pedido/maquina.go`, com o ator.** Nove linhas, a do nascimento inclusa (ela não passa pelo compare-and-swap: quem a faz é `Criar`, com `INSERT`), e o cancelamento é **uma** linha com quatro origens. Cada linha diz quem pode provocá-la — `COMPRADOR`, `PROVEDOR`, `VARREDURA`, `ADMINISTRADOR`, `SIMULACAO` —, e a expiração exige `motivo = TEMPO_ESGOTADO`. `Transicionar` consulta a tabela **antes** de tocar o banco: fora dela é `TransicaoInvalida`, com as permitidas para aquele ator.
- **O efeito mora dentro de `Transicionar`, e não em quem o chama.** Depois do CAS e da linha do histórico, na mesma transação: `Liberar` em toda recusa e todo cancelamento, `Reservar` com os Itens do próprio Pedido na nova Tentativa, `Consolidar` em `SEPARANDO → ENVIADO`. A simulação de entrega deixou de chamar `Consolidar` por conta própria — o efeito que o chamador tinha de lembrar virou efeito que ele não tem como esquecer.
- **O cancelamento reclassifica pela releitura do Status.** Um CAS perdido de `→ CANCELADO` relê o Pedido; se quem chegou antes o tirou da janela, a resposta é `ErrForaDaJanelaDeCancelamento`, e não `ErrEstadoJaAvancado`. Os três sentinelas saem em 409 com códigos distintos, e `erro.Escrever` põe `dados.permitidas` sozinho quando o erro é `TransicaoInvalida`.
- **`autor` virou `ator`, e ganhou `motivo`.** O `ator` é um dos cinco valores, com `CHECK`; o uuid do Comprador que o nascimento gravava não se perdeu, porque o Pedido já guarda `comprador_id`. `motivo` é anulável: nem toda transição tem um.
- **"Escrito uma vez" e "imutável" são gatilhos do banco.** `pedido.pedido` recusa `UPDATE` de qualquer coluna que não seja `status` — a comparação é a linha inteira menos o Status, então as colunas que a 5.3 e a 5.6 acrescentarem nascem protegidas — e recusa `DELETE`; `item_pedido` e `transicao_status` recusam `UPDATE` e `DELETE`. O erro é `restrict_violation` (23001). Migração futura que precise reescrever linha destas tabelas desliga o gatilho dentro dela mesma; o teste que envelhece o histórico o faz com `SET LOCAL session_replication_role = replica`, porque o usuário do contêiner de teste é superusuário.
- **`correlacao` no histórico foi adiada, e o motivo é o `AD-1`.** O `AD-15` quer a correlação da requisição na linha da transição, mas ela vive no `context` por `plataforma.CorrelacaoDe`, e `pedido` não importa `plataforma`. As saídas — `api/` passar a correlação como parâmetro, ou a chave do `context` descer para um pacote que os módulos possam importar — mudam a assinatura do `AD-3` ou a tabela do `AD-1`, e não cabem numa estória de máquina de estados. Está no `deferred-work.md`; a coluna, quando vier, é anulável e não pede preenchimento retroativo.

**Estória 5.2 — seleção de Endereço no checkout** (registrado na 7.3, 2026-09-25):

- **O checkout são duas rotas do Next, `/checkout/endereco` e `/checkout/revisao`, em toda largura.** Frete e pagamento são blocos da Revisão, e não passos. Com uma rota por passo, "um passo por tela" de 360 a 639 px vale por construção (UX-DR15), sem rolagem horizontal. (`web/app/checkout/etapas.tsx:1-6`; spec `spec-5-2-selecao-de-endereco-no-checkout.md:18,25`)
- **O navegador guarda da escolha de Endereço só o `id`, em `sessionStorage` sob `azamon:checkout:endereco_id`, e o banco não tem Endereço padrão.** Nunca Frete, CEP ou total. Toda leitura e escrita fica em `try/catch`, e armazenamento indisponível equivale a nada guardado. `guardarEscolha` devolve `false`, e a tela para com aviso. Guardar só o `id` obriga a Revisão a perguntar o Frete ao Go, e é isso que faz a troca de Endereço recalcular o Frete. O lugar é `sessionStorage` e não a URL porque a escolha precisa atravessar a ida ao Carrinho e a volta (FR-22), e o botão do Carrinho não conhece o `id`. (`web/lib/checkout.ts:1-9,15,22-43`; spec `spec-5-2-…:24,28,36,85`)
- **Duas funções puras decidem o Endereço: `enderecoMarcado` no passo Endereço e `enderecoEscolhido` na Revisão.** `enderecoMarcado` segue a ordem recém-cadastrado → guardado ainda presente na lista → primeiro → nenhum. `enderecoEscolhido` aceita só o guardado presente na lista, sem o primeiro como consolação. O guardado pode ter sido removido em "Meus endereços" entre uma visita e outra. Na Revisão, sem escolha válida, o Comprador não escolheu nada e volta ao passo Endereço. (`web/lib/checkout.ts:45-66`; spec `spec-5-2-…:44,49,102-105`)
- **O formulário de Endereço é um componente só, `FormularioDeEndereco`, usado por "Meus endereços" e pelo checkout, e devolve o Endereço salvo por `aoSalvar`.** Rota, método e erro por campo viraram funções puras em `web/lib/endereco.ts`. A spec não registra o porquê da extração; o `aoSalvar` com o `id` é o que permite ao checkout marcar o Endereço recém-cadastrado. (`web/components/formulario-de-endereco.tsx:54-62,123`, `web/app/checkout/endereco/escolha-de-endereco.tsx:17`, `web/app/enderecos/meus-enderecos.tsx:16`; spec `spec-5-2-…:26,68-71,133`)

**Estória 5.3 — cálculo do Frete** (2026-09-19):

- **As faixas de Frete nascem na migração, e não em `db/semente/`.** A épica dizia "semeada junto com o Catálogo Semeado", mas a semente roda uma vez por marcador de versão: um banco que já a tem nunca veria um arquivo novo, e trocar `VersaoSemente` reexecuta os `INSERT` do Catálogo, que colidem. A Regra de Frete é regra, não demonstração — precisa existir em todo banco, testcontainers inclusive. `20260919160000_pedido_faixa_frete.sql` cria a tabela e insere as nove linhas.
- **Os valores são por região, pelo primeiro dígito do CEP dos Correios, e em reais inteiros.** Sudeste R$ 15,00 · Sul R$ 20,00 · Centro-Oeste R$ 25,00 · Nordeste R$ 30,00 · Norte R$ 35,00 · região padrão R$ 40,00. Nenhum documento os fixava. O `CHECK (valor_centavos % 100 = 0)` não é estética: o Provedor Simulado decide pelos centavos do total (§4, PRD §7.1), e um Frete de ,90 transformaria todo Produto de ,00 numa recusa — o apresentador perderia o controle do desfecho que o Roteiro A pede.
- **A região padrão é uma linha, não configuração.** O `AD-13` fecha a configuração nos parâmetros do §7.1, e só a isenção é de Frete. A linha padrão não tem faixa, e um índice único parcial impede duas. Tabela sem ela é erro de montagem (`pedido.ErrSemRegiaoPadrao`), nunca Frete zero: um zero silencioso daria Frete grátis a todo CEP fora de faixa.
- **"Acima de R$ 299,00" é lido como `subtotal ≥ limiar`.** É o ponto em que o Carrinho da 4.5 já para de dizer "Faltam R$ X"; ler como `>` faria a Revisão cobrar Frete de quem o Carrinho acabou de dizer que não pagaria.
- **A cotação é uma leitura de `pedido`: `GET /api/v1/frete?endereco_id=…`.** `pedido.CotarFrete` lê o Endereço do dono (`identidade.BuscarEndereco`, nova, com o dono no `WHERE`) e o subtotal de `carrinho.Itens`, pelas portas que o `AD-1` já tinha. O total sai somado do Go; o navegador não soma. Congelar o Frete no Pedido continua sendo da 5.6.


**Estória 5.4 — Revisão do Pedido** (2026-09-19):

- **A `Idempotency-Key` nasce na *entrada* da Revisão, e não no clique.** Gerada no clique, o duplo clique produziria duas chaves e dois Pedidos, e a idempotência do servidor (`AD-7`) estaria correta e inútil — o teto que o `NFR-12` pede não seria atingido por ninguém. Gerada na entrada, as duas requisições do duplo clique carregam a mesma chave, e a segunda devolve o Pedido da primeira.
- **Ela fica em `sessionStorage`, porque o recarregamento é o caso que ela existe para proteger.** Em `useState` ou em `useRef` a chave morreria no F5, que é exatamente quando o Comprador não sabe se o Pedido nasceu e reenvia. A chave é **uma por tentativa de checkout**: reaproveitada no recarregamento, na ida e volta ao Carrinho e na troca de Endereço, e apagada só na criação do Pedido, por `limparCheckout` — que nasce na 5.4, testada, mas **quem a chama é a 5.6**. É estado de tela, não de domínio: o Node continua sem estado de domínio (`AD-10`).
  - *(7.3: substituída pela 5.6 — a chave vale uma tentativa de checkout, e o "Fechar o Pedido" do Carrinho a descarta; ver o penúltimo item da 5.6)*
- **A geração tem queda por contexto não seguro.** `crypto.randomUUID` só existe em contexto seguro — `localhost` ou HTTPS —, e a apresentação pode rodar por IP da LAN, que não é nenhum dos dois. `uuidNovo` cai em `crypto.getRandomValues` e monta o UUIDv4 à mão (versão no nibble alto do sétimo byte, variante nos dois bits altos do nono). Nada vem da rede (`NFR-15`) e nenhuma biblioteca entra por isso.
- **Sem armazenamento, a Revisão abre e o Confirmar Pedido não.** Janela privada, site bloqueado ou cota cheia devolvem `null` em vez de uma chave de mentira: a tela mostra tudo — Itens, Endereço, valores — e diz que a confirmação ficaria desprotegida, em vez de prometer uma proteção que não teria. Mesma disciplina do `try/catch` que a 5.2 já aplicava à escolha de Endereço.
  - *(7.3: precisão: com o armazenamento bloqueado de todo, a escolha do Endereço já falha antes e a Revisão volta ao passo Endereço; o aviso "sem chave" é do caso em que só a escrita da chave falha, como a cota cheia)*
- **A divergência entre as duas leituras não é decidida na Revisão.** O subtotal do `GET /api/v1/carrinho` e o da cotação podem discordar, porque um preço mudou entre uma leitura e a outra. O bloco de valores exibido é o da cotação, inteiro, e a tela não arbitra: quem recusa é a criação do Pedido, sob a mesma trava da Reserva, com `TOTAL_DIVERGENTE` (5.6).
- **Correções da revisão da 5.4.** Três viram mecanismo e ficam registradas. (a) **A chave guardada é conferida antes de reaproveitada:** `sessionStorage` é do navegador, e outra aba, uma extensão ou um dedo no console escrevem nele — na 5.6 esse valor vira o cabeçalho `Idempotency-Key`, onde um caractere fora do *token* HTTP, ou um valor quilométrico, quebra a requisição em vez de falhar com jeito. Fora da forma de UUIDv4, vale como nada guardado: gera e sobrescreve. (b) **O `disabled` do Confirmar Pedido deriva das razões, e não de um literal:** `CRIACAO_DISPONIVEL` é a única linha que a 5.6 vira, e a guarda de armazenamento continua travando o botão sozinha depois disso — com o literal, ligar a confirmação apagaria a guarda em silêncio e deixaria nascer Pedido sem chave. (c) **A Revisão lista Itens de Carrinho, não Itens de Pedido:** o Pedido ainda não existe, e só a criação congela o preço praticado (PRD §3, FR-23) — o título da seção promovia o termo antes do fato.

**Estória 5.5 — entrada no checkout: revalidação e confirmação do preço visto** (2026-09-20):

- **A entrada no checkout é rota própria, de `pedido`, e escreve.** `POST /api/v1/checkout/entrada` não é um segundo `GET /api/v1/carrinho`: numa transação só (AD-4), ela lê o Carrinho, **monta a resposta** e só então chama `carrinho.ConfirmarPrecoVisto` com **exatamente os preços que acabou de reportar**. A ordem é a estória inteira (AD-17): confirmar antes de reportar, ou reportar antes do `Commit`, são os dois jeitos de a mudança de preço sumir sem ninguém ver. O JSON sai **depois** do `Commit` — se o `Commit` falhar, nada foi reportado e o aviso volta na entrada seguinte; o sentido oposto, ciência gravada e resposta perdida, não existe. Mora em `internal/pedido/checkout.go`, no molde de `CotarFrete`: `pedido` recebe a transação e a passa a `carrinho.Itens`, que a aceita por tipagem estrutural.
- **`carrinho.ConfirmarPrecoVisto` é a única porta da ciência, e tem um chamador só.** Confirma só as linhas **visíveis** reportadas com `preco_mudou` — a invisível não tem preço a confirmar, e o visto dela nunca é tocado —, lista vazia é no-op com `nil`, e a posse entra no `WHERE` (AD-11). Ordena por `ItemID` antes de gravar, e isso é **determinismo do comando** — a mesma entrada produz sempre os mesmos parâmetros —, e **não** a ordem de travas do AD-5: num `UPDATE ... FROM (unnest ...)` quem ordena o bloqueio é o plano do executor, e a garantia do AD-5 vem de `SELECT ... ORDER BY id FOR UPDATE`, que é de quem decide Estoque. O resíduo — deadlock entre duas entradas simultâneas do mesmo Comprador, com a borda HTTP que ainda não repete em `40P01` — está no `deferred-work.md`. `Adicionar` e `AlterarQuantidade` continuam gravando o visto e **não** contradizem o AD-17: elas gravam o preço de uma alteração que o Comprador acabou de fazer, que já é ciência; o que só `ConfirmarPrecoVisto` faz é persistir a ciência de uma mudança que ele **não** provocou.
- **A resposta é o envelope do Carrinho, de propósito.** `saidaCarrinho` saiu de dentro de `lerCarrinho` para `envelopeDoCarrinho`, e as duas rotas o devolvem. Assim a tela do passo Endereço reaproveita `revalidacaoDe` e `textoDoBloqueio` sem uma linha de regra nova (AD-10), e os dois `Alert` ficam idênticos aos que o Comprador acabou de ver no Carrinho — só a ação do bloqueio muda: aqui não há Ajustar nem Remover, que são do Carrinho, e sim o caminho de volta a ele. Sem corpo na requisição: o cookie é `SameSite=Lax`, então um `<form>` cross-site não o envia, e o precedente é `DELETE /api/v1/carrinho/itens`.
- **A Revisão desvia, e não escreve.** `desvioDaRevisao`, pura, em `web/lib/checkout.ts`: bloqueio manda ao `/carrinho`, onde estão Ajustar e Remover; preço que mudou **depois** da entrada manda ao `/checkout/endereco`, que é a rota que reporta e confirma; senão `null`. Ela lê o `GET /api/v1/carrinho` que a Revisão já pedia — nenhuma leitura nova —, e o desvio é aplicado junto da guarda de Carrinho vazio, antes de qualquer pintura. Uma segunda escrita de `preco_visto_centavos` ali seriam dois lugares gravando o mesmo campo.
- **Confirmar o preço não desbloqueia.** As duas listas continuam independentes, como na 4.4: uma linha com preço mudado **e** bloqueio tem o preço confirmado e o bloqueio mantido, e o Continuar segue travado. É o que fecha o furo que a 5.2 registrou — quem digitava `/checkout/endereco` ou `/checkout/revisao` passava por cima do `podeAvancar` do Carrinho.
- **O `unnest` de dois argumentos não passa pelo sqlc v1.31.1.** `unnest(@ids::uuid[], @precos::bigint[])` sai como `function unnest(unknown, unknown) does not exist`: o analisador não modela a forma multiargumento, que no Postgres é construção de `ROWS FROM` e não função comum. O par sai de dois `unnest ... WITH ORDINALITY` juntados pela posição — mesmo plano, mesma semântica, e o gerador aceita.
- **O `:execrows` é conferido, e a escrita parcial é erro.** O número de linhas afetadas é comparado com o número de vistos: menos linhas significa que o Carrinho mudou debaixo da transação — um Item removido noutra aba, em `READ COMMITTED` —, e passar calado devolveria um "de X para Y" que não foi gravado, com o aviso voltando para sempre. É o defeito que a estória existe para matar, e por isso a divergência desfaz a transação: nada reportado, nada gravado, e a entrada seguinte avisa de novo. Sem sentinela novo (era `Ask First`), então a recusa sai no 500 genérico; nomeá-la fica para a 5.6, junto de `TOTAL_DIVERGENTE`.
  - *(7.3: substituída pela 5.6 — a escrita parcial sai em 409 `CARRINHO_MUDOU`; ver o item "`carrinho.Esvaziar` apaga só os Itens lidos" da 5.6)*
- **A abertura das duas telas é decidida por função pura, e não dentro do `useEffect`.** `aberturaDoPassoEndereco` e `aberturaDaRevisao` recebem as respostas e devolvem o que fazer; `podeContinuar` e `razoesDoContinuar` decidem a trava do avanço. O motivo é que o `web/` não monta componente em teste, e as duas linhas que carregam a estória — abrir pela rota que **escreve**, e travar o Continuar — passariam por `npm test` e `go test` se alguém as revertesse. Com a decisão fora do efeito, `web/scripts/checkout.test.mjs` prende a rota, o método, a ordem das guardas e a trava. Duas consequências ficaram registradas no próprio código: o relatório da entrada é aplicado **antes** da guarda da lista de Endereços — uma ciência já comitada não pode ser descartada porque uma leitura posterior falhou —, e a entrada dispara **uma vez por tentativa**, por `useRef`, porque o StrictMode do `npm run dev` monta duas vezes e o segundo POST já voltaria silencioso.
- **O Continuar usa `aria-disabled`, e não o `disabled` nativo.** Botão desabilitado de verdade não recebe foco, e o leitor de tela nunca alcançaria a razão que o `aria-describedby` aponta — a razão existiria só para quem enxerga. Quem barra o avanço é a guarda dentro de `continuar`, que é onde ela tem de estar de qualquer forma.
- **Nenhuma migração, nenhuma coluna, nenhuma variável nova.** `preco_visto_centavos` existe desde a 4.1, e o limiar de isenção continua sendo o da configuração. O que a estória acrescenta é uma consulta, uma função de módulo, uma função de `pedido`, uma rota e uma função pura no `web/lib`.
- **O recálculo do Frete ao cruzar o limiar já existia, e agora tem prova.** `CotarFrete` lê `carrinho.Itens` a cada abertura da Revisão, sempre pelos preços de agora; o que faltava era o teste. `api/checkout_test.go` derruba o preço abaixo do limiar de isenção, reentra no checkout e vê a cotação deixar de ser Grátis, e sobe de volta e a vê voltar a ser — nos dois sentidos, com o total acompanhando. Nenhum campo novo na resposta.

**Estória 5.6 — criação do Pedido com Reserva de Estoque atômica** (2026-09-21):

- **A `Idempotency-Key` é coluna do próprio Pedido, e só se prende quando o Pedido existe.** `pedido.pedido` ganhou `chave_idempotencia uuid` e `digest_corpo text`, com índice único `(comprador_id, chave_idempotencia)` — a chave é do Comprador, e a mesma chave noutra conta é chave livre. Recusa por Estoque, por total ou por Carrinho desfaz a transação e deixa a chave livre; é isso que fecha a dívida da 5.4 (voltar ao Carrinho, mudar a quantidade e confirmar sob a mesma chave não é 409). O 409 `CHAVE_REUTILIZADA` só sobra para a chave que **já criou** um Pedido com outro corpo, e leva esse Pedido em `dados`: a tela apaga a chave e mostra o Pedido. O digest é SHA-256 do Endereço (em minúscula) e do total do corpo, calculado em `pedido`.
- **O `INSERT` do Pedido é a reivindicação da chave, e vem antes da trava dos Produtos.** `CriarPedido` é `ON CONFLICT (comprador_id, chave_idempotencia) DO NOTHING`: o gêmeo do duplo clique espera e, quando o primeiro comita, recebe zero linhas e relê o Pedido pela chave — o `23505` nunca existe, então nunca chega ao navegador. Invertido, o gêmeo chegaria à última unidade depois do primeiro e voltaria `ESTOQUE_INSUFICIENTE` em vez do Pedido original. Na prática o primeiro ponto de espera é o contador por ano (`ProximoNumeroDoAno`), que vem antes porque o número vai no `INSERT`; o desfecho é o mesmo. A chave também é relida em toda recusa anterior ao `INSERT` que depende do Carrinho (Carrinho vazio, Carrinho só de Produtos invisíveis, total divergente): entre a primeira leitura e a recusa, o gêmeo pode ter comitado e esvaziado o Carrinho, e a resposta certa é o Pedido original, nunca `CARRINHO_VAZIO`. O 404 do Endereço não é relido: o gêmeo não apaga Endereço, então a mesma chave com o mesmo corpo não tem como virar Endereço alheio entre as duas leituras.
- **O total é conferido duas vezes, e a segunda é com os Produtos travados.** Antes do `INSERT`, contra o corpo; depois de `catalogo.Reservar`, com os Produtos travados por `ORDER BY id FOR UPDATE`, os preços são relidos, o Frete é recalculado sobre eles — a Regra de Frete é relida mas **não** travada: `pedido.faixa_frete` só muda por migração — e as **duas parcelas** — subtotal e Frete, não só o total — têm de bater com o que foi ao `INSERT`. Só o total não bastaria: um preço que cai e cruza o limiar de isenção pode fechar o mesmo total com outra divisão, e o Pedido gravado mentiria sobre o subtotal. O gatilho da 5.1 impede corrigir depois, então divergir é `TOTAL_DIVERGENTE` e rollback.
- **Congelamento em colunas do `INSERT`.** `subtotal_centavos`, `frete_centavos` e as oito colunas `endereco_*` nasceram protegidas pelo gatilho da 5.1 sem tocá-lo, com `CHECK (total_centavos = subtotal_centavos + frete_centavos)` (AD-9). Os Pedidos antigos, do esqueleto, receberam subtotal = total e Frete zero com o gatilho desligado só no preenchimento; Endereço, chave e digest ficam nulos neles, e um `CHECK` com `num_nulls` exige os dez presentes ou os dez ausentes. O Vendedor de cada Item já estava congelado em `item_pedido.vendedor_nome` desde a 1.6 — nenhuma coluna de Vendedor nova.
- **`item_pedido` recusa `INSERT` de Pedido que já tem histórico.** Fecha o `INSERT` tardio que a 5.1 deixou aberto: a regra é "não existe linha em `transicao_status` para este Pedido", e por isso `Criar` grava os Itens **antes** da primeira transição.
- **`carrinho.Esvaziar` apaga só os Itens lidos, e linha a menos é `carrinho.ErrCarrinhoMudou`.** Um Item adicionado noutra aba entre a leitura e o fim fica no Carrinho e não entra no Pedido. O mesmo sentinela passou a nomear a escrita parcial de `ConfirmarPrecoVisto` (409 `CARRINHO_MUDOU`), que saía no 500 genérico. Para registrar o sentinela, `internal/plataforma` ganhou a aresta para `carrinho` na tabela do teste de fronteira — é o tradutor do AD-14, e a seta contrária continua proibida.
- **A repetição única em `40P01`/`40001` existe em `api/transacao.go`.** `emTransacao` abre, roda, comita e repete uma vez, e só uma, quando o Postgres desiste por impasse ou serialização; a função de caso de uso devolve `confirmar` falso para desfazer sem erro, que é o reenvio da criação (200, nada gravado, nem o número do contador). `criarPedido` e `entrarNoCheckout` a usam; o JSON sai só depois dela, porque uma resposta escrita dentro da transação sairia duas vezes.
- **Carrinho só de Produtos invisíveis sai como `ESTOQUE_INSUFICIENTE`, e não no `CHECK`.** Sem parcela visível, o subtotal seria zero e o `CHECK (subtotal_centavos > 0)` derrubaria o `INSERT` num 500; `Criar` responde antes, pela mesma releitura da chave das outras recusas, com a falta de disponível zero que `Reservar` daria (`dados.produto_id` nomeia o primeiro).
- **O botão do esqueleto saiu da Página de Produto.** O único caminho até um Pedido é Carrinho → Endereço → Revisão, e o laranja voltou a aparecer uma vez só no fluxo, no Confirmar Pedido da Revisão. Na tela, o que fazer com cada resposta é decisão pura de `desfechoDaConfirmacao`, e o corpo e o cabeçalho, de `requisicaoDeConfirmar` — os dois presos em `web/scripts/checkout.test.mjs`; o envio único é um `useRef`, porque o `disabled` do estado só chega no render seguinte.
- **A chave vale uma tentativa de checkout, e o "Fechar o Pedido" do Carrinho começa outra (revisão da 5.6).** Substitui a decisão da 5.4 de que a chave sobrevive à ida e volta ao Carrinho. Se o 201 se perde no caminho, o Pedido nasceu e a chave ficou guardada; o Comprador enche o Carrinho de novo e confirma sob a mesma chave — e recebe em silêncio o Pedido **antigo** (200, mesmo total) ou o 409 que o abre. Como a chave só se prende quando um Pedido comita, rodá-la no começo de cada tentativa é inofensivo: `iniciarTentativaDeCheckout` a descarta, mantém a escolha de Endereço (FR-22), e a Revisão gera outra ao abrir. Dentro da tentativa — recarregar a Revisão, ir ao passo Endereço e voltar — a chave é a mesma, que é o caso que ela protege. O 409 `CHAVE_REUTILIZADA` descarta a chave **sempre**, com ou sem `dados.id`, e a tela diz que a confirmação já criou um Pedido, com o link quando há `id` — nunca navega em silêncio. O 404 `NAO_ENCONTRADO` descarta só a escolha de Endereço e volta ao passo Endereço; outro 404 é falha genérica.
- **A execução do desfecho também é pura (revisão da 5.6).** `executarDesfecho` recebe o desfecho e os efeitos injetados (limpar, descartar a chave ou a escolha, avisar o Carrinho, navegar, reler, avisar) e diz se a guarda de envio pode ser solta; o componente só entrega os efeitos. O Confirmar Pedido usa `aria-disabled` com a guarda dentro de `confirmar`, como o Continuar da 5.5, e o `fetch` tem prazo de 30 s por `AbortController` — esgotado, sai a falha de rede e a guarda é solta, e a mesma chave torna o novo clique seguro.

**Estória 5.7 — consistência de Estoque sob concorrência** (registrado na 7.3, 2026-09-25):

- **O teste do NFR-7 põe N Compradores distintos na mesma largada, com N maior que o Estoque inicial, e compara por igualdade.** Cada participante tem conta, Carrinho, Endereço, cotação e `Idempotency-Key` próprios, e todo esse preparo é sequencial e fica fora da disputa. A largada é um canal fechado depois de as N goroutines estarem paradas nele, e não o simples `go`, para nenhuma sair na frente por ter nascido antes. São duas rodadas: 8 sobre Estoque 1 e 12 sobre Estoque 3. A segunda existe porque uma trava que só distinguisse "existe ou não existe" passaria na primeira e venderia demais na segunda. `N <= Estoque` é falha, porque só com N maior o enunciado `pedidos_criados == estoque_inicial` vale; a comparação é `==`, nunca `<=`; e todo perdedor tem de sair 409 `ESTOQUE_INSUFICIENTE`. O `MaxConns` fica no padrão do `ambiente()`: ele limita quanta corrida a rodada gera, mas não o que a igualdade afere, e mudá-lo alteraria a bancada dos outros testes. (`api/concorrencia_test.go:49-56,86-103,105-126,164-169`; spec `spec-5-7-consistencia-de-estoque-sob-concorrencia.md:19,23-27,50,52`)
- **As asserções de Estoque leem as tabelas cruas, e não a VIEW, e contam as linhas novas de `pedido.pedido`.** A sobra é `estoque_total − Σ Reservas ATIVA` sobre `catalogo.produto`. A `produto_visivel` envolve a conta em `greatest(…, 0)` e devolveria zero tanto para uma unidade vendida quanto para oito, justamente no caso de venda a mais que o teste existe para pegar. A contagem de `pedido.pedido` antes e depois pega o Pedido órfão, sem Item e sem Reserva, porque o `INSERT` que reivindica a chave roda antes de `catalogo.Reservar`. Os dois furos foram achados na revisão. (`api/concorrencia_test.go:128-157`, `db/migracoes/20260918150000_catalogo_produto_criado_em.sql:13`; spec `spec-5-7-…:48,58-59`)
- **A trava do AD-5 é provada fora do caminho do contador, chamando `catalogo.Reservar` direto em duas transações abertas à mão, e a numeração não foi movida para depois da Reserva.** `travaDeReservaSerializa` exige que a segunda Reserva bloqueie enquanto a primeira está aberta, com espera fixa de 500 ms, e que depois do commit saia `EstoqueInsuficiente` com disponível 0. Se um dia flacar, a saída nomeada é sondar `pg_locks` com `granted = false`. O Pedido é um `uuidFalso`, o que funciona porque `reserva_estoque.pedido_id` não tem FK. A alternativa era mover a numeração para depois da Reserva, e foi recusada: seria mudar a produção e a ordem da 5.6, porque o `INSERT` do Pedido é a reivindicação da chave, e adiá-lo trocaria o desfecho do duplo clique para "o gêmeo perde a última unidade". Uma mutação confirmou a necessidade do teste isolado: tirado o `FOR UPDATE OF p`, a disputa de ponta a ponta continuou verde, e este teste morre em 0,03 s. (`api/concorrencia_test.go:279-339,344-346`; spec `spec-5-7-…:42-46,66,77`)
- **O teste da concorrência mora dentro de `TestSessaoEProduto` e nunca sai do `go test ./...` padrão.** Não é `Test` de topo porque `ambiente()` sobe um par de contêineres por função de topo, e uma função nova custaria mais dois por execução sem provar nada a mais. Não tem build tag nem `testing.Short()`, porque é o teste que "nunca é cortado". A verificação inclui `go test -race -count=1 ./api/`, porque é o primeiro pacote onde N goroutines atravessam os handlers em paralelo e escrevem na mesma fatia. (`api/sessao_test.go:538-540`, `api/sessao_test.go:88`; spec `spec-5-7-…:31,40,76`)

**Estória 5.8 — Tentativa de Pagamento e a tela do Pedido em processamento** (2026-09-22):

- **A resposta do Pedido carrega a tripla e o resto que a tela deriva, montada por `pedido`.** `GET /api/v1/pedidos/{id}` passou a devolver, além de id, número, Status, total, `atualizado_em` e `terminal`: `subtotal_centavos`, `frete_centavos`, `expira_em`, `tentativas_restantes`, `endereco` (com a forma da etiqueta de "Meus endereços", sem id; `null` no Pedido do esqueleto), `itens[]` com `disponivel` e `historico[]` com `de`/`motivo` nulos quando vazios. `pedido.Detalhar` substitui `pedido.Buscar` e lê `pagamento.TentativasRestantes` e `catalogo.Disponivel` pelas portas que o AD-1 já tinha. O histórico entra agora, e não na 6.2, porque é nele que o AD-18 põe o `motivo` que a superfície recusada exibe; a 6.2 só o desenha.
- **`expira_em` é a última entrada em `AGUARDANDO_PAGAMENTO` mais `AZAMON_PAGAMENTO_TENTATIVA_EXPIRACAO`, e `null` fora dele.** Sai do histórico (AD-6), pela pura `pedido.ExpiraEm`; "última" é o que faz a nova Tentativa da 5.10 recomeçar o prazo sem código novo. A expiração da 5.11 precisa usar o mesmo instante, ou a tela e a varredura discordam de quando venceu.
- **O teto de Tentativas é de `pagamento`, e a conta também.** `pagamento.TentativasRestantes(ctx, bd, pedidoID, teto)` conta as linhas de `tentativa_pagamento` e devolve `max(teto − feitas, 0)`; o teto entra como valor, da Config. A expiração não abre nem consome Tentativa.
- **`disponivel` é "o Estoque disponível de agora cobre a quantidade".** Enquanto a Reserva do próprio Pedido está ativa ela conta contra o Item, então um Pedido que levou a última unidade lê `false` em `AGUARDANDO_PAGAMENTO`. É de propósito: só `PAGAMENTO_RECUSADO` — com a Reserva já liberada — usa o campo para derivar ação, e descontar a própria Reserva seria nomeá-la numa conta que o Comprador não vê. Produto invisível (desativado, ou de Vendedor desativado) sai `false`.
- **A leitura roda numa transação só leitura em `REPEATABLE READ`.** São cinco consultas, e sem um instante comum o Status podia chegar de um lado de uma transição e o histórico — de onde sai `expira_em` — do outro. Só leitura não trava ninguém nem sofre falha de serialização, então não entra em `emTransacao` nem tem o que repetir.
- **O Provedor Simulado decide pelos centavos só na primeira Tentativa.** `Simulado.Decidir(total, numero)` aprova sempre com `numero > 1`, inclusive nos centavos que recusam e nos que nunca confirmam, e `TentativasSemConfirmacao` passou a trazer o `numero`. A emissão continua só do caminho aprovado: emitir a recusa sem quem a aplique faria a varredura sinalizá-la como `NAO_APLICAVEL_SINALIZADA` ao Administrador, e aplicar a recusa é da 5.10.
- **A tela tem três superfícies na mesma página, e nenhuma promete ação que não existe.** Aguardando: relógio que só subtrai o agora de `expira_em` a cada segundo, fora da região `role="status"`, e, zerado, a frase "O prazo para o pagamento terminou sem a confirmação chegar." — que diz o que houve sem prometer quando o Pedido muda — com a consulta seguindo; até a 5.11 existir, é onde um total de `,95` fica. Recusado: o motivo ("Tempo de pagamento expirado." para `TEMPO_ESGOTADO`) na região `role="status"`, as tentativas restantes e, abaixo, o Detalhe, sem "Tentar pagar de novo" (5.10) nem "Cancelar Pedido" (6.3), nem desabilitados. O resto: o Detalhe no lugar — Itens a preço praticado, valores congelados e Endereço; a linha do tempo é da 6.2. "Ver o Pedido em Meus pedidos" existe em todas, inclusive carregando e com erro. A leitura da resposta mora em `web/lib/pedido.ts`, testada.
  - *(7.3: a 5.10 pôs "Tentar pagar de novo" e a 6.3 o "Cancelar Pedido" nesta tela; a regra — nenhuma ação prometida que não exista — continua valendo)*
- **O desvio de relógio entre navegador e servidor ficou de fora.** Na demonstração os dois são a mesma máquina; apresentado por outro aparelho da LAN, um relógio adiantado encurta o tempo exibido, mas nunca o prazo real, que é do servidor.

**Estória 5.9 — Confirmação aprovada por webhook** (2026-09-22):

- **A estória chegou quase pronta, e o que ela fechou foi a borda.** O caminho aprovado inteiro — inbox com restrição única, `pagamento` sem conhecer `pedido`, a varredura lendo por `ConfirmacoesNaoAplicadas` e aplicando, `PAGO` com a Reserva mantida, a mesma confirmação duas vezes com um efeito só e a Tentativa superada sinalizada — está de pé e testado desde a 1.7, e a tela que vira sozinha, desde a 5.8. O que faltava era o caso que a FR-26 nomeia e a invariante 7 do §2 proíbe perder: **aprovação que chega para um Pedido já `CANCELADO`**.
- **O sinal ao Administrador é a própria linha da inbox, sem coluna nova.** `NAO_APLICAVEL_SINALIZADA` sobre uma confirmação `APROVADO` numa Tentativa de um Pedido `CANCELADO` é o registro; a 6.5 o deriva na leitura, metade em `pagamento` (pela porta, como `TentativasRestantes`) e metade no Status do Pedido, e junta em Go — SQL cruzando schema seria a aresta que o AD-1 não tem. Coluna de motivo foi rejeitada: estado a manter para responder o que uma junção responde, e a AC da 6.5 já nomeia `NAO_APLICAVEL_SINALIZADA` como o sinal. Confirmação de Tentativa superada que caia no mesmo predicado não é falso positivo — o Provedor aprovou aquela cobrança, e o Pedido está cancelado do mesmo jeito.
- **"Registrada na Tentativa" é a linha da inbox, e não uma coluna em `tentativa_pagamento`.** A invariante 7 do §2 e a FR-26 dizem "registrada na Tentativa de Pagamento"; quem guarda o estado é `pagamento.confirmacao_recebida`, que aponta para a Tentativa por `tentativa_id`. Quem chegar na 6.5 pela invariante procura a coluna na tabela errada, então fica dito aqui: o registro é a confirmação, presa à Tentativa.
- **O Pedido cancelado _depois_ de `PAGO` casa no predicado de propósito.** `janelaDeCancelamento` cobre `PAGO` e `SEPARANDO`, e nesse caso a confirmação sinalizada é uma **segunda** aprovação além da que já virou `PAGO` — cobrança duplicada sobre Pedido cancelado, que é mais urgente para o estorno, não menos. Restringir o predicado a "nunca esteve em `PAGO`" esconderia justamente esse caso. O aviso no log segue a mesma regra.
- **O caso ganhou nome e um aviso, não um caminho novo.** `aplicarConfirmacao` emite `slog.WarnContext` quando a confirmação é aprovada e o Pedido travado está em `CANCELADO` — **depois do commit**, porque commit que falha devolve a confirmação a `PENDENTE` e o aviso emitido antes se repetiria a cada tique sobre uma sinalização que ainda não existe. `Corrente` não entra na condição, ao contrário do `if` que aplica: aprovação de Tentativa superada também é dinheiro aprovado. O log **não** é caminho de leitura — é para quem estiver olhando o processo na hora; o que a 6.5 lê é a inbox.
- **Quem protege o Status do Pedido é o CAS de `Transicionar`, e não a condição do `if`.** Sem ela, o `UPDATE … WHERE status = 'AGUARDANDO_PAGAMENTO'` casaria zero linhas e sairia `ErrEstadoJaAvancado`. A condição existe para nomear o caso e não gastar uma transição perdida — por isso a mutação com dente é sobre o estado gravado na inbox, e não sobre ela.
- **A emissão continua cega ao Pedido (AD-7).** O Provedor Simulado emite para a Tentativa de um Pedido cancelado como para qualquer outra; fazê-la consultar o Status seria a aresta proibida, e apagaria justamente a informação que a invariante 7 manda guardar. Quem decide não aplicar é a varredura, com a trava na mão.

**Estória 5.10 — Pagamento recusado e nova Tentativa** (2026-09-22):

- **A recusa entra pelo mesmo caminho do aprovado, e o `Corrente` é o que a torna segura.** `EmitirConfirmacoesDevidas` passou a emitir o `RECUSADO` (só a faixa em que a confirmação nunca chega fica sem emissão, porque é ela que a expiração resolve), e `aplicarConfirmacao` aplica a confirmação da Tentativa corrente com o Pedido em `AGUARDANDO_PAGAMENTO`, qualquer que seja o resultado: aprovada vai a `PAGO`, recusada a `PAGAMENTO_RECUSADO` com ator `PROVEDOR`, e a Reserva é liberada dentro de `Transicionar`. Sem o `Corrente`, uma recusa atrasada da Tentativa 1 derrubaria a Reserva que a 2 acabou de criar — o caso que a espinha nomeia no AD-7, agora com teste.
- **A recusa do Provedor grava o motivo `RECUSADO_PELO_PROVEDOR`.** A tela o escreve "O Provedor de Pagamento recusou a Tentativa de Pagamento." — nomear o motivo sempre que o sistema souber, e o Provedor Simulado não sabe mais do que isso: inventar "saldo insuficiente" seria afirmar o que o sistema não sabe. `TEMPO_ESGOTADO` continua reservado à 5.11.
- **O número da Tentativa é derivado, e o teto é conferido por `pagamento`.** `IniciarTentativa` ganhou o parâmetro `teto` (da Config, AD-13) e numera como "as que o Pedido já tem, mais uma", recusando além do teto com `pagamento.ErrTetoDeTentativas` sem gravar nada. Contar e somar um só é seguro porque quem chama segura a linha do Pedido: a primeira Tentativa nasce no `INSERT` dele, e a nova depois do compare-and-swap. Nenhuma consulta nova: `ContarTentativas` já existia. `pedido.Criar` passou a receber o teto também, porque é ele que abre a primeira.
- **`pedido` traduz o teto, e é a tradução que chega à borda HTTP.** `plataforma/erro` não pode importar `pagamento` — o AD-1 não tem essa aresta, e o teste de fronteira a recusa —, então `pedido.NovaTentativa` embrulha a causa em `pedido.ErrTentativasEsgotadas`, e é este o registrado (409, `TETO_DE_TENTATIVAS`). É a leitura literal do AD-8: "`pedido` traduz isso na recusa da transição". O erro devolvido embrulha os dois, então `errors.Is` casa com qualquer um.
- **Na nova Tentativa, a Tentativa vem depois do CAS.** `NovaTentativa` lê o Pedido pelo dono (404 para alheio, inexistente e malformado), chama `Transicionar(PAGAMENTO_RECUSADO → AGUARDANDO_PAGAMENTO, COMPRADOR)` — compare-and-swap, a linha do histórico de que `ExpiraEm` recomeça o prazo, e a nova Reserva sobre os Itens do próprio Pedido — e **só então** `IniciarTentativa`. Na ordem inversa, dois cliques simultâneos contariam o mesmo número e o segundo sairia em 500 pelo índice único de `id_externo`; nesta, o segundo espera na linha do Pedido e sai `ESTADO_JA_AVANCADO`, que a tela já sabe tratar. O custo: com Estoque e teto esgotados ao mesmo tempo, a resposta é `ESTOQUE_INSUFICIENTE`, e a tela relida esconde o botão do mesmo jeito.
- **`POST /api/v1/pedidos/{id}/tentativas` não tem corpo.** Nada viaja do navegador além do identificador na rota, e nada é remontado a partir do Carrinho. O 201 devolve a forma curta da criação; o prazo novo e as Tentativas restantes a tela lê do Detalhe, que ela relê em seguida — também depois das três recusas em que o Pedido mudou (`ESTOQUE_INSUFICIENTE`, `TETO_DE_TENTATIVAS`, `ESTADO_JA_AVANCADO`). A razão de o botão ter saído vem da tripla relida, e não de uma mensagem que envelhece na tela.
  - *(7.3: as recusas `ESTOQUE_INSUFICIENTE` e `TETO_DE_TENTATIVAS` também mostram a mensagem além de reler, decidido na revisão da 5.10; ela some quando o Status sai de `PAGAMENTO_RECUSADO`)*
- **Produto desativado depois de o Pedido nascer sai igual ao esgotado.** É o que a FR-12 manda, e a tela nomeia o Produto pelo nome congelado no Item de Pedido, que continua existindo com o Produto fora do Catálogo. Fecha a entrada que a leitura humana da 5.1 deixou no `deferred-work.md`.
- **"Tentar pagar de novo" é o primário do shadcn, sem pill e sem laranja.** O pill é dos botões de ação da loja (UX-DR5: Adicionar ao Carrinho e Confirmar Pedido), e o laranja, só do Confirmar Pedido (UX-DR7). O "Cancelar Pedido" que a EXPERIENCE põe ao lado continua fora da tela até a 6.3, e nenhum texto o promete.
- **Os subtestes da 1.7 passaram a entregar só a própria Tentativa.** Com a recusa emitida, o transporte que postava toda Tentativa vencida levaria os Pedidos de R$ 249,90 dos subtestes anteriores a `PAGAMENTO_RECUSADO`, e os seguintes contam com eles aguardando pagamento. A 5.10 prova a recusa com conta e Produtos próprios.

**Estória 5.11 — Expiração da Tentativa de Pagamento** (2026-09-22):

- **A expiração e a simulação de entrega compartilham a casca, não a lista.** Os dois passos do tempo do AD-6 são a mesma mecânica — candidatos lidos fora da transação por `PedidosParaAvancar`, uma transação por Pedido, releitura travada por `TravarPedido` e `Transicionar` fazendo o efeito —, e só diferem no mapa de avanços, no ator e no motivo. `SimularEntrega` e `Expirar` passaram a ser duas linhas cada sobre `avancarPeloTempo`/`avancarUm`, com um `passoDoTempo` como parâmetro. Nenhuma consulta nova e nenhum `sqlc generate` em `pedido`: `PedidosParaAvancar` já recebia a lista de Status de origem.
- **O instante da expiração é o mesmo de `ExpiraEm`, e por construção, não por coincidência.** Para um Pedido em `AGUARDANDO_PAGAMENTO`, o `max(ocorrido_em)` que o `TravarPedido` devolve **é** a última transição *para* esse Status — qualquer linha posterior o teria tirado de lá. Por isso a varredura e a tela não fazem duas contas, e a nova Tentativa recomeça o prazo sem código novo. Derruba junto o risco do relógio em memória: o contêiner derrubado no meio do prazo volta e expira no instante que já estava marcado.
- **EMENDA (revisão do passe 1): a ordem do tique não é a garantia — a inbox lida sob a trava é.** A entrada abaixo continua valendo para a ordem, mas ela sozinha deixa três furos, e nos três o Comprador pagou no prazo e perderia a Reserva de Estoque: `pedido.Varrer` que falha na leitura da inbox e volta sem aplicar nada (o tique segue para `Expirar` assim mesmo), a linha que o `FOR UPDATE SKIP LOCKED` de `aplicar` pulou, e a confirmação que chega entre os dois passos. `Expirar` passou a perguntar, **com a linha do Pedido já presa por `TravarPedido` e antes de `Transicionar`**, se existe confirmação `PENDENTE` daquele Pedido; havendo, sai calado e o tique seguinte a aplica. A pergunta é uma porta nova de `pagamento` (`TemConfirmacaoPendente`), dono da inbox pelo AD-7, chamada por `pedido`: a aresta contrária continua não existindo. Só a expiração confere — a simulação de entrega move Pedido já pago, onde não há confirmação por aplicar que se perca.
- **EMENDA: a aprovação que chega depois de uma expiração legítima também avisa.** O `WarnContext` de `aplicarConfirmacao` cobria só o `CANCELADO`; passou a cobrir também o Pedido em `PAGAMENTO_RECUSADO` cuja última transição carrega `TEMPO_ESGOTADO` — dinheiro aprovado sobre Reserva já devolvida é o mesmo tipo de perda silenciosa que a invariante 7 do §2 proíbe. A recusa do Provedor não avisa (não é dinheiro que entrou), o registro continua sendo a própria linha da inbox, e o aviso continua saindo **depois do commit**. Quem exibe ao Administrador continua sendo a 6.5.
- **EMENDA: `CarregarConfig` recusa `AZAMON_CONFIRMACAO_ATRASO >= AZAMON_PAGAMENTO_TENTATIVA_EXPIRACAO`.** Com o piso novo, a janela é `criada_em <= agora - atraso` E `criada_em > agora - prazo`: o par invertido a deixa vazia, e o desfecho é mudo — confirmação nenhuma emitida, todo Pedido expirando, sem erro e sem log. Entra na mesma forma dos outros pares comparáveis (`PAGINA_TAMANHO`/`_MAX`, `SENHA_MIN`/`_MAX`, as duas faixas do Provedor), e o arranque é a única hora em que dá para ver.
- **`expirar` não tem interruptor, e entra entre `aplicar` e `simular`.** O `AZAMON_ENTREGA_SIMULACAO_ATIVA` é da simulação de entrega (FR-33) e não cobre a FR-34 🔒; a expiração também tem relógio próprio (`AZAMON_PAGAMENTO_TENTATIVA_EXPIRACAO`, 15 min, contra `AZAMON_ENTREGA_INTERVALO`, 24 h), o que é a segunda razão de não ser mais uma linha do mapa `simulacao`. A ordem `aplicar` → `expirar` é normativa: uma aprovação que chegou no prazo não pode ser descartada por um relógio que rodou primeiro, e `cmd/azamon/main_test.go` prende essa ordem no binário inteiro — invertidos os dois no tique, o Pedido pago no prazo termina em `PAGAMENTO_RECUSADO` e o teste morre.
- **A janela de emissão passou a ter os dois lados.** `TentativasSemConfirmacao` ganhou o piso `criada_em > @desde` e `EmitirConfirmacoesDevidas` recebe o prazo da expiração por parâmetro, como já recebia o atraso: passado o prazo a Tentativa está morta, porque a varredura já encerrou o Pedido, e emitir sobre ela não teria efeito. Sem migração e sem coluna — a emissão continua **derivada, não marcada** (AD-7) —, e fecha a entrada do ledger em que a faixa `,95`–`,99` era relida a cada tique para sempre.
- **A expiração não emite confirmação nenhuma.** O Provedor não recusou; o tempo esgotou. O caminho é o da recusa e o que os distingue é o motivo — `TEMPO_ESGOTADO`, reservado pela tabela do AD-3 ao ator `VARREDURA` —, e a inbox fica vazia. `web/` não mudou uma linha: `frasesDoMotivo` já tinha a frase e `podeTentarDeNovo` já não olha o motivo.

**Estória 6.1 — Meus pedidos** (registrado na 7.3, 2026-09-25):

- **`GET /api/v1/pedidos` desempata em `p.id DESC`, e não no `id` ascendente que o AD-18 escreve.** A ordenação é o nascimento do mais recente para o mais antigo, `NULLS LAST`, e termina em `id` para que o `OFFSET` fique estável quando duas transações empatam no instante. É `DESC` porque o `id` é uuidv7, portanto cronológico, e o desempate acompanha a direção da lista. A Tabela do Administrador desempata em `p.id` ascendente, então as duas listagens de Pedido não usam o mesmo sentido. (`internal/pedido/db/consultas.sql:210-228`, `:175`; spec `spec-6-1-meus-pedidos.md:128`)
- **`criado_em` é `omitempty` em `saidaPedido`, e só a listagem o preenche.** A criação, a nova Tentativa e o Detalhe compartilham `saidaPedido` e não leem o nascimento. Sem `omitempty`, as três passariam a responder `"0001-01-01T00:00:00Z"`, que é uma data errada e não uma ausência. Se o Detalhe quiser o instante, é `Detalhar` que passa a lê-lo. (`api/pedido.go:38-46`; spec `spec-6-1-meus-pedidos.md:85`)
- **A Sessão é conferida antes da paginação.** Sem Sessão a resposta é 401 `SESSAO_INVALIDA`, mesmo com `?pagina=abc`. Um 400 de campo contaria que a rota existe e como ela pagina. (`api/pedido.go:385-400`; spec `spec-6-1-meus-pedidos.md:86`)
- **`rotuloDoStatus` em `web/lib/pedido.ts` é o ponto único do texto dos sete Status.** Status desconhecido sai cru: aparece feio, e não invisível. O mapa é lido com `Object.hasOwn`, porque um `"constructor"` vindo do servidor acharia o valor herdado de `Object.prototype`. A lista `STATUS` do filtro do Administrador é derivada das chaves desse mapa, e não uma oitava cópia. Sem esse ponto, uma renomeação como `EM_SEPARACAO` → `SEPARANDO` deixaria uma superfície mostrando o identificador cru. (`web/lib/pedido.ts:298-317`, `:375`; spec `spec-6-1-meus-pedidos.md:36`, `:73`)
- **A 6.1 refez a listagem da 2.6.** A ordem deixou de ser `id DESC` e passou a ser o nascimento (`min(ocorrido_em)`) do mais recente para o mais antigo, com paginação e contagem; e o Status, que a 6.1 exibia como texto sem `Badge`, passou ao `SeloDoStatus` na 6.7, como a própria spec previa. (`internal/pedido/db/consultas.sql:210-228`)

**Estória 6.2 — Detalhe do Pedido** (registrado na 7.3, 2026-09-25):

- **A linha do tempo é o `historico` da própria resposta, na ordem do Go, e o `ator` não aparece para o Comprador.** `linhaDoTempo` é função pura. Ela mapeia `historico` (`ORDER BY ocorrido_em, id`) sem acrescentar etapa futura nem fazer chamada nova. O texto sai dos mesmos pontos únicos: `rotuloDoStatus` para o Status e `fraseDoMotivo` para o motivo, e `fraseDoMotivo` é também a fonte de `motivoDaRecusa`. O `ator` continua na resposta do Comprador (`saidaTransicao`) e é descartado na tela, porque pertence ao Administrador e ao NFR-9. (`web/lib/pedido.ts:103-134`; `internal/pedido/db/consultas.sql:86-90`; `api/pedido.go:113-116`; spec `spec-6-2-detalhe-do-pedido.md:22-23`, `:35`)

**Estória 6.3 — Cancelamento pelo Comprador** (2026-09-23):

- **O cancelamento lê o Pedido já travado, antes do compare-and-swap.** `pedido.Cancelar` abre com `TravarPedidoDoComprador` — o dono no `WHERE`, como em `BuscarPedidoDoComprador`, e `FOR UPDATE` **que espera**, e não o `SKIP LOCKED` da varredura: o Comprador que cancela precisa do desfecho, e não de "tente no próximo tique". Com a linha presa, o Status lido é o que o CAS de `Transicionar` vai ver, e as duas corridas viram desfecho certo: o segundo clique espera o primeiro comitar e lê `CANCELADO`, e o cancelamento que espera um `AGUARDANDO_PAGAMENTO → PAGO` ainda não comitado cancela o `PAGO`, em vez de sair `ESTADO_JA_AVANCADO` de um Pedido que ainda cancela. É também a primeira trava do AD-4 — a linha do Pedido antes dos Produtos, que `Liberar` trava depois. A varredura, com `SKIP LOCKED`, só pula esse Pedido naquele tique. Sem migração e sem mudar a tabela do AD-3 nem as três recusas de `maquina.go`: `corridaPerdida` continua lá para os outros chamadores, e para o cancelamento deixa de ser alcançável. Preso por mutação: tirado o `FOR UPDATE`, o teste do duplo clique sai 200 e 409 e o do envio concorrente sai `dados.status = SEPARANDO`.
- **`CANCELADO` é sucesso sem efeito, decidido em `pedido`, e não na tela.** `Cancelar` devolve o Pedido lido sem chamar `Transicionar` — nenhuma linha no histórico, nenhuma Reserva tocada, 200. É a leitura literal da FR-31 ("sobre Pedido já `CANCELADO` não produz efeito nem erro") e fecha a entrada da 5.1 no `deferred-work.md`: a alternativa de travar só o botão deixaria a segunda aba, ou o reenvio do navegador, recebendo "Este Pedido não pode mais ser cancelado." de um cancelamento que deu certo. `foraDaJanela` continua contando `CANCELADO` — é a regra da máquina, e quem a chama direto com `esperado = CANCELADO` continua recusado.
- **`Cancelar` não chama `Liberar` nem pergunta por Reserva.** O efeito é de `Transicionar`, incondicional (AD-5): o `PAGAMENTO_RECUSADO` já não tem Reserva ativa, e `Liberar` sem Reserva é `nil`.
- **`POST /api/v1/pedidos/{id}/cancelamento`, sem corpo.** 200 com a forma curta do Pedido em `CANCELADO`; `ENVIADO` e `ENTREGUE` saem 409 `FORA_DA_JANELA_DE_CANCELAMENTO` com o Status lido sob a trava em `dados.status`, sem gravar nada; alheio, inexistente e malformado, o mesmo 404 (AD-11), provado por igualdade em `negacaoPorDono`. Não existe par administrativo da rota (FR-32).
- **O botão mora no Card do topo, e não em Valores.** Em `AGUARDANDO_PAGAMENTO` o Detalhe não aparece, então o lugar que a 6.2 anunciou deixaria o botão faltando num dos quatro Status da janela. O botão (`outline`) fica no Card do topo nas três superfícies; a frase de quando ele não existe mudou-se para a região viva do Status, e a da corrida perdida ("Este Pedido foi enviado enquanto você estava nesta tela e não pode mais ser cancelado.") ocupa o lugar dela — nunca `Toast`, nunca a mensagem crua do Go. O `Dialog` nomeia o Pedido pelo número, é controlado e vive fora do botão (o botão some com a releitura), não fecha por Esc, clique fora ou "Manter o Pedido" durante o envio, e fecha **antes** de reler: aberto, ele esconde o resto da página do leitor de tela, e a mudança de Status não seria anunciada. Decisão e texto em funções puras de `web/lib/pedido.ts` (`podeCancelar`, `desfechoDoCancelamento`, `fraseSemCancelamento`, `textosDoCancelamento`), testadas; nenhum texto nomeia a Reserva de Estoque nem promete reembolso.
  - *(7.3: a frase da corrida perdida passou a valer só enquanto o Status for `ENVIADO` (`33ca441`), e sai de cena quando ele avança)*
- **EMENDA (revisão do passe 1): a janela vem do Go, em `pode_cancelar`.** A primeira versão copiava os quatro Status em `web/lib/pedido.ts` — regra de negócio no Node (AD-10), que divergiria da tabela em silêncio. O Detalhe do Pedido ganhou `pode_cancelar`, calculado como `slices.Contains(pedido.Permitidas(status, COMPRADOR), CANCELADO)`: uma fonte só, a tabela do AD-3, e a tela só o lê. Na mesma revisão: o `POST` do cancelamento ganhou o prazo de 30 s do Confirmar Pedido (`PRAZO_DO_CANCELAMENTO_MS`, igual a `PRAZO_DA_CONFIRMACAO_MS`), porque o `Dialog` não fecha durante o envio e uma requisição pendurada o prenderia aberto; e a corrida com a aprovação ainda não comitada, afirmada acima, ganhou teste — e morre com a mesma mutação do `FOR UPDATE`.

**Estória 6.4 — Painel de Pedidos do Administrador** (2026-09-23):

- **O `de` da transição vem da TELA, e é essa a chave do compare-and-swap.** `POST /api/v1/admin/pedidos/{id}/transicoes` recebe `de` e `para` no corpo, e `de` é o Status que o Administrador tinha na frente quando clicou. É ele que faz o CAS distinguir os dois desfechos que a UX exige separados: o Pedido que outro ator já moveu sai `ESTADO_JA_AVANCADO` (informativo, com o Status atual em `dados.status`), e a transição que não existe na tabela sai `TRANSICAO_INVALIDA` (destrutivo, com `dados.permitidas`). Lido no servidor, a corrida sumiria: o POST viraria `SEPARANDO → SEPARANDO`, que não está na tabela, e a recusa sairia como transição inválida — causa falsa, que a EXPERIENCE proíbe nomear. Preso por mutação: trocado o `de` do corpo pelo Status lido em `TransicionarPeloAdministrador`, o subteste da corrida contra a simulação morre com exatamente essa troca de código.
- **A transição administrativa lê o Pedido travado antes do CAS, no molde de `Cancelar`.** `TransicionarPeloAdministrador` abre com `TravarPedidoParaAdministrador` — `FOR UPDATE` que espera, sem dono no `WHERE`, porque o Administrador alcança o Pedido de qualquer Comprador — e só então chama `Transicionar(…, ADMINISTRADOR)`. A trava dá duas coisas de uma vez: o 404 do Pedido inexistente ou malformado (que o CAS vazio não distingue da corrida) e o Status que a recusa leva em `dados.status`, que é o mesmo que o CAS vai ver. Nenhum `UPDATE` de Status novo, e o efeito sobre o Estoque continua sendo de `Transicionar`: `SEPARANDO → ENVIADO` consolida, e não há uma linha a mais em `api/` por causa disso.
- **O Administrador não cancela, e isso não é um `if`.** Não existe rota administrativa de cancelamento (FR-32), e `{para: CANCELADO}` chega à rota de transição e sai `TRANSICAO_INVALIDA` como qualquer outra transição inexistente — porque a tabela do AD-3 simplesmente não tem linha de `AtorAdministrador` para `CANCELADO`. Pelo mesmo mecanismo não há um quarto botão: eles são montados de `permitidas`.
- **`permitidas` viaja pronto nas duas respostas, no molde de `pode_cancelar` (6.3).** A linha da Tabela e o Detalhe carregam `permitidas` — `pedido.Permitidas(status, AtorAdministrador)` — e `terminal`. A tela monta os botões da lista que recebeu; o 200 traz o `permitidas` novo para quem chama a API direto ter o desfecho completo numa resposta só (AD-18), mas a tela **relê a superfície** nas três saídas — o 200 e as duas recusas —, porque é a leitura que traz também a linha do histórico. Nenhum Status e nenhuma transição são declarados em JavaScript (AD-10). `pedido.Statuses` existe pela mesma razão, um nível acima: o filtro da Tabela valida contra a lista da máquina, e não contra uma oitava cópia dos sete nomes em `api/`.
- **O Detalhe administrativo é leitura própria, e não o do Comprador com um parâmetro nulo.** `BuscarPedidoDoComprador` tem o dono no `WHERE`, que é a garantia do AD-11, e afrouxá-lo com um `IS NULL OR` deixaria a leitura do Comprador a uma linha de virar leitura de todo mundo. `BuscarPedidoParaAdministrador` é consulta própria, com `comprador_id` a mais; e `saidaPedidoAdminDetalhe` não carrega `expira_em`, `tentativas_restantes`, `pode_cancelar` nem `disponivel` por Item — são da decisão de quem comprou. O que ela tem a mais é o Comprador (nome e e-mail, por `identidade.BuscarComprador`, porta nova do módulo — nunca um JOIN entre schemas, que o AD-2 proíbe) e o `vendedor_nome` congelado de cada Item.
- **O `vendedor_nome` saiu na consulta que já existia, e não numa segunda.** A coluna está em `pedido.item_pedido` desde a 5.6 e a linha já era lida: `ItensDoPedido` passou a trazê-la, e quem não a exibe (o Comprador) simplesmente não a serializa. Nenhuma migração, nenhuma coluna nova.
  - *(7.3: correção: a coluna está em `pedido.item_pedido` desde a 1.6, e não desde a 5.6)*
- **A Tabela ordena pela data do histórico, como "Meus pedidos".** `min(ocorrido_em)` continua sendo o instante de nascimento (6.1) — nenhuma coluna `criado_em` no Pedido —, e as duas ordenações (`recentes`/`antigos`) usam o `CASE WHEN @ordenacao` de `busca`, com desempate final em `id` (AD-18). O filtro por Status é opcional por `IS NULL OR`, e `ContarPedidosAdmin` repete o `WHERE` palavra por palavra: um filtro que divergisse pagina sobre um total que não é o da lista.
- **O bloco de ação é um componente só, usado nas duas superfícies.** Os botões e os dois `Alert`s aparecem na linha da Tabela e no Detalhe, e moram em `web/app/admin/pedidos/acoes-do-pedido.tsx`: dois tratamentos do mesmo desfecho divergiriam na primeira mudança. O desfecho em si é função pura testada (`desfechoDaTransicao`), e nas três releituras — sucesso e as duas recusas — quem traz o Status novo é a leitura, nunca uma montagem local. Nada de `Toast` para o Pedido que já avançou: é `Alert` neutro, com a frase "Este Pedido já está em {estado}. A linha foi atualizada.".
  - *(7.3: substituída pela correção `7014591` — o avanço pede `Dialog` de confirmação, o sucesso tem `Alert` próprio, e o desfecho é reportado à superfície, que o mostra fora da linha. O porquê está na entrada da 7.3)*
- **O estado inteiro da Tabela vive na URL** (`?status=&ordenacao=&pagina=`), lido e montado por uma função só (`filtroDaURL`/`consultaDaTabela`): a URL e a chamada à API saem da mesma montagem, e o padrão fica de fora do endereço. Valor fora das listas fechadas cai no padrão na tela, em vez de ir ao Go levar 400 — o que chega ali é o que alguém digitou na barra de endereços; o 400 `CAMPO_INVALIDO` continua existindo para quem chama a API direto.

**Estória 6.5 — Pagamento aprovado sobre Pedido cancelado, visível ao Administrador** (2026-09-23):

- **O sinal é derivado na leitura, e a junção é em Go.** `pagamento_aprovado_sobre_cancelado` não é coluna nem Status: é "o Pedido está `CANCELADO`" **e** "existe confirmação `resultado = 'APROVADO'` em `estado = 'NAO_APLICAVEL_SINALIZADA'` numa Tentativa dele". A segunda metade é de `pagamento`, lida pela porta nova `PedidosComAprovacaoSinalizada(ctx, bd, pedidoIDs) (map[string]bool, error)` — a aresta `pedido → pagamento` do AD-7 já existia, e `pagamento` continua sem saber o que é Status; a primeira é de `pedido`, que junta as duas em `aprovadosSobreCancelado`. SQL cruzando schema seria a aresta que o AD-1 não tem. Sem migração, sem coluna, sem Status novo, e `aplicarConfirmacao` e o `WarnContext` intocados: o log continua não sendo caminho de leitura.
- **O escopo é `CANCELADO`, e só.** A inbox é perguntada só pelos Pedidos `CANCELADO` — Pedido em outro Status nem lê `true` nem custa consulta. Tentativa superada conta (é dinheiro aprovado), `RECUSADO` não conta (é dinheiro que nunca entrou), e o Pedido cancelado **depois** de `PAGO` com uma segunda aprovação sinalizada casa de propósito: é cobrança duplicada. Ficam **fora** a aprovação de Tentativa superada com o Pedido ainda ativo e a aprovação sobre Pedido expirado (`PAGAMENTO_RECUSADO` por `TEMPO_ESGOTADO`): inalcançáveis com o Provedor Simulado — a primeira Tentativa ou recebe a sua única confirmação (a recusa que a supera) ou não recebe nenhuma (a faixa que expira), e da segunda em diante ele aprova dentro do prazo, com a expiração adiada enquanto houver confirmação pendente —, seguem cobertas só pelo `WarnContext`, e a decisão está no `deferred-work.md`. Estender o sinal a Pedido que não está `CANCELADO` é conversa a ter antes, não durante.
- **Em lote, e não um `bool` por Pedido.** A Tabela do Administrador lê a inbox numa consulta só para a página inteira (`t.pedido_id = ANY(@pedido_ids::uuid[])`, `DISTINCT`), com os ids `CANCELADO` dela; uma porta por linha seria N+1 contra a inbox. O Detalhe usa a mesma junção com um id, dentro da transação `REPEATABLE READ` da rota, então a porta vê o mesmo instante do Status. `ListarParaAdministrador` passou a devolver `[]PedidoAdmin` (o Pedido e o sinal) e `DetalheAdmin` embute `PedidoAdmin`.
- **O campo existe sempre nas duas respostas administrativas, e em nenhuma do Comprador.** Está em `saidaPedidoAdmin`, sem `omitempty` — `false` quando não se aplica, nunca ausente —, e não em `saidaPedido`, que a resposta do Comprador compartilha. O 200 da transição do Administrador o leva `false` sem ler a inbox: o destino dele nunca é `CANCELADO`, porque a tabela do AD-3 não tem essa linha para o Administrador.
- **Persistente é o registro não sumir, e não chegar ao vivo.** `CANCELADO` é terminal e as duas telas param de consultar; a aprovação que chega depois aparece na próxima abertura ou recarga. Fazer a Tabela consultar enquanto listar `CANCELADO` a faria consultar para sempre. Na tela, o Detalhe mostra um `Alert` neutro, sem botão de fechar, abaixo do Status ("Pagamento aprovado sobre Pedido cancelado" / "O Provedor de Pagamento aprovou uma Tentativa de Pagamento deste Pedido cancelado. O Status do Pedido não muda, e a aprovação fica registrada na Tentativa de Pagamento."), e a linha da Tabela ganha um `Badge` `outline` "Pagamento aprovado" na mesma célula do Status, para o leitor de tela ler os dois juntos. Texto, e não cor — nem o verde nem o laranja do DESIGN —, e nenhuma frase promete estorno nem reembolso. Presos por mutação: tirar `resultado = 'APROVADO'` da consulta acende a recusa sinalizada, e tirar a condição `CANCELADO` do Go acende o `PAGO` com a segunda aprovação sinalizada.

**Estória 6.6 — Simulação de entrega** (2026-09-24):

- **Nenhuma linha de produção mudou: a 6.6 prende por teste o que a simulação da 1.8 já fazia.** A FR-33 estava inteira em `SimularEntrega` e `avancarPeloTempo`, mas só o caminho feliz e o interruptor tinham prova, e o resto era afirmado em comentário. `api/simulacao_test.go` chama `pedido.SimularEntrega` direto, com o estado montado por `INSERT` (no molde de `cmd/azamon/main_test.go`), histórico de uma hora e intervalo de meia hora, e prende três linhas da FR-33 e do AD-6. Toda asserção de "não mexeu" confere o Status, a contagem do histórico e o estado da Reserva, e não só o Status:
  - **O que nunca move.** `CANCELADO`, `AGUARDANDO_PAGAMENTO`, `PAGAMENTO_RECUSADO` e `ENTREGUE` vencidos ficam intactos, e a chamada devolve `nil`.
  - **O Pedido travado.** Com a linha presa por outra transação aberta (o Administrador no meio da transição), o `SKIP LOCKED` pula o Pedido sem erro. O `PAGO` do lado avança na mesma chamada, e, solta a trava, a chamada seguinte leva o travado.
  - **O Pedido que falha.** A Reserva `ATIVA` maior que o `estoque_total` faz o `CHECK (estoque_total >= 0)` derrubar o `Consolidar`, sem gancho nenhum. O Pedido fica em `SEPARANDO`, com a Reserva `ATIVA` e o Estoque intacto, e o erro nomeia só ele. O `SEPARANDO` saudável, criado depois, vai a `ENVIADO` e consolida na mesma chamada. O que falha nasce antes porque `PedidosParaAvancar` ordena por `id` (uuidv7): só assim juntar as falhas se distingue de desistir na primeira.

  Presos por mutação, cada uma sozinha: o mapa `simulacao` com `CANCELADO` ou com `AGUARDANDO_PAGAMENTO`, o `SKIP LOCKED` trocado por trava que espera (a chamada tem prazo curto, e o estouro é falha), e `return` na primeira falha no lugar do `append`. O subteste roda por último em `TestSessaoEProduto`: `SimularEntrega` é global, e o Pedido que falha continua falhando em toda chamada seguinte.
  - *(7.3: substituída por `a610f86` — o subteste desarma o Pedido que falha, e a posição dele em `TestSessaoEProduto` ficou indiferente.)*
- **Reiniciar retoma de onde parou, e agora isso está medido no binário.** `cmd/azamon/main_test.go` ganhou um terceiro arranque. O binário com a simulação desligada deixa um `PAGO` e um `SEPARANDO` vencidos, cada um com Reserva `ATIVA` de um Produto só dele. O arranque seguinte, com a simulação ligada, leva os dois a `ENTREGUE`, com 3 e 2 linhas `SIMULACAO` e uma consolidação cada. O que ele prova é que nada se perde na troca de processo: cada Pedido retoma do Status em que estava, sem refazer etapa, e consolida uma vez só. Ele **não** prova que o instante do avanço sai do histórico — com o intervalo de 200 ms, um relógio em memória zerado no arranque também entregaria os dois a tempo; essa prova é a de `api/` (`simulacaoDeEntrega` e `simulacaoDeEntregaFR33`), em que o histórico de uma hora vence o intervalo de meia hora já na primeira chamada. Antes de subir o binário novo, o teste confere que o desligado não mexeu em nenhum dos dois.
- **`ErrEstadoJaAvancado` não tem cenário próprio na simulação, e é de propósito.** Com a linha presa por `TravarPedido`, o CAS de `Transicionar` na mesma transação não perde, e o ramo é defensivo. O "alguém chegou primeiro" do AD-6 acontece de verdade no `SKIP LOCKED` e na reconferência do Status sob a trava. A releitura travada de `Desde` continua sem teste que a mate: alcançá-la pede gancho dentro de `avancarPeloTempo`, e a entrada da 5.11 no `deferred-work.md` segue aberta.
- **O teste do binário que piscava passou a ler o histórico.** O `noPrazo` de `oTiqueExpiraNaOrdemDoAD6` era julgado pelo Status corrente, e a simulação a 200 ms podia levá-lo adiante de `PAGO` antes da leitura. Agora a asserção diz que a primeira saída de `AGUARDANDO_PAGAMENTO` é para `PAGO` e que nenhuma linha vai a `PAGAMENTO_RECUSADO`. Ela passa esteja o Pedido em `PAGO` ou já adiante, e continua morrendo se a expiração vencer a aprovação. Isso fecha a entrada da 6.3 no `deferred-work.md`.

**Estória 6.7 — a forma de exibição dos sete Status** (registrado na 7.3, 2026-09-25):

- **`SeloDoStatus` é a forma única do Status do Pedido nas três superfícies, e a aparência é dado em `lib/`.** `aparenciaDoSelo` devolve `{ variant, className }` e fica em `web/lib/pedido.ts`, onde o `node --test` a prende classe por classe. O componente só repassa o resultado ao `Badge`. As classes começaram no componente, e assim trocar `falha` por `entregue` passava com a suíte verde. O verde fica fora do mapa, numa comparação isolada com `ENTREGUE`. Status desconhecido cai em `progresso` e nunca sai verde: a cor de fim bem-sucedido não se adivinha. (`web/components/selo-do-status.tsx:10-17`; `web/lib/pedido.ts:319-371`; spec `spec-6-7-a-forma-de-exibicao-dos-sete-status.md:17`, `:22`, `:78`)
- **A guarda de fonte é o mecanismo de teste de tela do `web/`.** O `web/` não monta componente em teste. Por isso `web/scripts/pedido.test.mjs` lê o código-fonte das telas e prende três coisas:
  - as quatro telas contêm `<SeloDoStatus` e não chamam `rotuloDoStatus` solto;
  - o verde de `ENTREGUE` (`bg-`/`border-`/`ring-`… `-available`, `--available`, `#007600`) só aparece em `lib/pedido.ts` e `globals.css`;
  - `text-available` só aparece nas duas telas de Estoque.

  `npm test` roda `node --test "scripts/*.test.mjs"` e está no `prebuild`, então a guarda também derruba `docker build ./web`. A spec a justifica assim: é o que impede uma quinta forma de exibir o Status de voltar. (`web/scripts/pedido.test.mjs:282-344`; `web/package.json:6-7`; spec `spec-6-7-a-forma-de-exibicao-dos-sete-status.md:65`, `:94`)

**Correção `7014591` — confirmação e anúncio do avanço de Status** (registrado na 7.3, 2026-09-25):

- **O `de` da transição administrativa é o Status visto no clique que abre o `Dialog`, e não o de quando se confirma.** O clique guarda `{de: pedido.status, para}` e o POST sai desse destino guardado. Se a consulta mudar o Pedido com o `Dialog` aberto, o compare-and-swap do Go ainda nomeia a corrida como `ESTADO_JA_AVANCADO`. (`web/app/admin/pedidos/acoes-do-pedido.tsx:66-68`, `:91-103`, `:152-155`; spec `spec-6-4-confirmacao-e-anuncio-do-avanco-de-status.md:21`, `:79`)
- **Com o `Dialog` de avanço aberto, a Tabela do Administrador pausa a consulta de 10 s, e as outras superfícies não.** `AcoesDoPedido` avisa a abertura por `aoConfirmar`, e o intervalo pula o tique enquanto `confirmando` for verdadeiro. Sob filtro, a releitura tiraria a linha, e o `Dialog` junto, no meio da decisão.
  - O Detalhe administrativo não pausa. A spec não registra o porquê; a razão dada para a Tabela (o filtro) não se aplica a ele, que não tem filtro.
  - O `Dialog` de cancelamento do Comprador também não pausa (decisão O3). Ele fica no nível da página e não desmonta, e a corrida já termina com a frase certa.

  No sucesso, a linha apaga o próprio Status anterior antes de reler, para que a região viva do intervalo não repita o anúncio que o `Alert` do desfecho já fez. (`web/app/admin/pedidos/pedidos.tsx:130-139`, `:273-281`; `web/app/admin/pedidos/[id]/detalhe-admin.tsx:144`; `web/app/admin/pedidos/acoes-do-pedido.tsx:56-59`; spec `spec-6-4-confirmacao-e-anuncio-do-avanco-de-status.md:54`, `:64`, `:88`; `handoff-historico-ate-epica-6.md:18`; `spec-dialog-e-foco-d2-d6-d7-e2-e3.md:23`)

**Defeitos D3 e D5 — preço por Item conferido sob a trava** (2026-09-24):

- **Sob a trava, cada Item confere o preço contra o preço visto, e não só as somas.** A conferência de subtotal e Frete da 5.6 deixava passar dois preços que se compensam, ou um que cai e cruza o limiar de isenção fechando o mesmo total: o passo 2 já lê o preço novo, então as somas batem nas duas leituras. Agora `produto.PrecoCentavos != item.PrecoVistoCentavos`, lido com os Produtos travados, é `TOTAL_DIVERGENTE` — mesmo código, sem migração e sem mudança no `web/`, cuja releitura já leva ao passo Endereço com o "de X para Y". A consequência é o AD-17 por inteiro: reenviar com o preço novo **sem** passar pela entrada no checkout é recusado, porque só a entrada grava a ciência.
- **A checagem mora só no passo 5, e há um teste que prende o lugar.** Cenários que mudam o preço antes do POST não distinguem passo 2 de passo 5. O subteste `Janela` troca os preços numa transação aberta que segura as linhas: a criação lê os preços velhos no passo 2, para em `Reservar`, e o commit a solta. Mutações: checagem apagada derruba `Compensados`, `Limiar` e `Janela`; checagem movida para o passo 2 derruba só `Janela`.

**Correções D1–D4, E1, E4 e E5** (registrado na 7.3, 2026-09-25):

- **O Catálogo Semeado tem um Produto de `,95`: "Anotações de um Andarilho", R$ 39,95.** Sem ele, o passo 5 do Roteiro B (a expiração da FR-34) só se demonstrava mudando preço, o que contraria "nada é reconfigurado entre os roteiros". R$ 39,95 com qualquer Frete inteiro fecha em `,95`, abaixo da isenção. O preço mudou no gerador (`go run media/gerar.go`), e não no SQL à mão. Nenhum teste lê esse Produto nem esse preço, então trocá-lo quebra o Roteiro B sem nenhum teste falhar. (`media/gerar.go:86`; `db/semente/001_catalogo_semeado.sql:42`; spec `spec-d1-d4-e1-e4-e5.md:15`, `:40`, `:52`, `:60`)
- **A semente mudou sem trocar `VersaoSemente`, e um banco já semeado só vê o preço novo depois de `docker compose down -v`.** A semente é `INSERT` sem `ON CONFLICT`. Uma versão nova sobre um banco já semeado bate na chave primária e derruba o arranque. (`db/embutido.go:31`; spec `spec-d1-d4-e1-e4-e5.md:42`)

**Correções D2, D6, D7, E2 e E3 — `Dialog` e foco** (registrado na 7.3, 2026-09-25):

- **O foco volta a quem abriu o `Dialog`, e isso é feito uma vez só, no `DialogContent` compartilhado.** Todo `Dialog` do Azamon abre por estado, sem `DialogTrigger`, e o Radix só devolve o foco ao `Trigger`. No `Esc` o foco caía no `body` (WCAG 2.4.3).
  - `DialogContent` guarda o elemento focado em `onOpenAutoFocus`, antes de o `FocusScope` movê-lo; `body` não conta.
  - Ao fechar, devolve o foco a esse elemento, a menos que quem usa tenha chamado `preventDefault` no próprio `onCloseAutoFocus` (título do Pedido na 6.3, `focarBloco` na 6.4) ou que o elemento tenha saído do DOM.
  - A correção edita `web/components/ui/dialog.tsx` em vez de pôr `DialogTrigger` tela por tela, e assim cobre os oito `Dialog`s de uma vez.

  (`web/components/ui/dialog.tsx:50-91`; spec `spec-dialog-e-foco-d2-d6-d7-e2-e3.md:15-17`, `:21`, `:25`, `:64`)
- **Não se usa `autoFocus` dentro de `Dialog`.** O React foca antes do `FocusScope`, o `onOpenAutoFocus` não dispara e nada é guardado. A revisão achou isso no Ajustar Estoque. O Radix já foca o primeiro campo; para focar outro, usa-se `onOpenAutoFocus`. (`web/components/ui/dialog.tsx:55-57`; spec `spec-dialog-e-foco-d2-d6-d7-e2-e3.md:84-88`)
- **Quando uma ação desmonta o próprio botão, o foco vai ao título da tela.** O `h1` recebe `ref`, `tabIndex={-1}` e `outline-none`, no molde da 6.3. "Confirmar o novo preço", no passo Endereço e no Carrinho, faz sumir o `Alert` e o próprio botão, e o foco caía no `body`. (`web/app/checkout/endereco/escolha-de-endereco.tsx:250`, `:300`; `web/app/carrinho/meu-carrinho.tsx:205`, `:267`; spec `spec-dialog-e-foco-d2-d6-d7-e2-e3.md:17`, `:53-54`)

**Testes das retros das Épicas 5 e 6** (registrado na 7.3, 2026-09-25):

- **O `ORDER BY id` da trava do AD-5 tem um teste que morre sem ele.** `travaSegueOrdemDoID` faz três coisas:
  - põe fisicamente o Produto de menor `id` depois do de maior (`ORDER BY ctid`);
  - desliga os índices na segunda transação (`enable_indexscan`/`enable_bitmapscan = off`);
  - fecha o ciclo com a primeira transação.

  Tirado o `ORDER BY p.id`, o Postgres acusa `40P01`. Este teste prende a ordem da trava; `travaDeReservaSerializa` prende só a serialização. (`api/concorrencia_test.go:177-196`, `:208`, `:230`; spec `spec-testes-da-retro-5-e-6.md:16`)
- **O `de` do clique também está preso por guarda de fonte do lado do `web/`.** `pedido.status` aparece uma vez só em `acoes-do-pedido.tsx`, dentro do clique, e o corpo do POST sai do destino guardado. Três mutações a derrubam: o corpo lendo `pedido.status`, a captura movida para o confirmar e o clique sem o `de`. (`web/scripts/pedido.test.mjs:649-664`; spec `spec-testes-da-retro-5-e-6.md:18`)

**Estória 7.1 — o README leva do clone ao sistema rodando** (registrado na 7.3, 2026-09-25):

- **A verificação do NFR-1 passou a ser recorrente, num Registro de subidas dentro do próprio `README.md`.** Cada linha traz data, quem mediu, máquina, Docker e cache, tempo frio e morno, e o que não bateu. A regra é uma linha por semana até a entrega, cada semana por um integrante diferente. A linha "pessoa de fora" fica em aberto. O registro mora no README porque é o arquivo que a pessoa de fora segue, e a data da última subida é o que diz se ele ainda vale. (`README.md:63-83`; spec `spec-7-1-o-readme-leva-do-clone-ao-sistema-rodando.md:18`, `:56`, `:76`)
- **A medição a frio usa um builder BuildKit descartável (`docker buildx create --name azamon-medicao`), e não o `docker builder prune -af` da 1.9.** Assim o cache frio não apaga o cache de quem desenvolve. O pull de `postgres:18.6` e `redis:8.10.1` ficou morno por decisão humana, e o Registro diz isso. Resultado em 2026-09-25: **4 min 28 s**, e 16 s morno. O número não se compara com os 3 min 26 s da 1.9: a 1.9 apagou todas as imagens base, e esta medição pagou 86 s exportando a imagem `web` do builder próprio. O clone leva `-c core.longpaths=true`, porque numa pasta de 156 caracteres o Git para Windows parou em `Filename too long`. (`README.md:25`, `:74`, `:81-97`; `HANDOFF.md:187`; spec `spec-7-1-o-readme-leva-do-clone-ao-sistema-rodando.md:22`, `:26`, `:55`, `:71`)

**Estória 7.2 — o diagrama dos seis módulos corresponde ao código** (2026-09-25):

- **O desenho segue o código, e não o contrário.** O grafo do AD-1 (o mesmo desenho na espinha e no `DIAGRAMA-MODULOS.md`) parou em 2026-09-06 com 14 setas, e a tabela `arestas` de `internal/fronteira_test.go` chegou a 23. O redesenho parte da tabela e rotula cada seta com os símbolos exportados que o código chama, conferidos por `grep`. Entram as 9 arestas que faltavam, de `plataforma`, `db`, `media` e `cmd/azamon`. As de `plataforma`, `db` e `media` são pontilhadas: estão na tabela e são exaustivas, mas não são módulos do NFR-2. `cmd/azamon → api` é cheia. O relógio passa a ser `cmd/azamon`, com os quatro passos do AD-6. `pedido → carrinho` passa a `Itens · ConfirmarPrecoVisto · Esvaziar`, coerente com o AD-3 emendado na 5.5. `ProvedorDePagamento`, nome que nunca existiu no código, vira "porta do AD-8", com as suas duas operações. Nenhuma aresta real pareceu defeito, e a tabela `arestas` não mudou.
- **"`fronteira_test.go` passando é a prova" passou a ser literal (SM-C4).** Antes, o teste conferia a tabela que carregava, e o desenho envelhecia calado. `TestDiagramaEhATabela` extrai de cada arquivo o bloco mermaid cuja primeira linha é `%% AD-1` e exige os dois idênticos, com CRLF normalizado. Depois compara as setas com `arestas` nos dois sentidos: `relogio` é lido como `cmd/azamon`, `web`, `porta` e `simulado` ficam fora da conferência, e nó desconhecido é falha. As quatro mutações da spec (aresta nova só na tabela, seta a mais só no desenho, só a espinha editada, nó desconhecido) derrubam a guarda com a mensagem que nomeia o defeito, e `TestGuardaDoDiagrama` prova cada uma aplicando-a ao desenho e à tabela reais — uma guarda que passasse sempre seria a mesma boa vontade que ela veio substituir. `TestExtracaoDoAD1` prende a leitura do bloco (marca exata, e `%% AD-12` não conta; um bloco só; bloco que não fecha é erro). `TestTabelaEhOCodigo` fecha o sentido que faltava: toda aresta de `arestas` precisa de ao menos um importe real, e a raiz isenta `internal/plataforma` conta como `→ plataforma`, que é o que sustenta `cmd/azamon → plataforma`.
- **Os rótulos não passam pela guarda, de propósito.** Um parser que conferisse cada símbolo contra o código seria cerimônia. O que envelhece em silêncio é a seta, e a seta está presa; o rótulo se confere por `grep` quando alguém mexe na travessia.
- **Os dois arquivos continuam com o grafo.** A espinha é o substrato que os agentes leem, e o `DIAGRAMA-MODULOS.md` é o entregável do §6.1, que se lê sozinho. A guarda é o que impede os dois de divergirem.
- **Quatro achados ficaram no `deferred-work.md`. Três têm estória dona (7.3, 7.3 e 7.4); o quarto ainda não tem dono, e fecha quando alguém mexer em `db/schema_test.go`.** A porta do AD-8 não tem `interface` Go: é a assinatura de `IniciarTentativa` mais a rota do webhook, com `pagamento.Simulado` e `EmitirConfirmacoesDevidas` como adapter, e se isso basta é assunto da 7.3. A tabela do AD-6 na espinha ainda põe os passos 2 e 3 dentro de `pedido.Varrer`, e o código tem `pedido.Expirar` e `pedido.SimularEntrega` separados, o que também é da 7.3. O `deck-banca.html` ainda escreve `ProvedorDePagamento` e não tem `contador_numero`, e é insumo da 7.4. E o subteste "existem só as catorze tabelas do esqueleto" de `db/schema_test.go` não confere o schema `carrinho`, que ficou fora de `schemasDeModulo`. Fechar isso é mexer em código que a 7.2 não toca, então o `DIAGRAMA-MODULOS.md` diz a cobertura real em vez de afirmar a inteira.

**Estória 7.3 — o addendum percorrido contra o código** (2026-09-25):

- **Toda decisão deste documento foi conferida contra o código de `main`, e nenhuma é contradita (SM-7).** São 260, de §1 a §10: 225 confirmadas, 10 trocadas por estória posterior que já dizia por quê, 9 trocadas sem registro (o porquê de cada uma vem abaixo) e 16 históricas, medida ou processo ancorados em commit. O registro, com a evidência `arquivo:linha` de cada decisão, está em `_bmad-output/implementation-artifacts/percurso-do-addendum.md`. Nenhuma decisão antiga foi apagada: onde o código mudou, entrou logo abaixo do item uma nota `*(7.3: …)*` que aponta a entrada nova, e o texto antigo ficou como estava.
- **O segundo sentido: as decisões da construção que nunca chegaram aqui.** Vinte e sete estórias e correções não tinham bloco neste §10 — as Épicas 2, 3 e 4, a 5.2, a 5.7, a 6.1, a 6.2, a 6.7, a 7.1 e as correções dos passeios —, e a CA desta estória pede que toda decisão de arquitetura da construção esteja aqui. Quatro leitores varreram a spec e o commit de cada uma atrás de decisão de mecanismo que nem este documento nem a espinha diziam, e cada uma foi conferida no código. São 109 itens em 27 blocos novos, no lugar cronológico e marcados "registrado na 7.3"; o porquê é o da spec, e onde ela não o dá o item diz isso. A varredura achou também quatro pontos em que a espinha descreve o código de outro jeito (AD-10, AD-19, a assinatura de `Disponivel` e a tabela de rotas do AD-16), que ficaram em `deferred-work.md` junto com o AD-18 e a Semente Estrutural, o teto por Item de Carrinho repetido no `web/`, também adiado, e mais comentários de código velhos, corrigidos.
- **As nove trocas que ninguém tinha registrado, com o porquê.**
  - §4, "uma interface": a porta do AD-8 é contrato, e não tipo — item abaixo.
  - §9, "um arquivo público por módulo": a porta é o pacote. A fronteira do AD-1 é conferida por importe de pacote (`internal/fronteira_test.go`), e o Go não fecha símbolo exportado num arquivo só; `identidade.BuscarEndereco`, em `endereco.go` (2.5), e `catalogo.ErrVendedorComProdutos`, em `vendedor.go`, foram os primeiros a mostrar isso. Os cabeçalhos dos seis módulos, que diziam "o ÚNICO que outro módulo importa", foram corrigidos. A mesma frase na Semente Estrutural da espinha ficou em `deferred-work.md`, porque esta estória só emenda o AD-4, o AD-6 e o AD-8.
  - §9, "três superfícies e mais nenhuma": o Detalhe do Pedido do Administrador consulta a cada 10 s enquanto o Pedido não é terminal (6.4, `081a60e`). A spec da 6.4 não registrou por quê. A leitura desta estória: o Detalhe mostra o mesmo Pedido que a Tabela, e sem a consulta ele pararia no Detalhe enquanto a simulação de entrega o avança na Tabela. É a quarta superfície, e o AD-18 da espinha ainda diz três; a emenda ficou em `deferred-work.md`.
  - 1.3, "um módulo nunca enxerga o schema do outro" no sqlc: o bloco de `busca` no `sqlc.yaml` lê as migrações de `catalogo` (3.5, `423fe9d`). O sqlc precisa do schema para tipar a VIEW `produto_visivel`, que é a única leitura de `busca` no Catálogo (AD-16, AD-19). Enxergar para tipar não é consultar: `busca` não tem consulta sobre tabela de `catalogo`.
  - 1.8, "a única passagem em que `estoque_total` muda": a 3.4 criou a outra, o ajuste do Estoque total pelo Administrador (FR-11), com rota própria e a guarda de Reservas do AD-5. Entre as transições do Pedido, `Consolidar` continua a única, e é isso que a decisão da 1.8 protegia.
  - 1.5, a Sessão com `{id, nome}`: ganhou `papel` na 2.4, para a guarda de prefixo decidir o papel sem voltar ao Postgres a cada requisição, e `email` na 2.6, para a tela de Perfil o mostrar sem uma ida a mais (`internal/identidade/identidade.go:52-62`).
  - 1.6, "não há middleware de sessão": do lado do Comprador continua não havendo. O Administrador ganhou guarda por prefixo, `somenteAdministrador` em `api/admin.go` (2.4), porque a área administrativa tem de ser indistinguível de inexistente para quem não é Administrador — o mesmo 404 do AD-11 —, e uma guarda no prefixo cobre toda rota de `/api/v1/admin/` de uma vez.
  - 6.4, o bloco de ação sem `Dialog`: a correção `7014591` pôs o `Dialog` de confirmação que a `EXPERIENCE.md` pede para o avanço de Status, deu ao sucesso um `Alert` próprio e passou a reportar o desfecho à superfície, que o mostra fora da linha — com a Tabela filtrada, a releitura desmonta a linha, e o `Alert` sumiria junto.
  - 6.6, "o subteste roda por último": `a610f86` (`epic-6-retro-item-32`) desarma o Pedido que falha no fim do próprio subteste, e a posição em `TestSessaoEProduto` ficou indiferente.
- **A porta do AD-8 é contrato, e não tipo.** As duas operações são a função exportada `pagamento.IniciarTentativa` e a rota `POST /api/v1/webhooks/pagamento`; o adapter é `pagamento.Simulado` mais `pagamento.EmitirConfirmacoesDevidas`, que o relógio chama. Com um adapter só, uma `interface` Go seria cerimônia: o Paradigma da espinha põe portas-e-adaptadores só onde a costura é real, e aqui a costura é a assinatura. Na troca pelo Stripe sai o adapter e entra quem chama o gateway depois do commit, também pelo relógio, porque o AD-4 proíbe rede dentro da transação; ficam a assinatura de `IniciarTentativa`, que só grava a Tentativa, a rota do webhook e a inbox. Mudam também a autenticação do handler do webhook — hoje o segredo compartilhado da 1.7, amanhã a assinatura do gateway — e entra o passo síncrono com o navegador que um gateway real pede para coletar o meio de pagamento, que o MVP não tem porque não coleta cartão. O AD-8 foi emendado no lugar, e a entrada da 7.2 em `deferred-work.md` fechou.
- **A ordem de trava do AD-4 nomeia o contador do ano.** `pedido.Criar` trava a linha do ano em `pedido.contador_numero` (`ProximoNumeroDoAno`) antes de inserir o Pedido, porque o número legível vai no `INSERT`; depois vêm os Produtos por `id`, e por último os Itens de Carrinho que `Esvaziar` apaga. Nenhum caso de uso trava o contador depois de um Produto, então a ordem continua sem ciclo. O contador é o primeiro ponto de espera de duas criações do mesmo ano, e serializa toda criação de Pedido: custo aceito nesta escala, e o motivo de o teste de ponta a ponta do NFR-7 não provar sozinho a trava do AD-5 (quem a prova é `travaDeReservaSerializa`). O AD-4 foi emendado no lugar, o comentário da migração `20260921120000_pedido_criacao.sql`, que dizia que o gêmeo espera no índice da chave, foi corrigido, e o `epic-5-retro-item-27` fechou.
- **A tabela do AD-6 nomeia as três funções de `pedido`.** Dizia "idem" de `pedido.Varrer` nos passos 2 e 3; o relógio chama `pedido.Varrer`, `pedido.Expirar` e `pedido.SimularEntrega` — esta só com `AZAMON_ENTREGA_SIMULACAO_ATIVA`, que não cobre a expiração — e depois `pagamento.EmitirConfirmacoesDevidas`. A ordem normativa não mudou. Emendado no lugar; a entrada da 7.2 em `deferred-work.md` fechou.
- **O NFR-4 tem dois números medidos, e o teste só guarda o primeiro.** O p95 de 0,34 ms da 1.4 é de uma sonda que lê `catalogo.produto` direto. A busca da Épica 3 lê a VIEW `produto_visivel`, com o Estoque disponível, filtros `IS NULL OR`, `ORDER BY CASE` e a contagem `ContarVisiveis` em toda requisição, e a spec da 3.7–3.9 raciocinou a partir do 0,34 ms em vez de medir. A retrospectiva da Épica 3 (`epic-3-retro-2026-09-19.md`, 2026-09-19) mediu a forma embarcada sobre os mesmos 5.050 Produtos: **pior p95 de 8,47 ms por requisição** (Vitrine sem termo, lista mais contagem), cerca de 59 vezes abaixo do teto de 500 ms, e o índice GIN continua alcançável. É o número do NFR-4 para a consulta de produção. `db/medicao_nfr4_test.go` ainda mede a sonda, e apontá-lo para a consulta real é o `epic-3-retro-item-13`, aberto; o comentário da sonda deixou de dizer que ela é a forma que a Épica 3 implementaria.
- **Os comentários de código que descreviam o sistema de antes foram corrigidos; nenhum comportamento mudou.** A migração da 5.6 (onde o gêmeo espera), a Revisão (a chave não sobrevive mais ao "Fechar o Pedido"), `api/rotas.go` (as duas rotas de Pedido, o que fecha a entrada da 7.1 em `deferred-work.md`), `catalogo.Consolidar` e a consulta dele (a outra passagem que muda o Estoque total é o ajuste do Administrador), a sonda do NFR-4 e os cabeçalhos dos seis módulos. A lista está no registro do percurso.
- **`internal/pedido/pedido.go` fica em um arquivo.** O B2 da retro da Épica 6 o mediu em 1.196 linhas, com três blocos que não se tocam. Decidido em 2026-09-25, antes deste percurso: a porta é o pacote, e mover 1.200 linhas às vésperas da entrega não muda comportamento nenhum e custa uma revisão inteira.
